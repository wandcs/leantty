# LeanTTY 1.6.0 AppGallery 更新说明

新增 Mosh 连接：网络短暂中断或切换后，可继续使用原有远端会话；支持本地输入预测。
远端需安装 mosh-server 并允许 UDP 通信。

应用意外退出后，可恢复标签页和分屏布局，方便重新开始工作。此功能不保存终端正文，
也不会自动恢复已结束的连接。

改善键盘输入、SSH 主机指纹确认和终端重绘的稳定性，优化性能与资源释放。
