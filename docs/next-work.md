# LeanTTY 当前工作

> 唯一有效的项目 TODO · 更新：2026-09-26
>
> Mosh v0.1.4 与整屏反色修复已合入；首 Tab 诊断和 SGR 5 文字闪烁继续暂停。
> 1.7 候选已冻结，C3 自动阶段通过、人工锁屏与合盖待验；C4 与发布仍暂停。
>
> 上位规则：[产品原则](project-principles.md)、[路线图](roadmap.md)
>
> 执行依据：[编码指南](coding-guide.md)、[质量策略](quality-strategy.md)、[发布流程](release-process.md)

## 09-26 1.7 候选重新定基

候选源码、软件门禁与构建身份已固定，QH/C3 已获准继续；C4 与发布仍暂缓。
首 Tab 和 SGR 5 仍按各自条目暂停。
09-26 维护者已解除 QH/C3 暂停，并确认正式 Mosh 验收使用已保存的备用 Wi-Fi
`CU_lin`；后续沿用此 SSID，不再重复询问，网络前提仍由每轮验收核实。发布仍暂停。

- [x] 核对旧归档与当前源码。09-25 C4 生产提交为 `6a5eb82`；重新定基前的 `main`
  为 `f4a82168c56301a3259786e3daf71403a91d1130`，已增加 Mosh v0.1.4 依赖与整屏
  反色的原生、ArkTS 和 Pane 背景改动。旧 `candidate-1.7.0-20260925-r3` 只作为历史
  证据，不能再作为当前产品的发布包。当前本地/远端 `main` 一致，工作区核对时干净；
  本地和远端均无 `v1.7.0` tag，GitHub Release 未创建。
- [x] 准备 1.7 发布文案：整屏反色修复已移入 1.7 Changelog；候选冻结日期
  定为 2026-09-26，若实际发布需改日期则重新定基、重建。七处版本元数据均为
  1.7.0 / 1007000；AppGallery 文案限定 Mosh 修复范围。中英离线指南修订 14
  已审阅并与打包副本逐字节相同（102388 字节）。`policy,web` 与指南浏览器
  63 项检查通过；浏览器截图和编辑审阅记录保存在本地忽略目录
  `build/verification/guide-review-20260926-r2/`，不代替新候选包内字节核对。
- [x] 候选日期 PR 已合入并推送，精确 C0 为 `718e33716211a5399880cf68a55d690f98dd928a`。
  源码或包内容再变则重新定基；旧 `release/1.7.0` 分支与当前候选已分叉，
  不以该旧分支或旧归档构建新包。
- [x] 同一 C0 的 C1 软件门禁 33/33 通过；C2 ARM64 测试签名包已留存，SHA-256
  为 `3f307a266e5616cfbccf393a190393d4b65feef5eb05004f8c665124c5ab7fb4`。
  测试机安装和启动通过，只证明部署。报告在发布区 `freeze-1.7.0-20260926/c2/`。
- [x] 对同一候选补齐 Mosh 原始二进制输出后独立输入、整屏反色在普通/透明
  分屏与 Surface 重建中的受影响真机证据。首 Tab 原故障仍未归因；维护者要求
  诊断继续暂停，不在候选验收中暗中重启该诊断。
  09-26 已安装的 C2 候选在原样执行 `\cat /bin/ls` 后保留远端提示符；随后独立提交
  `echo LTTY_17_BINARY_ALIVE_20260926` 并观察到回显，App PID 5289 保留，
  协议错误日志为零。DEC `?5h` 显示浅底深字，分屏左反色、右普通；五档透明度
  `FF/D1/B8/99/73` 均由两个 Pane 独立颜色共用相同 alpha。切 Tab 和受控 Surface
  重建后状态保留，页面重建日志确认触发；`?5l` 恢复深底浅字。Mosh 正常退出，
  本次新增主机信任已移除，WSL 仅剩测试前四个 Mosh server；透明度恢复原 `B8`，
  工作区恢复单 Tab、单 Pane。截图含终端内容，仅存本地忽略目录
  `build/verification/candidate-1-7-focused-20260926/`，索引为 `focused-summary.json`。
  这是候选定向证据，不代替尚待人工锁屏、合盖的完整 C3 结果。
- [ ] 同一候选的 C3 自动阶段已通过；待维护者在测试机旁完成独立锁屏与合盖场景，
  使用原 R3 报告和干净 `h11` 工具检出执行 `-Phase operator -Resume`。在两项通过前
  不声称完整实体矩阵通过；首 Tab 与 SGR 5 的暂停不随 C3 自动结果解除。
- [ ] C3 准入及所需证据满足后，从同一新源码制作 C4 生产包和 review 包，核对
  成员、签名、版本、许可与诊断标记，再做新包短 smoke。只有新候选完整闭合且另获
  发布授权，才创建签名 tag、GitHub Release；AppGallery 由维护者提交。

本轮按新候选执行 QH/C3；不启动 C4 或发布动作，不将首 Tab 与 SGR 5 的暂停
误写成已解决。

**09-26 C3 自动阶段结果：** 候选提交 `718e337`、测试包 SHA-256
`3f307a266e5616cfbccf393a190393d4b65feef5eb05004f8c665124c5ab7fb4`，
干净工具提交 `7c48a22`。PR #270 将已审 Pi 0.87.1 加入严格第三方限制判据，
不把 tmux 外层未转发的 OSC 777 认作 LeanTTY 的系统通知通过。R1 原失败报告保留；
R2 在 QH 读取夹具就绪文件时遇到 Windows 文件占用，正式报告失败，随后已独立确认
夹具进程、监听端口、临时目录与 HDC 映射清理。R3 重新入场审计后，QH、8 项 Agent、
完整 SSH 矩阵和 6 项自动 Mosh 场景通过；报告中 9 项新阶段 passed、16 项有凭据 reused。
Wi-Fi 切换使用已保存的 `CU_lin`，切回原网络及清理通过。正式报告
`freeze-1.7.0-20260926/c3-automatic-r3/release-report.json` 为 `awaiting-operator`，
两项人工阶段均 pending、attemptCount=0，完整矩阵标志为 false。C3 当前等待人工
锁屏/合盖；本候选的原始二进制输入与整屏反色定向证据见上，不启动 C4。

**09-26 正式自动验收中断与有界诊断：** 同一 C2 候选的 QH 及 Agent 之前的
16 个行为阶段通过；Agent 第 6 项 Pi/tmux 在内层观察到 OSC 777、正常退出，
完整外层 PTY 为零通知帧，LeanTTY 未收到信号。Agent 自有清理通过，后缀未启动；
原报告 `freeze-1.7.0-20260926/c3-automatic-r1/release-report.json` 保持 failed。
预期是已传到 LeanTTY 的通知得到处理；最后正确边界是 Pi 内层发帧，首个缺失边界是
tmux 外层转发，tmux 拥有该边界。候选的通知分类器只承认已审 Pi 0.84.4，当前 Pi
为 0.87.1；两版安装的通知扩展哈希相同，tmux 均为 3.6。09-26 零模型、无设备
的裸帧/包裹帧探针再次确认裸 OSC 777 不转发、DCS 包裹帧转发，证据在
`build/verification/agent-tmux-boundary-20260926/`。下一步只把已审 0.87.1 加入
严格身份判据，保留未来版本及反例拒绝；验证工具后冻结新干净检出，先审计设备和
独立残留，再检验现有 `agent-split-R2` 是否能复用无关前段。不能热改原验收报告，
不能把第三方未转发写成系统通知通过，也不自动重发已执行的模型请求。

## 09-25 Mosh 使用问题与库修复交接

- [x] 定位滚动、二进制输出断连与上下键历史三项反馈。滚动符合现行临时页面合同；
  WSL zsh 错误历史绑定已备份并改为内置 widget，新 Mosh 会话真机上下键通过。
  `\cat /bin/ls` 断连已缩小为单个合法 `U+FFFD`：隔离 `mosh-client-rs v0.1.1`
  返回 `Terminal(UnsupportedCharacter)` → `Protocol`，stock Mosh 1.4.0 客户端对照正常。
  详见[诊断交接](design/mosh-replacement-character-diagnosis-20260925.md)。
- [x] 接入维护者已发布并批准升级的 `mosh-client-rs v0.1.2`，核对 U+FFFD 修复及
  私有 vt100 补丁来源，同步依赖与许可证记录。只做受影响 Rust/策略检查、最小
  stock-server 对照、ARM64 签名构建及真机 U+FFFD / 原始 `\cat /bin/ls` 定向验证；
  不重跑正式矩阵，不扩大为任意二进制兼容工作。若原始样本暴露其他错误则保存新
  错误边界再报告，不把库修复声明直接当作真机通过。
  锁定 `177d2a1`；受影响软件检查、stock-server 对照、ARM64 签名构建与真机均通过。
  真机可显示两种来源的替换字符，原始二进制样本完成后后续命令正常回显；本轮会话
  正常退出，服务端进程消失，原中文拼音模式和终端焦点恢复。证据与包身份见上述诊断。
- [x] 维护者随后在新包直接执行 `\cat /bin/ls` 仍退回 ltty，重新打开断连诊断。
  PID 19877 的 21:13:18 日志确认 `mosh_udp / protocol`；既有通过只覆盖当时样本，
  不能视为原主诉已解决。WSL `cat` 为 bat 别名，绕过别名后的 PATH 程序与 `/bin/cat`
  指向同一可执行文件。预期正常远端输出不结束连接；最后正确边界是 Mosh 已连接，
  首个错误边界是库任务返回 Protocol，终端协议状态由 mosh-client 库拥有。
  本批 20–30 分钟，先保存现场，在隔离 v0.1.2 副本区分错误子类；比较原始直接输出
  与旧测试结束时立即 RIS 清屏的状态/时序差异，不以反复运行同一样本撞通过。
  不改产品或库项目、不加容错分支、不重跑矩阵或模型；新证据进入原诊断文档。
  若不能收窄，保留新失败和缺口后重审，不将单次通过再次作为修复结论。
  **已收窄并交接，库修复待办：** 两种 cat 写法在交互 zsh 隔离对照均曾失败；新增
  最小样本为行首 U+0605。libc 认为宽 1，库认为宽 0，并在 scalars=0 时误报
  `TooManyScalarsInCell` 后返回 Protocol。stock 客户端正常，当前新包真机同样退回
  ltty；测试 Tab/服务端清理、原 Tab 和输入法恢复。完整源码边界及建议见原诊断文末。
  仅移除报错可能造成宽度漂移，需由库修复统一语义；本轮未修改库或产品，不撤回有效
  U+FFFD 修复。后续大样本不得在确认存活前立即清屏，先验固定字符/光标与后续输入。
  **v0.1.3 接入：** 维护者已发布修复并继续本工作链。锁定标签提交 `77f2101`，审查
  共享宽度策略和资源限制，更新依赖/许可证记录；只跑受影响 Rust 检查、stock-server
  的固定宽度与二进制后续输入样本、ARM64 构建及真机定向回归。先验证 U+0605 和
  U+FFFD，再直接执行 `\cat /bin/ls` 并单独提交后续输入，确认前不得清屏。软件或真机
  出现新失败则保留证据、停止扩大验证；不重跑正式矩阵，不调用模型，不改库项目。
  **v0.1.3 真机仍失败：** U+0605 行首/ASCII 后及 U+FFFD 样例通过；直接执行
  `\cat /bin/ls` 在 23:08:10 返回 `mosh_udp / protocol`，PID 58811 保持。停止后续
  验收和合入，保存截图/日志并清理本轮服务端。追加最多 15 分钟隔离诊断：预期原始
  输出后仍可输入，最后正确边界为已连接且固定字符正常，首错仍为库 Protocol；状态
  所有者是 mosh-client。只在忽略目录副本增加错误类别和数值边界，区分另一种资源/
  字符准入错误与网络错误，不改处理行为。不能收窄则保留失败并交接，不反复撞通过。
  **新错误已收窄：** 隔离交互 shell 捕获 `Terminal(UnsupportedOsc)`；普通 OSC 0/2
  标题 `hello;world` 即可复现，`hello-world` 正常，stock 客户端正常。私有 vt100
  只接受两段参数，标题中的分号导致回调被当作致命错误；详见原诊断文末。v0.1.3
  保留为独立草稿 PR，暂不合入。
- [x] 维护者要求将断连改为一次批量诊断，不再逐个发现后交接。固定 v0.1.3，先审计
  所有会话致命出口；在忽略目录隔离副本录制受控 stock-server HostBytes 与原始状态，
  记录错误后仅在诊断副本推进解析，继续消费到原始输出及独立后续输入完成。离线
  重放全部记录，枚举同一批中的多个错误，提取最小正反例，按根因合并；再用未改
  错误处理的固定源码复验案例。网络/鉴权/资源防线与正常输出误拒绝分别标注。
  不把跳过错误后的状态当作产品通过；不改产品/库项目、不安装新包、不扩大正式
  矩阵、不调用模型。预期是交付可一次执行的 case 集与致命分支覆盖表，明确录制样本
  和静态审计边界，不声称穷尽任意输入。先做 45 分钟离线批次，若成本越界按既有
  重审规则收敛；记录原始样本、完整读完的证据、每个失败和清理结果。
  已完成：原始样本 48 个差分完整读到独立后续命令，8 次拒绝归并为标题分号；另做
  277 个直接输入与 271 个 stock 输出扫描，三份记录共 325 帧、32 次拒绝全部重放。
  归并为七个真实 stock 拒绝族，并另列五类原始输入/合同边界。原版独立 stock
  19 项正反例（12 失败、7 正常）与官方客户端九项对照均已记录；124 项原有库单测
  通过。完整案例、致命出口和批量脚本见
  [批量交接](design/mosh-disconnect-batch-20260925.md)。本轮不实施库修复，不放宽
  资源/策略。后续按全组处理和验证，不回到单例修复循环；升级 PR 保持草稿。
- [x] 接入维护者发布的 `mosh-client v0.1.4`，按完整案例包和独立 stock Session
  复验，再做受影响软件检查、ARM64 测试签名构建和真机原始 `\cat /bin/ls` 后独立输入。
  OSC 标题、SGR 闪烁/隐藏、整屏反色按修复验收；鼠标 1001/1015、非法 OSC 52、
  每格 scalar/字节限制仍应拒绝。视觉效果以当前原生终端真机观察单独记录，不用
  xterm.js 功能表推断。仅在该工作链通过后处理 PR #264；首 Tab 诊断与 1.7 发布
  保持暂停，不启动完整矩阵或模型请求。
  已核对发布标签指向 `8a45c8e68aa586afa5bebc241fad6d8d80259536`，PR #4 已合入；
  固定源码的 19 项 HostBytes 案例恰有 7 项预期变化，277 项扫描恰有 21 项变化，
  其余保持原结果。独立 stock Session 的 19 项正反例与原始二进制输出后续输入通过；
  LeanTTY 锁定 v0.1.4，ARM64 图仍为 178 个 registry 包、一个 Git 包且许可字段齐全。
  `policy,rust-core,rust-native`、原生终端与设备脚本合同检查通过，ARM64 调试签名包
  已构建。真机原样提交 `\cat /bin/ls`，确认二进制输出后再单独提交固定后续命令，
  远端继续回显且 Mosh 会话保持；App PID 未变，过滤日志没有协议错误。随后正常 `exit`
  回到 ltty，原三个 Tab 保留、首 Tab 焦点恢复，应用 TCP/UDP socket 数均归零；WSL
  仅剩测试前原有四个 server 进程。证据在本地忽略目录
  `build/verification/mosh-v014-20260926/`，截图含终端内容，不上传仓库。原始 ST 终止
  及零宽字符分段仍按既有边界另行处理。PR #264 的四项云端检查通过，已合入 main
  (`f7d7808d97e50da82614fc9c697d19a411c8e32f`)；本地 main 与远端一致。
- [x] 单独诊断并修复 v0.1.4 整屏反色在当前原生终端的视觉边界，不把库的状态输出等同实机
  呈现。真机受控 SGR 8 隐藏文字不可见、SGR 28 后续文字可见；间隔 700 ms 的两张
  截图中 SGR 5 文字均可见，尚不能判断其闪烁时序。发送 DEC `?5h` 后会话仍存活，
  但屏幕历史内容在截图中变为空白、提示符仍可见；发送 `?5l` 后内容恢复，未见预期
  整屏反色。上述受控测试已在退出会话前复位模式，截图保存在同一本地忽略目录。
  **只读定位（09-26）：** 预期 DEC `?5h` 将终端网格显示为浅底深字，`?5l` 恢复；
  最后正确边界是固定 Ghostty RenderState：本地独立探针确认文字与 SGR 5 样式保留，
  反色模式开启，默认前景/背景从 `205,214,244` / `30,30,46` 交换为
  `30,30,46` / `205,214,244`。首个错误边界是 LeanTTY 原生 renderer：它以零 alpha
  清屏，默认背景单元格不画矩形，交换后的深色字落在固定深色 ArkUI 承载面上。
  SGR 5 的 `style.blink` 也到达 renderer，但当前绘制没有使用它；600 ms 定时器只按
  光标闪烁模式重绘。独立探针源码/输出在本地忽略目录
  `build/verification/mosh-v014-20260926/ghostty-reverse-probe.*`。
  **取舍门禁：** 直接在 renderer 为反色画不透明底最局部，但透明模式会突然失去透视，
  与产品原则 §4.7 的单一透明度所有权冲突；维持透明度则需按 Pane 将终端反色状态
  协调到 ArkUI 承载面，增加状态和生命周期验证。文字闪烁还需独立决定周期重绘与
  能耗成本。先向维护者说明收益、替代方案和成本，明确取舍后才做实现性探针或改产品；
  不以库状态输出或本次只读定位声明视觉效果通过。
  **维护者决策（09-26）：** 保持既有透明度契约，批准将工作区背景所有权迁至每个
  Pane，以便分屏两侧独立反色且不叠加 alpha。此轮范围是 DEC `?5h/?5l` 状态跨原生
  worker、ArkTS controller 和 Pane 背景同步；验收普通主题、透明度档位、分屏、切 Tab
  与 Surface 重建。停止条件是受影响软件检查及具名真机视觉场景通过；SGR 5 文字闪烁
  作为独立时序问题记录，不以本轮反色修复的结论覆盖。
  **修复与验证（09-26）：** 原生 worker 在 DEC `?5h/?5l` 或临时页切换时发出当前反色
  状态；ArkTS controller 跨 Surface 生命周期保留状态；工作区背景迁到各 Pane，反色用
  默认前景 RGB，同时沿用当前透明度 alpha。`terminal-native`、`arkts,policy` 聚焦软件
  检查及 ARM64 调试签名构建通过。实体 PC 的第三个测试 Tab 中，`?5h` 后浅底深字可读，
  `?5l` 后恢复深底浅字；分屏左反色、右普通，切 Tab 及受控页面重建后仍保留。页面
  重建日志确认动作实际发生。五档透明度中，左右背景分别共用 `FF/D1/B8/99/73`
  alpha，测试结束已恢复原“关闭”档位。新增右 Pane 已关闭，Mosh 正常 `exit` 后三个
  空闲 Tab 保留，App TCP/UDP socket 均为零。证据为本地忽略目录中的 `fixed-*.png`
  及构建日志，截图含终端内容，不上传仓库；这轮是变更定向真机观察，不代替正式发布验收。
- [ ] 单独决定并验收 SGR 5 文字闪烁呈现。库与 Ghostty 保留 `style.blink`，当前
  renderer 没有文字闪烁时序；是否加入周期重绘和相应能耗尚未定。维护者 09-26
  要求继续暂停，本轮不实施或验收文字闪烁。

## 09-25 实际使用中的终端异常

- [ ] 定位首个 `user@hsl` Tab 的暂停提示。维护者不确定触发步骤，已批准先做 30 分钟
  诊断。初始现场安装 review HAP `25c4de55…78cbd9`，进程 PID 50155，产品 `6a5eb82`；
  初始截图与进程日志保存在 `build/verification/display-unavailable-20260925/`。
  预期是正常终端交互持续可用；最后已知正常边界为之前短 smoke，首次观察到异常为
  实际会话首 Tab 常驻提示且无重试按钮，二者之间的准确触发时刻未知。
  UI 提示由 NativeTerminalController / NativeTerminalPane 拥有，既可能来自显示恢复，
  也可能来自终端失败，不能直接归因 GPU 或远端网络。先用现有日志、UI 与连接状态区分
  局部显示失败、终端不可恢复失败与提示状态残留；不重启、不关闭现有会话、不提交远端
  命令。必要时补仅含故障类型/状态的最小诊断，不记录终端内容或凭据；真机换包须先
  保存现场并确认会话中断时机。不加重试或兼容分支，不启动完整矩阵或模型请求。
  到时仍无法定位则报告证据与缺口，重新决定下一步。已有差分通过回执保留为历史
  事实，不能覆盖此新异常；发布保持暂停。
  只读快照确认进程仍为 50155、应用 UID 20020172 在 TCP/TCP6 表中无连接；单次
  Tab 往返后首 Tab 仍异常，第二个 Tab 正常产生显示恢复事件，已返回原 Tab。
  现有环形日志最早约 17:20，不能确认覆盖触发瞬间；controller 的失败入口未记录原因，
  因而不能倒推出触发者。
  选定最小诊断：controller 首次失败记录固定原因与队列/显示/焦点状态，显示不可用
  单独分类；原生 IME 只记录失败返回数值。不打印异常原文、事件文本或终端数据，
  不增加重试、恢复或兼容分支。先做隐私/错误分类合同与 ARM64 构建，暂不替换现场。
  软件阶段 41 项 controller 合同及生产 IME 光标错误码/资源清理检查通过。首次 debug
  构建遇到验收注入的 Logger 导入冲突，改为独立别名并置于注入声明前后通过；未改
  注入工具。诊断 HAP SHA-256 为
  `5ba754b8ee2251f9e3c6aa838a119f265d7fabbcde57e8d8317d8f586fc680c7`，签名与安装准入
  检查通过。维护者确认现场已保存后允许换包及最多 10 分钟观察；诊断包已安装，
  新进程 PID 5484。6 分钟观察完成三个空闲 Tab 往返、最小化 12 秒再恢复、输入一个
  ASCII 字符并退格清除；进程保持，未观察到暂停提示或新增故障日志。已停止本轮
  日志采集并保存最终截图；未提交远端命令、调用模型或修改系统设置。
  此轮仅覆盖本地空闲会话，未重现原 SSH 数据场景，不能证明根因消失或恢复发布。
  包、构建日志与 `device-observation/result.json` 同在上述证据目录。
  30 分钟诊断检查点：原现场证实局部终端持续不可用，应用进程仍在；触发原因未知，
  本轮仅补观测能力，不宣称修复。下一步优先在有界 SSH 输出及焦点切换场景捕获固定
  故障分类，区分消费超时、原生输入拒绝与 IME 生命周期异常；不重复空闲动作凑覆盖，
  不扩展完整矩阵或增加恢复分支。维护者 09-26 要求本诊断继续暂停；新候选准备
  独立推进，但此故障不能因此视为已解决或已通过发布验收。

## 09-25 体验评审后的选定改动

维护者批准以下范围并要求逐项执行。依据是可靠、简洁与高频操作价值，不要求恢复
xterm 的全部内部行为。基线 `d37d729`，开发分支 `codex/terminal-experience-parity`。
本批改变产品树，原 `06160c0` 候选及 C4 归档不可直接用于发布新代码；保留其全部
产物和证据。逐项做受影响软件检查，同一开发包串行验证真机，再按发布流程重新冻结，
不为每项重复完整矩阵，不调用模型、不发布 tag/Release 或提交 AppGallery。

- [x] 字号一致性：保留 8–48 的用户字号范围，让高系统缩放下的实际字号继续增长。
  预期是设置、绘制、预编辑和吸附度量一致；最后正确边界为 vp 字号，首个错误边界
  是 controller 将物理字号截至 96 px，renderer/N-API 也使用该上限。原生显示拥有
  度量。用 density 2.2125 下 44–48 的反例区分正常舍入与静默截断；保持有界资源。
- [x] 普通 URL：支持明确 HTTP(S)、协议大小写、常见括号和 `URL=` 前缀后的地址，
  继续使用既有系统打开/校验与 OSC 8。最后正确边界为 VT 文本，首个错误边界为
  `linkAt` 按空白切词后仅检查小写前缀。worker 拥有识别；真实 VT 样本核对范围，
  模糊文本不猜测，不增加通用解析框架、路径识别或依赖。
- [x] 复制/粘贴：本轮明确保留单次 1 MiB UTF-8 上限。超限清楚提示、保留选区、
  整次拒绝，不截断、不因拒绝断开会话。此决定独立于搜索上限。复制已有超限事件
  但仅反馈普通失败；粘贴容量还受原生共享队列占用影响，核对拒绝是否进入 fatal。
  controller 拥有用户反馈，worker 拥有准入；验证边界长度、超限、忙碌和后续输入。
- [x] 触摸板滚动：慢划精细、快划温和加速并封顶；换向、停顿及手势/所有权结束时
  清理旧余量。鼠标滚轮保持固定步长。最后正确边界为 AxisEvent，首个缺口是 Pane
  只做 `/40` 累加；由 Pane 输入策略单一拥有手势状态。使用系统 sourceTool/
  timestamp/action，先核对单位与真机事件；不加惯性、定时补滚、设置或按 TUI 特判。
- [x] 明确延期：拖出边缘选择的距离加速。本轮保留固定速度；只有实际长历史选择
  经常受阻时才重新评估，不因 xterm 曾提供就恢复。
- [x] 文档与定向验证：指南、Changelog、迁移审计已更新，软件/ARM64/本批真机证据
  已具备；PR #253 承载代码、调参与记录。新倍率的证据边界见下方。
- [x] 重新冻结正式候选：PR #254 已合入 `5a4f8d6`，新 C0 已固定。
  维护者已批准
  [本轮差分验收方案](design/1.7-differential-acceptance-20260925.md)，不启动 R4 全矩阵。
  新 C0/C1/C2、QH、C4 与历史 C3 的证据身份分别记录，开发包和旧 C4 不冒充新候选通过。
  新增检查目标 10–15 分钟、零模型、零自动重试，构建/签名耗时单列；发布仍须授权。
- [x] 闭合新候选 C4：许可证与 SDK 上下文修复已通过，同源签名、归档和短 smoke 完成。
  以下保留前两次失败及恢复边界，最终结果见下方“C4 完成”。
  维护者随后要求继续，授权有界构建工具修复及 C4 续接；原失败记录保持不变。
  预期是生产构建后七份原生通知可用；最后正确边界为 native 构建生成通知，首个错误
  边界为 Hvigor 根项目 clean 后 `build/native-terminal` 消失，`build-all.ps1:569` 读取失败。
  构建入口拥有顺序；下一次最小验证区分“Hvigor 初始化清理删除旁路输出”与通知生成失败，
  将必要初始化放在生成之前或保留通知于清理范围外，优先选择更少改动。
  仅修改构建工具，验证一次真正首次 clean、七份通知、锁文件字节、完整 manifest 与同源签名；
  目标半小时内闭合，第一次新失败即重新报告，不复制旧许可证冒充新产物、不扩展矩阵。
  按工具兼容证据复用现有 C1/C2/QH，产品及依赖不变时不重跑完整物理矩阵或模型请求。
  本轮采用：七份通知每次重生成于既有 `.cache/native-terminal/licenses`，由 LeanTTY
  `-Clean` 明确清退，避免 Hvigor 首次清理删除；不增加初始化构建或恢复分支。
  同时复用现有 OHPM 文本比较，恢复安装引起的纯换行变化，拒绝真正依赖漂移。
  C4 的新 Git 身份仅含工具/记录差异，逐路径证明产品、锁文件和原生依赖输入与
  `5a4f8d6` 一致，原 C2 HAP/QH 身份保持不变，不移植旧完整矩阵 Pass。

09-25 C4 工具续轮：PR [#256](https://github.com/wandcs/leantty/pull/256) 已合入
`6a5eb822b45d0b8f5a032ee750d3354fd897a0a2`，tree
`e5dfaf16a3bdfed02342592c7ac22089aff0a573`。定向构建工具检查与 policy 通过；
独立 `p9/r9` 固定该提交，产品、锁文件及原生依赖输入与 `5a4f8d6` 的对象逐项一致。
一次真正首次 clean 生产构建保留七份新生成通知，两份锁文件字节不变，完整 manifest
生成；APP/HAP 的 SDK 签名与摘要验证通过，两个检出保持干净。生产 APP SHA-256
`07ed4a0c8ae69f067252601f27fe57291cf262a9245422d900b0a6647a32c4c7`，HAP SHA-256
`f5cc003b9f1af341352112374b1a77e98d8b8b6ba733094a7e984de9caf51a7c`。

随后 SDK 凭据模块加载失败，按“第一次新失败即报告”停止；本次构建至失败共 196.2 秒。
无凭据的模块加载对照证明：SDK 按当前目录选用项目 Hvigor 缓存，生产 `p9` 缓存存在且
加载成功，全新评审 `r9` 缓存不存在，缺少 `@ohos/hvigor`。尚未进入凭据解密或签名，
不是已证实的证书或密码错误。证据在 `freeze-1.7.0-20260925/c4-r2/` 的
`source-equivalence.json`、`c4-timing.json`、`module-diagnosis.json` 与 `result.json`；
原失败回执保持不变。未生成 review HAP、未完成归档或 smoke，不能发布；零模型请求。

下一步仅闭合签名工具的 SDK 模块初始化环境，复用已成功构建的生产 unsigned HAP 与
manifest，通过既有 `-SkipProductionBuild` 续接同 payload 签名、包体/归档核对及一次
短 smoke。先明确唯一有效的 SDK 工具上下文，不以额外编译整个评审项目补齐缓存。
本续轮未实施该修复；不重跑未受影响的 C1/C2/QH、产品编译或完整物理矩阵。

维护者再次要求继续后，本批准入签名环境修复：由已验证 manifest 的生产检出提供 SDK
项目上下文，评审检出仅提供测试签名配置和输出；继续使用同一个 SDK 初始化/解密入口，
不加缓存搜索、回退或额外编译。最后正确边界是生产 manifest 核验，首个错误边界是
bridge 切到没有 Hvigor 缓存的评审目录；签名调用者拥有两类路径的区分。用冷评审目录
回归验证 SDK 初始化仅使用生产上下文，再执行一次真实同源签名、归档和短 smoke。
修复/定向检查预计 15 分钟，签名与冒烟另计；第一次新失败仍清理后报告。按 R1/R2
恢复 C4，复用现有 C1/C2/QH，产品源码及生产包不变，零模型，不打 tag 或发布。

**09-25 C4 完成：** PR [#258](https://github.com/wandcs/leantty/pull/258) 四项云端检查
通过并合入 `d2b43a94cb821b7edc3bfb9c1fbd3a178a001219`。独立工具/材料检出 `m10`
固定该提交，生产 `p9` / 评审 `r9` 保持 `6a5eb82` 且干净；产品输入与原 C0 一致。
复用原 unsigned HAP，以生产检出初始化 SDK 模块，测试配置仍来自未编译的评审目录。
真实签名及三份包验签通过，生产与评审 signed HAP 全部 18 个成员、三份 ARM64 原生库
一致，APP 内嵌模块、Profile 角色/权限、版本/ABI 和验收标记隔离均通过。

新归档为发布根目录 `candidate-1.7.0-20260925-r3/releases/1.7.0/`，19 个附件摘要
全部复核通过。生产 APP/HAP 与上述保留产物完全相同；review HAP SHA-256 为
`25c4de55dbaf70dcfe97fe5a06971c10e330f319d27bfc6c4230c3a89e78cbd9`。
同一 `m10` 运行一次 `verify-review-smoke-pc.ps1`，安装、新 Tab、键盘分屏、关闭 Pane
及清理通过，临时 Tab 已移除，原工作区/Settings 与休眠策略恢复。设备当前安装此
review HAP；smoke 的 `acceptanceEligible=false` 保持原意，不冒充完整 C3。

本次签名/比较/归档 5.7 秒，短 smoke 23.7 秒；C1 + QH + smoke 共 476.8 秒（约 7 分
57 秒），不含构建、工具修复、PR 等时间。未重编译生产、重跑完整矩阵或调用模型。
最终回执 `freeze-1.7.0-20260925/c4-r3/differential-acceptance.json` 为本次批准范围
的通过证据；两份旧失败回执与历史完整矩阵报告原样保留。首次锁屏退出未归因等风险
仍按差分方案保留，不声称新包完整矩阵通过。

09-25 原待办“以 `6a5eb82` 归档发布”因后续产品源码变化失效；新候选和发布门禁见
顶部“09-26 1.7 候选重新定基”。当前没有创建 tag、发布 Release 或提交商店。

09-25 差分结果：新 C0 `5a4f8d67d244d31e8300488cd412d4a83d74fe32`，tree
`79e6f6370048225252d3450414e21b04bf9fde1b`，1.7.0 / 1007000。
一次 C1 全部 33 项通过（311.9 秒）；C2 干净 ARM64 构建并部署，HAP SHA-256
`f18f9e73c47502a23a402ed21cef7cf6316685c41e052ed2a564da4729f6c042`。
一次 QH 为 formal / passed / releaseEligible=true（141.1 秒），3 次普通输入均一次匹配，
认证、临时信任、映射、夹具及偏好清理通过；输入法前后均为英文，零模式修改，焦点已恢复。
生产编译生成了签名 APP/HAP，但元数据在七份许可证读取处失败；同源 review 签名、完整
归档及最终 smoke 未执行，不能发布。外部证据在发布根目录 `freeze-1.7.0-20260925/`。
`p8/r8` 均保持新 C0 且干净；生产构建造成的两份锁文件纯换行变化保留副本后恢复。
旧矩阵报告哈希不变，原 `releases/1.7.0` 原样保留；没有新完整矩阵 Pass、自动重试或模型请求。

09-25 实施进度：四项代码及中英文指南已更新，维护者已确认三种新增 URL 的实体点击通过。
`policy,terminal-native` 定向检查通过；补充的真实 VT URL 正反例、括号路径保留及
满队列整次拒绝粘贴通过，controller 39 项合同、指南 100 KiB 边界检查通过。
首次 ARM64 构建发现验收注入后的导入顺序错误，调整导入位置后构建通过；不修改注入工具。
测试签名 HAP SHA-256 为 `369918399cc1111be0a6ef939c9c7adb0212d18f9b5e9b20146f0fc1c1a13263`，
维护者同意切换后已安装启动。构建日志 `build/verification/experience-build-r2/`，
定向软件记录 `build/verification/experience-focused.json`；后者早于补充 URL 反例，
不可将它单独作为最终源码的全量证明。后续实体反馈及调参记录见下；新增模型请求为 0。

同一 HAP 的 `verify-ssh-auth-pc.ps1 -DiagnosticHap -Only transport-main-path` 通过，
1 MiB 粘贴与受控服务器逐字节匹配，连续输出、分屏 resize、断开重连及清理通过；
证据 `build/verification/experience-transport/device-ssh-auth.json`。超限/队列忙碌拒绝
由 controller 与真实 VT 软件反例覆盖，不把主路径成功描述为实际触发了这些拒绝。
字号诊断在 density 2.2125 下逐项截图 44–48；同一青色单词的像素宽度为
454/462/476/484/492，实际绘制持续增长。`build/verification/experience-font/` 保留
截图、菜单值及像素量测；已恢复字号 16、density 1.9、休眠策略和空命令输入。
诊断早期的 Windows HDC 路径格式与合并键事件问题只影响临时脚本，未修改产品或公共工具。
低速调参完成并验证后按 CI 与评审流程合入 PR，再单独冻结新候选。
PR [#253](https://github.com/wandcs/leantty/pull/253) 已创建，产品提交 `19a62fc`。
维护者首轮反馈：精细双指移动稍快，其余滚动行为暂未发现问题；随后确认三条 URL
均能正常打开，并批准最低倍率降到 0.75、最高保持 4。以 40 vp/行为基准，最低速
累计 160 vp 滚动 3 行；起加速速度仍为 250 vp/s，鼠标固定步长保持不变。
这次只有纯策略数值调整：复用本批真实设备事件/手感、URL、字号和 SSH 证据，
补跑精确低速距离、快速上限、换向/停顿/所有权测试及 ARM64 构建，并安装同轮调参包。
依 §4.9 不重跑未受影响的 SSH/字号矩阵，不把新倍率的主观手感声称为再次人工验收。
脚本 `~/ltty-links` 保留供维护者复查；正式候选仍需重新冻结和适用门禁。
最终调参已通过 39 项 controller 合同、ARM64 构建，并安装启动；HAP SHA-256 为
`986bab4a7c7d7cbf1eb976e86203cb7a47b425907a08caabf1a4d9faf37def12`，
构建日志在 `build/verification/experience-scroll-075-build/`。本批没有增加设置、
兼容分支或模型调用；新倍率未要求重复完整人工矩阵，后续正常体验若仍偏快再定向调整。

## 09-24 验收前基线、依赖和产品范围收口

### 新候选正式验收启动

**09-25 C4 完成：** PR #251 已合入 `2f4e9cd14566262298eda296e9eb01ab45d1ad55`，
四项云端检查通过。独立材料/工具检出 `m7` 冻结到该提交；产品 `p7` / `r7` 均保持
`06160c056d8d33067b6dc25cbfc3ecc84b08109a` / `59b0d718ad344b1fa3fe4fe6e2d17f6ab66fed01`、
1.7.0 / 1007000，三个检出均干净。未重新编译产品，复用原生产 unsigned HAP，
通过已批准的 SDK 模块初始化、凭据处理和标准签名入口派生测试签名包。

归档在发布根目录 `releases/1.7.0/`，19 个文件的 bundle SHA-256 全部复核通过。
production APP `990228ca…d0ce3a5`、HAP `73ce94c4…e14efb` 与原成功构建完全相同；
最终 review HAP 为 `2b470715687e7adb95f92cc3e004fbcc10764d255370ac7b6498df39280509d6`。
`evidence/review-signing.json`、`release-identity.json` 保留完整成员清单、三份实际包内
原生库摘要、APP 内嵌模块核对、SDK/工具摘要及两类签名/权限边界。两个 signed HAP
全部 18 个成员一致；SDK 位图与 APP `pack.info` 排版边界按已合入规则严格验证。

同一冻结工具运行 `verify-review-smoke-pc.ps1`，实际安装上述归档 review HAP，
package admission、预检、安装启动、新 Tab、键盘分屏、关闭 Pane、清理七阶段均通过。
临时 Tab 已移除，原工作区/Settings 保留，主机信任 SHA-256 与原基线相同，fport 为空。
证据为 `formal-verification/1.7.0-06160c0-r3-20260924/c4-review-smoke/review-smoke.json`，
`result=passed`、`cleanup=passed`；其 `acceptanceEligible=false` 保持原意，仅证明 C4
普通产品路径，不替代正式 C3。原完整 R3 报告、保留候选摘要与 `h9` 被动实体锁屏
正式通过证据重新核实，文件未修改；首次锁屏退出仍未归因，不因后续通过宣称已修复。

- [x] 1.7 候选 C1–C3 适用证据、C4 生产/评审身份、标记隔离、最终真机冒烟及材料归档闭合。
- 原 C0/归档只保留历史身份，不发布为修改后的新候选。新体验批现已完成差分验收，
  唯一发布待办在本文开头；当时没有创建 tag、发布 Release 或提交商店，新增模型调用
  为 0，未重跑完整软件/物理矩阵。

**09-25 单一产物签名准入：** 维护者在讨论后批准按建议执行。目标是证明测试与发布
使用同一程序；本轮不解决独立构建可复现性，不再移动目录或重复编译，产品保持 C0。
复用已通过的生产 unsigned HAP，通过现有测试 Profile 生成评审 HAP；核对完整 ZIP
条目内容、生产 APP 实际嵌入的 HAP、版本/ABI、签名用途、Profile 权限和验收标记隔离，
再运行既有 release-mode review smoke。复用仍有效的 C1–C3，零模型，不打标签或发布。

工具批预算 30–45 分钟：标准 SDK `sign-app` 接收现有未签名 HAP；已有凭据只有
DevEco 加密格式，采用安装包中同一 Hvigor 凭据函数在内存中解密并调用 SDK，不复制
算法、不记录密码、不新增明文密码文件或多版本适配。SDK 内部依赖明确记录，
加载/签名失败直接停止，SDK 升级需定向验证。相比要求维护者重复录入或另存密码，
本批复用现有凭据所有者，信任边界不扩展。若需要进一步兼容或私有实现则重新评估。
新增验证限实际产物比较反例、签名/归档工具和一次最终真机冒烟，不扩大正式矩阵。

**09-25 工具集成阶段记录（后续完成见上文）：** PR #251
四项云端检查通过。维护者明确批准新增 SDK 模块初始化后，真实签名与验签成功；
冻结 C0 的生产/评审 signed HAP 全部 18 个成员及三份原生库一致，APP 模块核对、
release/debug Profile 角色、权限边界、版本/ABI 和验收标记隔离通过。不宣称 C4 完成。
真实产物已确认 SDK 签名增加 `.pages.info`；APP 包含未签名模块，
仅将 `pack.info` JSON 改为带缩进格式。核对保留全部程序/资源/库字节门槛，双签名
HAP 连位图也必须相同，APP 的 `pack.info` 比较完整 JSON 值并分别记录字节摘要。
相关正反例已通过；原输入、独立构建失败和签名入口失败都保留，未安装新包。

直接加载凭据模块原失败于 `@ohos/hvigor` 未初始化；复用 SDK 自身 `initNodePath()`，
不复制模块解析实现。自动审批曾因新增 SDK 内部依赖拒绝执行，维护者本轮明确
“批准，继续”后执行通过。SDK 两处入口的摘要均写入签名记录，升级需定向复验。
本地资格记录为 `r7/build/review-signing/fcf93323ce7c45179467c86565dce2e5/review-signing.json`，
测试签名 HAP `0357806a…5f38f13`；它是工具集成证据，不替代冻结工具归档后的最终包。
下一步合入 PR #251、冻结工具，从原生产输出归档并执行已有最终冒烟；零模型，
本轮仍不创建标签、不发布 GitHub Release、不提交 AppGallery。
离线 `policy,tooling` 已通过，记录
`build/verification/software-focused-20260925T043848476Z.json`；随后新增的生产 Profile
一致性检查通过语法与定向 payload 回归。所有生产原件和 C1–C3 证据保持不变。

**09-25 独立重编译路径结果：未通过，未安装新包。** PR #249 已合入 `0ace347`，商店文案
与 C4 授权记录完成。生产及测试 Profile 的 release 构建、签名、版本与验收标记隔离
分别通过，均为冻结 `06160c0` / `59b0d71`、1.7.0 / 1007000；但实际 HAP 中
`libleantty_terminal.so` 摘要不一致，不能用只比较 Rust 库的旧 manifest 字段放行。
生产终端库 `b908ec05…4ee5c8` / 1,435,184 字节，评审为
`1e457baf…d6acc8` / 1,435,200 字节；同路径清除 Zig 缓存重生成后仍为后者。
`.text` 差 4 字节，尚未证明完整差异原因，不称为纯调试信息或产品缺陷。

已按停止条件结束独立重编译路径；两份检出恢复到 `p7` / `r7`，都保持 C0、干净，
评审 OHPM 链接重新准备并恢复锁文件字节。原 C3 完整报告、新被动锁屏正式资格与
保留候选哈希已重新核实，证据未改动。未进行真机安装、冒烟、归档、标签或发布，
新增模型请求为 0。结果在原 R3 证据目录的
`c4-path-comparison/c4-native-comparison.json`，全部失败日志和构建清单均保留。

**已获批准的流程调整（后续准入见上文）：** 用已验证生产构建的同一份 unsigned HAP 生成测试
Profile 签名包，比较完整包内容（含三份原生库）逐字节一致，再执行现有普通产品路径
真机冒烟。签名用途、版本、ABI、源码身份、验收标记隔离和所有 C4 一致性门槛保留。
预计归档工具/来源记录及定向验证 30–45 分钟，零模型、不改 C0，也不重跑 C1–C3。
这将把 release-process 的独立评审重编译步骤改为同一产物双签名，维护者已批准。
不继续追查任意目录构建可复现性，不启动第三种编译绕行，也不以复制库或手改
manifest 伪造一致。SDK 签名产生的元数据边界以本轮实际核对为准，见上文。

**09-25 C4 授权与执行范围：** 被动观察的实体锁屏单项通过后，维护者明确回复
“确认，按照你的建议继续”，批准解除 C4 暂停。复用 `06160c0` 产品候选及原完整
矩阵、`h9` 新增正式锁屏证据；首次进程退出仍保留为未归因历史观察，不宣称修复。
本批从同一冻结产品提交构建生产 APP/HAP 和独立测试 Profile 的 release-mode HAP，
核对提交、树、版本、原生库、签名、哈希及验收标记隔离，执行普通产品路径真机冒烟，
整理发布材料。商店文案与执行记录单独通过 PR，不改变冻结的产品资源或版本。
本轮不创建标签、不发布 GitHub Release、不提交 AppGallery，也不重跑模型或完整矩阵。

**C4 构建边界诊断（本批 20 分钟）：** 首次生产 clean 构建完成编译、包隔离及签名，
随后元数据阶段因 `build/native-terminal/licenses` 不存在停止，不能记录 C4 通过。
原生构建脚本在成功返回前生成七份通知；Hvigor 日志记录其后执行根项目 `clean`，
实际整个 `build/native-terminal` 已消失。预期通知持续存在至归档；最后正确边界为
原生构建返回，首个错误边界为打包后通知目录缺失，目录清理所有者为 Hvigor。
先保留首次日志，检验一个假设：首次 Hvigor 初始化的清理删除了旁路生成物；用原脚本
在同一 clean 编译所得工作区执行不带 `-Clean` 的 release/metadata 恢复，核对缓存
身份、通知及所有最终产物。此操作不修改源码、不复用其他候选；若仍失败，停止重复，
按证据重新决定归档工具修复。生产/评审均未放行，真机安装尚未开始。

诊断结果：第一次恢复在源码预检即停止，两份 OHPM 锁文件仅被重写换行（Git 内容
diff 为空）；保留生成副本并恢复 HEAD 字节后，原入口不带 `-Clean` 恢复成功，
七份通知、签名和 metadata 全部通过，产品提交/树及 Rust 原生哈希不变。
原失败与预检停止均保留，不改写为通过。评审侧仅清除已核实的 `entry/build`、
`build`、`leantty_ssh/target` 输出，再用 `-ForceNative -BuildMode release -Metadata`
重建，保留已初始化的 `.hvigor`；最终对照两包实际内容和全部原生库。
此次采用人工有界恢复，不改冻结构建脚本，不为该问题重做 C1–C3；持久工具修复单列
到效率记录，当前 C4 仍以完整归档和最终冒烟结果为准。

**C4 原生身份边界（本批 15 分钟）：** 评审侧重建和元数据通过，未重现通知丢失；
但生产 Rust 原生库 `6efba88a…62ab3a` 与评审 `974b339c…5bdc8a` 不同，不能归档通过。
两者同一提交/树/工具链；二进制中分别存在 `p7`、`r7` 下本地 `ssh-key` 补丁源码的
绝对路径。最后正确边界为相同源输入，首个错误边界为独立目录生成的原生字节，
编译器/Cargo 拥有此输出。先保留差异产物和摘要，检验目录身份假设：串行临时移动两份
独立检出，使评审在生产使用过的同一绝对路径重建，再恢复原目录；不复制原生库、
改写 manifest、改变编译参数或产品源码。所有移动限定发布工作目录内，只有一个构建
所有者。若哈希仍不同，停止该路径并重新评估，不能放宽现有 C4 同源同库要求。

同路径重编译已得到与生产完全相同的 Rust 哈希，目录假设成立。搬移前以 Hvigor
官方项目级停止命令解除空闲守护进程占用；未停止其他项目。首次打包在 ArkTS 类型
解析处停止，现场 OHPM 类型包 junction 仍指向旧 `r7` 路径，目标已不存在。
下一步复用现有正式输入准备入口重新生成此检出的 OHPM 链接并恢复锁文件字节，
清除本次旧路径的打包输出后继续；不改 ArkTS 类型，不复制依赖或放宽编译检查。

重审：同路径恢复后的 SSH 库已逐字节一致，但包内终端库仍不同；ELF 的 `.text`
相差 4 字节，不能只归为调试信息。两个检出的 C++ 文件顺序一致，Ghostty 静态库
摘要不同，评审仍保留搬移前 `r7` 下的 Zig 生成缓存。交付目标是验证相同发布字节，
不是追求任意目录可复现构建；本轮保留源码与编译参数，只清除评审自有
`build/native-terminal` 后在统一路径重生成一次，以区分搬移缓存与真正不确定输出。
不修改验证器或复制生产库；若干净重生成仍不同，则结束此路径，重新讨论/选择构建
产物复用方案，不能继续无证据调试编译器或重复完整产品验收。

**09-25 本轮结论：被动观察的实体锁屏 R1 通过。** 同一候选
`06160c0` / `b00eaf44…17ae0f`、干净冻结工具 `h9` / `9ce9d40` 的正式资格
检查通过。维护者按实时提示实体 Win+L/解锁；新单项报告为 `passed`、
`acceptanceEligible=true`，进程 `65050:107857174`、同一 Mosh 会话和远端终端
均保留，恢复命令成功，关闭与清理通过。定向应用日志没有 `onNewWant` 或销毁事件；
该证据证明移除启动副作用后的锁屏恢复，本轮未重跑自动矩阵、合盖或模型请求。

两个日志采集进程均停止，原信任摘要不变，反向映射为空，下载权限仍未授予，
单 Pane 已恢复焦点。新 `lock/device-mosh.json` 通过 `previousAttemptId` 关联
原锁屏 attempt-2，未修改或导入旧聚合报告。首次退出仍未归因，不宣称本次通过
证明其根因或永久修复；当时依维护者选择保持 C4 暂停。后续 C4 授权见上文，
不自动追加第四轮对照或整套矩阵，标签、Release 和商店发布仍未获本轮授权。

**09-25 被动观察单项重验：** PR #247 已合入 `e1e3eac`；四项 CI、桌面身份的
policy/tooling 和实际函数反例通过。第一次工具组因沙箱身份无法启动 WSL 失败，
按既有身份规则重跑通过，不改测试或忽略失败。

直接从主线冻结的 `h8` 虽通过工具资格检查，但 Mosh 在任何场景启动前拒绝复用
候选，原因是主线新增的范围冻结文档不在原固定路径名单中；没有进行锁屏或创建
夹具。临时启动器第一次未收集启动前异常，第二次捕获同一校验错误；两次均未进入
验收阶段，日志观察进程均停止。保留此启动记录，不放宽候选校验名单。

采用较小修复：从原 `h7` 的 `3bc42e1` 组装 `h9` / `9ce9d40`，仅加入 #247
已审查的观察器、对应测试和质量文档，三文件 blob 与主线逐一相同，其他工具仍为
原冻结版本。与候选仅差六个既有允许路径，无产品输入变化；祖先与路径预检通过。
重新绑定资格记录到此精确身份；新证据目录为
`LeanTTY-release/formal-verification/1.7.0-06160c0-lock-passive-h9-20260925/`，
`harness-provenance.json` 保留组装来源。原聚合报告及 `h7` 不改写。

**09-25 观察器修理准入：** 首次退出前的 `START_ABILITY`、退后台和终止事件
共享跟踪 ID，但缺少中间调用者记录，仍不能证明退出原因。成功后缀的定向日志亦
记录 95 次 `onNewWant`，证实锁屏观察器持续改变被测生命周期。系统现有只读
`hidumper -s 3704 -a -all` 能提供唯一 `screenLocked` 状态；上游锁屏服务实现
及本机输出均已核对，不需要新的依赖或产品分支。

本批只替换 Mosh 两个人工场景的锁屏观察函数，拒绝缺失/歧义状态，保留断联处理、
超时、进程/会话与清理判据。预算 30–45 分钟，验证函数反例及一次零模型真机
锁屏/解锁状态转换；不改冻结工具、产品或原报告，不把注入操作充作人工验收。
新工具只在下一轮启动边界采用；历史退出不因移除观察副作用自动判为修复。
若系统状态不能可靠区分，则停止，不加后备启动或继续变换锁屏条件。

结果：实际观察函数的反例先证实旧实现会启动 App，替换后设备工具测试通过；覆盖
锁定/解锁、缺失/非法/重复/多用户状态及设备断联。真机读数为
`false → true → true → true → false`，PID/starttime 始终为
`39522:107615569`，解锁和单 Pane 焦点清理通过，模型请求为 0；这仅证明新观察器
的状态转换，不升级成正式人工验收。证据为本轮 `preparation/passive-lock-owner/`。
冻结 `h7` 和原报告保持不动。

退出来源的只读复核已到证据边界：同一跟踪 ID 仅剩两条 START、退后台、终止事件；
上游 [`LogTerminateAbilityEvent`](https://github.com/openharmony/ability_ability_runtime/blob/8057adac0f3a08a34627fe2ee68a169bd4bb29e2/services/common/src/event_report.cpp#L207-L214)
只写应用与 Ability 名称，不含调用者或退出原因，
所以不能从事件名补出责任方。公开源码用于解释字段，不能等同于本机闭源系统实现。
已修观察副作用；原锁屏退出风险仍需维护者取舍。最初建议保留原失败为未解决观察项并
继续 C4，原因是同候选实体锁屏/合盖重验与三个对照均未再退出，继续重复不能定位
已丢失的历史调用链。代价是仍可能发生一次无法解释的会话中断；这不是小工具瑕疵，
不自行解除阻断。另一选择是保持发布暂停，在被动观察下做一次新的人工定向验收；
即使通过也只能增加置信度，不能声称已证明原退出原因。此处是待维护者决定的建议，
不是已接受风险或已授权重验。维护者随后明确选择保持暂停，并再做一次被动观察的
实体锁屏验收；不接受此时进入 C4。执行范围为新工具干净提交的资格检查及单个
`operator-lock-recovery -Formal` 阶段（R1），同一保留 HAP，零模型，不重跑合盖、
自动矩阵或修改原聚合报告；新记录独立保存。操作时按实时提示实体 Win+L/解锁，
通过后再评估剩余风险，不预先把它解释为已授权发布。

**09-25 最新结果：人工后缀完成，发布尚未放行。** 维护者按实时提示完成实体
Win+L/解锁和合盖/打开/解锁。冻结候选 `06160c0` / `b00eaf44…17ae0f`、
工具 `h7` / `3bc42e1` 均未改变；原入口 R2 报告为 `passed`，
`completeApplicablePhysicalMatrixClaimed=true`，26 个阶段通过、1 个候选检查点
复用。锁屏 attempt-2 保留 `34210:107588144`，合盖 attempt-1 保留
`39522:107615569`；两者同一 Mosh 会话和远端终端均保留，恢复命令及清理通过。
本轮没有重复自动矩阵或新增模型请求，没有修改产品或冻结工具。

报告通过只证明这次重验的行为，不能消除首次锁屏退出。首次失败原件、三项诊断和
R2 连续日志均保留在同一外部证据目录；首次退出原因仍未知，不能定性为用户操作、
系统回收、外部限制或已修复。根据质量策略的根因/复用门禁，暂不以聚合报告的
通过标志放行 C4。下一步先用已有事件时序和观察器代码收窄退出来源；只有能提出
可区分假设的新证据入口才增加诊断，不重复锁屏撞通过，不自动启动 R3/R4。
若现存证据不足以归因，应说明实际连接中断风险及取舍，再由维护者决定后续范围；
不能自行把锁屏会话丢失归为可延期的小问题。

终态审计：两个旁路日志进程已停止；场景夹具、反向映射、临时状态均清理；
原 `known_hosts` SHA-256 仍为 `718a9c22…eb0f21b`；下载权限仍未授予；
英文输入法保持选中，观察菜单已关闭，单 Pane 焦点已恢复。
证据：`formal-verification/1.7.0-06160c0-r3-20260924/release-report.json`、
`stages/mosh-operator-{lock,lid}-recovery/` 和 `preparation/operator-r2-observer/`。
C4、标签、GitHub Release 和 AppGallery 均未启动。

**09-25 人工续验停止：** 维护者回复“继续”后，同候选/工具执行 operator resume。
启动前 PID/starttime 与自动阶段最后现场一致，信任摘要一致、映射为空、英文输入法
及单 Pane 确认；预检通过，按既有凭据流程解锁准备，零模型。人工 Win+L/解锁均被
观察到（58 秒），但进程从 `19800:107483123` 变为 `22641:107497868`，锁屏阶段
失败，合盖未启动；正式报告不改写。夹具、映射、临时状态清理通过，原信任摘要保持。
维护者明确只操作了 Win+L 与解锁，没有关闭窗口、合盖或任务管理器操作。

预期锁屏后保留同一应用及 Mosh 会话；最后正确边界是受控命令成功，首个错误边界
是进程身份变化，权威所有者为系统进程/Ability 生命周期。故障服务没有本时间点的
应用崩溃/卡死记录，不能据此排除所有退出原因。系统事件记录 10:16:14.741 应用
退后台，10 ms 后 TERMINATE_ABILITY；同时存在窗口关闭事件。原工具用反复
`aa start` 观察锁屏（窗口内有 92 条启动事件），它不是纯读取，需要区分观察副作用
与正常锁屏触发的产品/系统退出。现场环形日志未保留完整原因，不推测为内存回收。

本批有界零模型诊断，预算 20–30 分钟：保留原报告，只用同包和隔离本地证据，
先在空闲单 Pane 对照一次标准 Win+L 锁屏（不轮询启动）与现有启动轮询，连续保留
限定本应用的生命周期日志。注入操作只能是诊断，不能替代人工验收。先区分是否
仅观察器导致退出，再决定是否需要带连接诊断；不改产品、不添加兼容分支、不扩大
正式续验机制。两种假设均无新证据或预算耗尽则重审，不能继续反复锁屏撞通过。

空闲对照结果：不轮询启动和加入 16 次启动轮询的标准按键注入均实际锁屏/解锁，
同一 PID `22834` 保留。前者日志采集参数引号错误，不能提供连续日志证据，但独立
PID 样本/锁屏布局与解锁结果有效；后者修正采集参数后留存生命周期日志。不能据此
宣称正式失败已修复，也不再假设启动轮询是充分原因。诊断重审：原失败另有活动
Mosh、实体按键及更长等待三个差异；下一次只用现有同包 `operator-lock-recovery`
诊断入口加标准锁屏注入和连续定向日志，恢复原连接条件，零模型，不作为人工验收。
若仍不复现，保留未知原因并停止自动变换条件，安排带被动日志的人工 R1，而非改产品。

活动 Mosh 注入诊断通过，34 秒锁屏/解锁保留同进程和远端会话，恢复命令及全部
清理通过；`acceptanceEligible=false`，不能填入正式检查点。三项对照没有定位原
实体按键退出原因，停止自动变换条件。维护者明确同意带连续日志再次实体操作。
采用既有 R2 同包续验入口：只重跑失败锁屏，之后执行未开始的合盖；复用已通过的
自动阶段，零模型。旁路日志仅作诊断补证，不改变冻结脚本或正式判据，退出时关闭
采集进程；若再次失败，用新日志定位，不继续盲重试。

**09-25 人工操作前记录：自动阶段完成，当时 C3 尚未完成。** 同一产品候选 `06160c0` /
`b00eaf44…17ae0f`、干净冻结工具 `h7` / `3bc42e1` 的 R3（含同工具
R2 前提恢复）最终报告为 `awaiting-operator`：24 个执行阶段通过、1 个候选检查点
复用；锁屏、合盖两个阶段均为 pending、attemptCount=0。完整矩阵标志仍为 false。
QH、密钥和 Host、通知、卸载恢复、长任务、8 项 Agent、4 组完整 SSH、6 项自动
Mosh 均完成；Wi-Fi 切换保留同一 Mosh 会话和远端终端，SSH 按预期断开后重新连接。
原网络已恢复，各场景拥有的清理已完成，SSH 四组另有进程/映射不存在审计。

Agent 通知不扩称全通过：OpenCode tmux 未发出原生信号；Pi tmux 属已记录的上游
不转发；Qwen direct/tmux 的受控任务完成但未发出信号。按现有已资格化分类保留
非阻塞限制，相关系统通知均为 not-exercised，其他交互检查通过。模型用例预算
仍为 9、自动重试 0；长任务记录实际 1，Agent/总报告的实际请求数为 unavailable，
不把 planned 数写成已核实的实际总数，也未追加诊断模型用例。

自动入口已正常退出。补充清理审计 `preparation/automatic-final-audit.json` 证明：
原 107 条主机信任按原 SHA-256 恢复，临时快照经最终复核后删除；Downloads 的
系统权限和复选框均恢复关闭；英文输入法由语义菜单及截图确认，未改变模式；
HDC 映射为空，单个空闲 Pane 已恢复焦点。原始阶段报告未修改。
证据根为 `LeanTTY-release/formal-verification/1.7.0-06160c0-r3-20260924/`。
修复/准入/恢复判据记录 PR #244 已合入 `8491660`；运行中的冻结工具未移动。

此时等待维护者确认就绪，再沿同一证据目录用 `h7` 的
`verify-release-pc.ps1 -Phase operator -Resume` 执行实体操作。
后续维护者已确认，执行结果及剩余发布阻断见本节顶部。

维护者在第 4–6 项完成后批准“进行下一步”。本轮继续 `06160c0` / `b00eaf44…17ae0f`
的 C3；当前开发任务独占设备、端口与 WSL。使用干净冻结工具 `LeanTTY-release/r7`
及标准 `verify-release-pc.ps1 -Phase automatic -HapPath`，复用原 C1/C2，不复制旧 C3。
固定最多 9 次模型请求、零自动重试；失败保留报告，按当前检查点与清理证据选择
R1–R4，不盲目追加模型诊断。自动阶段完成才准备人工锁屏/合盖。C4、tag、Release
和商店操作在完整矩阵通过前不启动。

启动核对发现前批没有新 C0 的独立就绪演练记录；在行为验收前补跑现有零模型入口，
使用同提交的独立生产/评审检出和已有 release-mode 标记排除样本，不重建测试候选。
这是补齐遗漏，不把前批记录改写为当时已执行；真实 1.7 生产包检查仍属于 C4。
预算为现有演练约 3–5 分钟、自动矩阵按既有阶段耗时执行；不改冻结工具或产品。
本轮证据在本地 `LeanTTY-release/formal-verification/1.7.0-06160c0-20260924/`。

首轮静态检查发现：Mosh `operator-lid-recovery` 的进程替换分支仍正向搜索已取消的
`Workspace layout was recovered`；同进程真实会话回收的 `Workspace layout was kept`
提示仍在产品中，不能一起删除。首轮未热改运行中的工具；该轮停止后已按下方
准入记录校正两个进程替换分支，采用布局、专用 Recovery 日志与交互结果。

卸载场景会清除 1.7 的私有 `known_hosts`，现有工具未保全历史记录。本任务负责
以只读文件快照补齐此次清理责任，不改运行中工具：在前一 Host 场景完成清理、
下一场景尚处安装准备时保存 107 条既有记录，读前/读后及本地 SHA-256 一致，
不含本轮认证夹具端点。仅存当前桌面用户临时目录，不打印主机身份或提交文件。
矩阵停止并确认夹具清理后恢复原始字节、复核摘要，再删除临时快照；若恢复失败，
保留快照并明确清理未完成。元数据为本轮 `preparation/trust-preservation.json`。
此补充不清理其他历史条目，也不将数据保全称为产品验收通过。

本轮 C3 在 `long-task-notification` 前置清理停止，原报告保留。预期新装后空信任
可继续；最后正确边界是 `ssh-keygen -R` 返回，首次错误是共享观察器要求 `cat`
成功哨兵，而卸载后文件确实不存在、应用私有父目录可访问。权威状态是私有文件，
不是旧 Asset projection。另查到 Windows WSL 启动器已停止但本轮配置对应的 sshd
仍监听 23150；模型实际请求 0、workloads 为空、反向映射为空、通知权限已恢复。
下一最小验证：区分已证实不存在与不可读，及验证精确归属的 Linux sshd 清理。

准入当前工具修理：只修该已证实的文件观察/WSL 夹具清理链，使用反例合同和零模型
定向真机检查，不改产品、不引入通用续跑框架。预算约 30–45 分钟，第二种实质
修法失败或累计 90 分钟重审。原入口不接收补充清理证明，不能手改原报告恢复；
共享观察器变更按 R3 重新执行 C3（前缀约 33 分钟、无模型重耗），复用 C0–C2。
先修理并确认清理，再冻结新工具启动新轮；本轮原始证据不拼接成通过。

修复及定向验证完成：通知回归 49 项通过，其中新增四项先红后绿；非特权真实 shell
九种文件状态通过；policy/tooling 七组通过。原候选的零模型 shell-only 真机场景
通过，Linux sshd/23150/映射/临时文件独立审计无残留。107 条原信任经应用私有
`file send -b` 恢复，摘要与备份一致；传输使用 `/data/storage/...` 应用命名空间，
`/data/app/...` 仅供此处 shell 观察。备份暂留供同任务下一轮使用，最终收尾删除。
证据在 `build/verification/trust-cleanup-20260924/`，不是正式 C3 通过。
工具 [PR #242](https://github.com/wandcs/leantty/pull/242) 只含三个脚本文件；
当时干净 `LeanTTY-release/h7` 的 `7e6c817` 与已测工具树相同，五个现有场景兼容门通过。
原 `r7` 不移动、不修改；R3 从新工具执行，原始失败报告及补充恢复记录分别保留。

新轮尚未启动时进一步确认，受控 `process-recovery` 与人工合盖的进程替换分支
均正向搜索已取消的 warning。此时继续保留旧断言会使后续受控/人工恢复验证必然
误报，已不能只作为未触发风险延期。准入第二个独立工具改动：用新进程的
`Recovery run started ... unexpected=true`、实际单 Pane 布局、旧远端输出消失和
本地命令成功替换文案判据；运行时回收仍检查有效提示。证据明确区分布局恢复与
warning，不改变产品。预算 15–20 分钟，纯观察器反例及零模型受控进程重启验证；
失败按边界诊断，不为凑断言恢复已取消的产品文字。完成后再冻结 R3 工具。
首轮受控重启已进入新进程/单 Pane，通用日志观察超时；该通道的标签集合不含
`UnexpectedExitRecoveryStore`。清理通过、零模型，改用卸载场景已有的精确 PID/
专用标签查询并留存原日志，仅重验这一诊断，不修改共享日志投影或产品。
R2 诊断通过：新进程、布局恢复、旧输出消失、本地命令可用且会话未恢复；报告明确
`workspaceRestored=true`、`workspaceWarningObserved=false`，清理全通过、零模型。
最后仅校正文据的 `primaryOracle` 描述，不重跑设备；原 R2 报告保留旧描述及实际
结构化观测，不改写历史。原始日志、软件反例及 R1/R2 见上述工具修理证据目录。

PR #242 已合入 `a42d831`；恢复观察 [PR #243](https://github.com/wandcs/leantty/pull/243)
已合入 `3864c91`，各自四项云端检查全通过。新冻结工具 `h7` 为 `3bc42e1`，只比
原候选提交多五个已准入脚本文件，与合入后的主线 `tools/` 完全一致；五个场景的
候选兼容门通过。系统输入法菜单明确勾选英文，零模式改动并已恢复终端焦点；
主机信任原摘要仍一致、映射为空。本任务继续 R3 自动阶段，证据目录为
`LeanTTY-release/formal-verification/1.7.0-06160c0-r3-20260924/`。前轮实际模型请求
为零，原 9 次总预算不增加；人工阶段仍须自动阶段全部通过后按实时提示执行。

R3 在默认 ECDSA 的 Downloads 准备阶段停止：页面确为“隐私和安全”，但侧栏和
页标题同名，旧的全树唯一匹配报歧义。此前六个检查阶段通过；该失败阶段在导出/
密钥删除之前停止，清理通过，原权限仍关闭，零模型。真实界面/日志不支持产品故障。
选择 R2：由当前任务在系统界面手工准备同一个场景本来就要启用的 Downloads 权限，
记录原关闭状态并负责正式入口结束后的恢复；权限值由 atm 与复选框双重观察。
冻结工具和候选保持不变，以已有 `-Phase automatic -Resume` 从此阶段继续。工具
将看到已准备的权限，不走歧义导航；此前独立证据不依赖该场景的 Downloads 开关。
此处是明确的场景前提与恢复责任，不跳过产品导出/认证检查、不手改报告。预计
约 3–5 分钟，比增加跨工具报告导入或再次重跑已通过前缀成本低；通用设置导航
定位改进留作非阻塞工具维护，详细事实见 test-release-efficiency。

### 前批第 4–6 项完成记录

维护者已授权继续第 4–6 项。本批由当前开发任务独占构建、签名及测试 PC，
不启动模型请求、完整 C3 行为矩阵、C4、发布或商店操作。产品基线为 PR #239 的
`d1e9f3f`，没有计划继续纳入的产品修改；只接纳已定位的发布阻断。

- [x] 第 4 项：选定依赖、warning 和 Host/Key 修改已分别通过 PR #129、#238、#239
  合入；受影响验证已完成，未按每个改动重跑完整正式矩阵。
- [x] 第 5 项：按已选的显式输入法方案完成本轮观察、场景及原状核对；采用当前主线
  工具，记录前提/清理责任，以新候选完成正式 QH，保留旧冻结工具及失败报告。
- [x] 第 6 项：核对版本、依赖、指南和 changelog，合入范围冻结记录；从精确主线提交
  做一次 C1/C2，留存候选 HAP/源码/工具身份。正式候选不等于 C3 或生产发布通过。

本批预算：离线核对及文档 30 分钟，唯一一次完整软件/干净构建按既有耗时执行，
定向工具资格 15–25 分钟；不新增输入法框架或产品兼容分支。失败先保留报告并定位，
不自动重跑全门禁，沿用两种实质修法/90 分钟重审点。候选与证据放在独立本地发布
目录，避免 clean 构建删除开发证据。具体范围及采用责任见
[1.7 冻结记录](design/1.7-scope-freeze-20260924.md)。

候选准备 R1 在 Windows 主机 VT 合同处停止，尚未构建 ARM64 或安装：
`uucode_generate.exe` 实际存在，Zig 启动却返回 FileNotFound；拼接相对路径为 268
字符、规范路径 194 字符。最后正确边界是生成器文件已产生，首次错误边界是 Zig
子进程启动，生成路径由 Zig build 拥有。下一最小诊断是将本轮新建的独立检出移至
同一发布目录的短路径，保持提交/文件不变，只重验 VT 主机合同，以区分路径条件与
源码故障；不修上游或添加长路径兼容分支。若通过，现有入口没有 C1 续跑，重复约
两分钟已执行检查比新增续跑工具成本低，允许新目录 R2 重做完整 C1/C2；R1 原报告
及证据保留，不能拼接为 Pass。再次失败先按新边界诊断，不反复整套重跑。

完成：PR #240 合入 `06160c0`，七个版本来源为 1.7.0 / versionCode 1007000，指南
源/随包 rev12 摘要相同。短路径独立冻结检出 `LeanTTY-release/r7` 的 C1/C2 R2 通过
33 项完整软件检查、干净 ARM64 签名构建和安装。候选 SHA-256：
`b00eaf447901e66199801cff9d452e3540608463d6085f1a4b5f1de50517ae0f`。
同提交/树 QH 为 formal / passed / releaseEligible=true；3 条普通命令共 3 次输入、
3 次 Enter、零偏差，秘密输入、布局/日志/服务器观察、信任/映射/夹具和偏好清理通过。
系统菜单前后明确勾选英文，本轮零模式改动，结束恢复终端焦点和 awake lease，保留
两个空闲 ltty Tab。证据在 `LeanTTY-release/freeze-1.7.0-20260924/`，完整身份见
[冻结记录](design/1.7-scope-freeze-20260924.md#本批完成结果)。仅完成准备及资格，
不重命名为 C3 通过；模型调用为零，后续记录提交不移动已冻结源码/工具检出。

- [x] 剪贴板二次授权及完成记录已通过 [PR #236](https://github.com/wandcs/leantty/pull/236)
  合入 `35e1607`。09-24 重新 fetch 后本地主线/远端主线一致、ahead/behind 0/0、
  工作区干净；下方真机体验和软件合同记录保留，不重复创建修复 PR 或重装 App。
- [x] 评审三个现有依赖 PR、上游维护/发布/安全公告、产品及独立 fixture 锁文件，
  形成[依赖与范围决定](design/dependency-scope-review-20260924.md)。两份 cargo audit
  均发现 RSA 未修复公告及 chacha20/wnaf 撤回版本；npm audit 为 0，不能将仓库
  Dependabot open=0 扩称依赖安全全绿。现有 PR 的 core CI 不覆盖完整 NAPI 产品。
- [x] 维护者确认 warning 与 Host/Key 两项均纳入 1.7，具体合同见下方对应任务。
  依赖及两项产品修改的实施与定向验证已完成，证据与交付边界见下方。
- [x] 纳入 russh 0.63.3：补齐 PublicKeyOrCertificate 回调适配，保持
  普通主机公钥信任语义，明确拒绝未支持的主机证书；不引入 CA 支持或 fallback。
  同步主 workspace 与独立 SFTP fixture 的必要版本/锁文件，补齐 #216 未覆盖的调用者。
  同批定向更新 chacha20 0.10.2、wnaf 0.14.1；后者有 trait 约束变化，不扩大 vendor
  补丁来迁就升级。保留 ssh-key 0.7.0-rc.11 的现有可移除 ECDSA 补丁及来源校验。
  完整产品编译、受影响 Rust/信任/认证/通道回归、ARM64 构建与定向真机通过后经 PR 合入。
- [x] #129 的 data-encoding 2.11.1 单独维护更新：核对来源/锁文件与 core 合同，
  避免附带无关依赖刷新；不为包元数据变化单独重跑完整真机矩阵。
  core 回归和四项 CI 通过，PR #129 已合入 `9bfa9f9`。
- [x] #217 的 russh-sftp 3.0.0 明确延期至 1.7 后的独立准入评估；当前保持 2.4.0，
  不为清空 PR 合入读写流水线和取消语义变化。Host 证书/CA、列表宽度框架及新选项
  明确不纳入 1.7。RSA 支持保持现有合同；记录未修复公告与实际签名路径，不新增
  blanket ignore、不引入未发布加密库或扩展本地密码学补丁，不宣称时序安全已证明。

顺序：安全依赖与必要适配 → warning/列表实现及受影响验证 → 最终主线新候选。
依赖与产品字节变更不沿用旧签名包身份。完整行为验收、模型调用、生产发布仍未恢复。

09-24 实施启动：定向更新 russh/chacha20/wnaf 后，预期 workspace 全目标编译通过；
core/依赖编译正常，首个失败为 proxy_jump_transport.rs:42 的旧 PublicKey 回调签名。
权威接口为 russh 0.63.3 Handler；此前评审漏计此实现，实际共六处。下一最小检查为
同步该 fixture 回调并重新全目标编译，不修改协议/信任语义，不扩展 vendor 补丁。
补齐后完整 workspace/all-targets 与独立 SFTP fixture 编译通过；policy、rust-core、
rust-native、ssh-fixture 定向组通过，包含 57 项产品原生测试、ECDSA 标量合同、
ProxyJump/背压/认证回归和 fixture E2E。新增合同检查默认不协商主机证书，产品回调
也明确拒绝 Certificate。标准 ARM64 签名构建及真机密码、传输主路径、加密公钥、
ECDSA 加密导入/错误口令/重启认证、Mosh SSH 引导和固定 UDP 连接通过，清理独立核对通过。
OpenSSH SFTP E2E 通过；两份升级后 cargo audit 仅余已记录的 RSA 公告，无撤回版本。
源码 `169d64a`，开发 HAP SHA-256 `b6399e49ee8ef52ef16eb637462772f3cc6579b268976c3e9edf1797ac971c9b`。
PR #238 已合入 `a07627d`，替代并关闭 #216；整合 #129 的元数据更新后，组合 core 合同再次通过。
复用该开发包的受影响设备证据，不将它改称组合源码或正式候选；后续列表开发包重新构建。
证据 `build/verification/ssh-security-dependencies/`，完整发布验收与模型调用未启动。

## 09-23 当前产品修复与设备使用边界

### 本地粘贴按需申请剪贴板权限：已交付，维护者确认解决

09-24 维护者体验已安装的二次授权修复包后，在主工作区任务直接回复“可以了”。
据此记录为维护者真机体验确认本问题解决，不扩称自动化弹框或拒绝矩阵已验证。
交付源码 `c3884c169cb3c5cc207d7ff96d0db3253f6dc1b4`，签名 HAP SHA-256 为
`26b79aa33285854752eb7befb9fc8345ce1747ffd2974dae5043e81da3f62bf3`。
主任务已留存 `build/verification/paste-settings-c3884c1-20260924/` 下的
`operator-feedback.json`、`build-delivery.json` 和 `device-delivery.json`；本记录已核对三者
源码/包身份一致。标准 debug 构建和覆盖安装完成，未卸载，原两个 Tab 保留。
安装检查没有调用授权弹框或读取剪贴板；维护者体验反馈是另一路人工证据。

09-24 状态补齐前（同一工作链）：当时设备只读取证发现 READ_PASTEBOARD 为
`grantStatus=-1 / grantFlag=2 (PERMISSION_USER_FIXED)`，权限定义为
`USER_GRANT / SYSTEM_BASIC / NORMAL`。此前已授权的设备状态已过时，不推测变化来源。
当时普通申请返回拒绝，原实现缺少已有 Downloads 使用的系统设置授权弹框路径。
系统拥有权限/弹框资格，ClipboardManager 拥有申请，controller/inputOwner 拥有粘贴意图。

- [x] 在原 `78ea349` 上，仅当普通结果明确为单项拒绝且单项
  `dialogShownResults[0] === false`，并且原粘贴意图仍有效时，调用一次正式
  `requestPermissionOnSetting`。普通弹框已显示或字段未知时不接续弹框；不跳设置页、
  不缓存权限、不做设备特判、不循环重试。两阶段等待都保留原 owner 校验和 finally 清锁。
- [x] 补 4 个定向软件合同，旧实现先复现未调用设置授权，补齐后 35 项 controller
  合同通过；覆盖设置授权成功/拒绝/异常、字段未知、旧 owner 和重复请求。
  定向 policy/diff 检查通过。产品补齐限于两个现有 ArkTS 文件，未增加状态缓存、
  依赖或平台分支。
- [x] 固定源码已交主工作区负责人完成标准构建和覆盖安装；维护者体验确认问题解决。
  本任务不追加构建/签名/设备工作，复制签名配置方案已停止。

判别证据为主任务只读 `permission-support-research/findings.md`（09-24 追加）；
实际弹框逐步轨迹及拒绝矩阵没有自动化证据，不能把设备定义/状态或软件替身当作系统 UI 通过。
本次改变产品源码，下面旧版 `6d56873` 构建/包证据不覆盖本次补齐；新包身份与反馈见本节开头。
定向红/绿日志保留在忽略目录 `.cache/native-terminal/paste-permission-setting/`。

维护者明确要求用户尝试粘贴且未授权时调用系统权限申请；本授权替代此前取消自动
申请的决定，包括 Ctrl+V、标准 Right 无选区及既有本地粘贴入口。系统决定是否显示
授权框；不模拟授权框，不改 APL/ACL，不模拟 Ctrl+V，不自动跳系统设置页或循环申请。

- [x] ClipboardManager 保持唯一读取封装，复用 controller UIContext/inputOwner；
  权限申请和读取等待后均检查 owner、焦点、呈现、搜索及关闭状态。重复请求不排队，
  拒绝零读取并提供中英文下一步提示；失效请求取消，授权导致失焦时提示再次粘贴。
- [x] 31 项实际 controller 合同通过，平台替身覆盖授权/拒绝/异常、重复、失效、
  复制及 OSC52 不申请；实际 Ctrl+V 函数和 ClipboardManager 均执行。Right 的 NAPI
  替身按 button=2/release 路由；未变的真实 VT K1/D1 合同沿用 #234 证据。
- [x] 定向 policy/diff 与四条中英文资源检查通过；正常 `build-all.ps1 -NoDaemon`
  未签名 ARM64 debug 构建通过，SDK 24 API 集成成功。无本轮临时探针，保留标准
  debug 验收入口，不是正式候选；构建临时源已还原，构建资源已归还。
- [x] 维护者于 09-24 在本任务明确授权将相关修复合并远程主干，随后归档本任务。
  此前复制签名配置因敏感值风险、push/PR 因缺直接授权被拒绝，均未绕过；复制方案已停止，
  签名交付由主工作区标准构建完成。新的直接授权仅用于正常 PR 合并，不恢复正式验收或发布。
- [x] 本次补齐已由主工作区负责人构建签名并覆盖安装，维护者随后确认可用；
  首次系统弹窗的逐步自动化验证和真实拒绝矩阵仍未测，不为补证据重置权限或再操作设备。

本轮独立工作区基线为 `8f12de0`（含 #233/#234），不重做 K1/D1，不改共享主工作区。
已整合纯文档主线 `ec064c6`（#235），保留原现场由维护者手动授权关闭的独立事实。
实现 `4b0ad77`，构建源码 `6d56873`；未签名 HAP SHA-256 为
`331848eec885bd06e3e4965cfedceb66e8beac4e14c7b5a280214dc84505dfed`。
`78ea349` 的测试替身校正与交付记录未改变上述旧包源码；`c3884c1` 二次授权修改使用本节
开头的新包证据。本次反馈记录仅改文档，不改变已交付产品源码。
正式验收、模型调用、标签和发布保持暂停。

### 副键粘贴与鼠标释放：K1/D1 已交付，维护者实测关闭主诉

维护者在 PR #234 交付包上先确认“双指轻点在各处不能粘贴、同一环境 Ctrl+V 可用”，
随后于 09-23 明确：“不用了，我在系统里给这个APP打开了剪贴板权限，就正常了。”
据此按维护者现场干预验证关闭右键/双指粘贴主诉，不要求额外手势或设备证明。

关闭前的只读核对确认：K1 已包含在交付包机器码中；READ_PASTEBOARD 当时为未授予，
源码仅声明权限、自定义右键 getData 没有运行时申请；有效签名 Profile 的 allowed-acls
已包含该权限。实际恢复来自维护者在系统设置中授权，不归功于 K1/D1。PR #234 的
K1/D1 是独立证实的 tracking/释放回归修复，保留其实现和证据，不回退。

原诊断不再实施临时诊断包或权限申请代码，没有对应产品改动、构建或安装。维护者
随后另行要求“用户尝试粘贴时，未获剪贴板权限应申请授权”，由独立任务承接，
不重新打开本次已解决主诉。设备保持维护者授权后的现状，不读取个人剪贴板或收回权限。
正式验收、模型与发布继续暂停，其他开发不因本条关闭自动恢复。

维护者于 09-23 补充“双指轻点在各处无法粘贴，同一时刻 Ctrl+V 可粘贴”，并授权
独立鼠标负责人在 `codex/secondary-pointer-repair` 实施局部修复。软件工作可与 Known
Hosts 并行；设备、共享夹具及签名构建仍须统筹明确交接，不能并行写共享产物/缓存。

- [x] 维护者开启系统剪贴板权限后，当前交付包的右键/双指粘贴实测正常；原主诉关闭。
- [x] K1 软件修复：原生右键在 tracking 下被远端 reporting 消费，恢复既有选区复制/
  无选区粘贴合同，并保持完整 Press/Release 配对，不留下远端孤立 Press。
- [x] D1 软件修复：普通 Left Press 后 Ctrl Release（tracking 下 Ctrl+Shift）跳过
  选择/鼠标收尾，导致后续链接 hover 与 gutter 命中受阻；同一原生所有者按 Press
  选择的交互类型完成释放，替换原 bool，未增加平台适配状态层。
- [x] 实际 pinned VT 的 pointer/hover 合同先复现失败，再通过修复后回归；受影响
  控制器/原生检查通过，独立复核无阻断项。
- [x] 构建并安装含 Known Hosts 修复的正常开发验证包；启动、布局恢复和公开文本
  输入/取消通过，未接触个人剪贴板，不将其标成真实双指或完整副键链验收。

诊断基线 `877a0b7`：预期副键粘贴与 Ctrl+V 使用同一当前 Pane 文本输入；K1 最后
正确边界为 worker 收到右键，首个错误边界为 `mouse()` 抢在副键动作前返回；D1 为
链接分支抢在已有左键释放清理前返回。VT worker 拥有模式/选区，controller 拥有
输入代次，系统拥有剪贴板准入。维护者随后明确“正常修复，我后面会自己使用来确认”，
因此取消等待现场手势、剪贴板标记及临时读前截断探针；不读取、覆盖或粘贴未知内容。
已批准预算为软件 1–2 小时，另一次构建与 15–25 分钟定向设备验证。R1 非活动 Pane
路由与 R2 平台取消仍为待证风险，不顺带修改；不增加触摸板识别/兼容层，不盲改权限，
不撤销 owner/搜索保护，不扩展为正式验收。若两种实质路径失败或新增显著成本，先向
统筹重述证据与替代方案；本轮按 K1/D1 软件证据、独立复核和正常开发包交付收口，
后续现场结论见本节开头的维护者系统授权后实测记录。

软件证据：独立复核已读取固定归档身份、真实 VT 失败样本 `k1-before.log` /
`d1-before.log`、修复后 `pointer-after.log` 及 `software-focused.json`，并记录判据。
上述原始文件在随后首次 Hvigor 构建期间消失，当前保留外部工作树的独立复核记录，
不能再把原路径当作可读取证据。K1 的 8 个 tracking/选区/Shift 组合、D1 的修饰键增加/
移除、精确远端释放字节和后续 gutter 命中通过；其余真实 runtime 合同及
`policy,terminal-native` 分组通过，未冒充手势验证。原始无 tracking 的右键 runtime
就能发出 paste-request；当时不能仅凭 K1/D1 解释“各处失效、Ctrl+V 正常”，
现由维护者开启系统剪贴板权限后的实测结果关闭，不再追加入口诊断。
已整合 Known Hosts 实际主线 `8c7fa1a`（PR #233）；其树与验证过的 `2a9ea47` 相同，
鼠标修复迁移后整树与 `c8d7bd6` 相同，复用对应软件证据，不因提交号变化重跑。
统筹已交接设备/构建签名独占权；本轮没有加入临时剪贴板或平台事件诊断探针。

PR #234 开发包源码 `5ff299f`，树 `f53df9f15d91025371731ab2aab2abe7c85213ce`；
签名 HAP SHA-256 `67155e884f81c6661f9ef9033ac442e9ca36618d66e20bd55c66e8c35231eec8`。
沿用标准 debug/test Profile，保留标准验收入口，排除前轮临时 Known Hosts 探针；
不是正式候选。覆盖安装后保留两个 ltty Tab，已迁移信任文件大小/摘要与交接一致。
普通公开输入精确到达本地命令缓冲后取消，未提交命令、注入右键或建立远端夹具。
原始构建在 Hvigor 子进程返回后因日志目录消失而写入失败；捕获器原本会预建目录，
不是 Hvigor 启动前失败。重建目录后第二次构建通过，保留该工具问题及原始证据缺口，
详见 [测试工具观察](test-release-efficiency.md#2026-09-23首次开发构建期间证据目录消失)。
本目录后续交付记录只改文档，软件与包证据按内容一致性复用。

### Known Hosts 文件权威：开发验证完成

维护者批准仅将 Known Hosts 改为应用私有文件唯一权威，升级保留已提交信任；清退后
卸载不再恢复信任。config、密钥和字号的 Asset 合同不变，无双写或缓存。按维护者
明确批准的狭义版本例外纳入未发布 1.7.0，旧候选和冻结证据不改身份。

- [x] 单一串行文件所有者、原子替换/同步及旧 Asset 校验后清退；34 项信任合同、
  14 项受影响软件检查、无探针稳态增删和九项生命周期真机检查通过。
- [x] 经维护者批准补齐首次迁移：原 10,659 字节内容只种一次为 12 条旧记录，
  最终无探针包首进程恢复精确原摘要并完成查询，之后审计旧 pointer/chunks 为零。
  文件缺失后无探针包查询不复活；恢复原文件、移除临时标记/备份，正常包恢复完成。
- [x] 架构、安全/隐私、双语指南、changelog 和 1.7.0 版本例外已同步。约 10.7 KB
  下确认到连接观测 3962→620 ms；删除含输入/观察 11265→8591 ms，不当作内部写入耗时。

[设计与证据](design/known-hosts-file-20260923.md) 保留旧 401、一次未定位 pending、
锁屏启动失败和布局传输失败。后两项发生在查询提交之前；首次迁移没有重新造数据
或重启进程试通过。实际整包卸载未做，不能用文件缺失检查冒充；正式矩阵仍暂停。
最终开发包 SHA-256：`6d8a4efe5dff37e87927880cf6470631080558e4224a4219c4f174925d78cc92`。
补证约 15 分钟完成，产品代码无额外增量；一次性入口和探针均已移除。

维护者暂停后续人工锁屏/合盖、C4 和正式矩阵，先串行完成以下两项。执行任务独占本轮
开发及必要定向真机验证，全部 HDC/UI/安装/夹具/共享端口/签名构建串行；若发现其他
任务占用则停止设备动作并通知统筹。仅零模型、受影响的软件检查和最小真机场景，
不自动重跑完整发布门禁，不要求人工配合；两项分别形成独立产品提交/PR。

- [x] Host Key 新增/删除延迟的最小优化及定向验证：追溯既有异步持久化优化，先测 UI 输入、队列等待、Asset
  读取/写入、投影提交与完成反馈边界，区分主线程阻塞和实际提交耗时。后台执行只是
  候选方案；复用既有串行操作与提交确认，不提前信任或报成功，不静默失败、不乱序。
  明确 pending、失败、重启恢复与紧接重连语义；用相同数据规模的前后计时及信任/删除
  定向验证判断收益。先读既有诊断，再记录本次可证伪假设，定位前不改产品行为。
  [09-23 证据](design/trust-persistence-latency-20260923.md)：约 10.7 KB 下，慢点为安全
  存储多次调用，非连续 UI 阻塞。四块并发无明显收益，已撤回；仅采用官方允许的
  1024 字节块，新增 3408→2806 ms、删除 3339→2763 ms，仍有约 2.8 秒等待，不做
  第三种存储改造。32 项信任合同、16 个受影响软件检查及无探针包 9 项真机检查通过，
  含旧数据读取、重启持久化、删除后立即/重启重连重新确认和清理；[PR #231](https://github.com/wandcs/leantty/pull/231)
  四项 CI 通过并合入 `45b3aef`，本地主线与远端提交/树一致。
- [x] 光标会话生命周期：本地默认闪烁短竖线，活跃远端尊重标准光标样式/模式；检查
  VT、渲染与 Session 退出/断连/重连的所有权，清除离开会话后的远端配置，保持 Pane
  隔离。先追溯旧迁移和重置路径，用协议与上游主源及真实 VT 反例定位，再验证实际
  SSH/Vim 模式、退出、下个服务器与分屏；不增加程序识别或渲染层补偿。
  [09-23 调查](design/remote-cursor-lifecycle-20260923.md)：受控真机六种形状/闪烁均遵从
  远端，明确配置的 Vim 可切换竖线/块状/下划线；原始 Vim 的终端光标序列为空，
  但这不能替代维护者现场根因。维护者随后在 WSL 使用 Helix 确认需要方块时已正常
  变方块，认为问题已解决；按现场确认关闭，无新增光标产品修复，不再追问环境。
  正常/异常退出、重连、
  两个独立 SSH 服务与 Pane 隔离已完成受控核对，新增原生重置与 Session 回归通过。
  首轮输入拦截和补充清理如实保留；最终夹具/进程/映射清空，原 Tab/设置恢复，设备
  已释放。独立 PR 交付证据与合同回归，既有受控证据保留，不将本次现场确认写成
  新增产品修复。

冻结证据保留：源码/工具 `02a5e7d884762b120c5818f138556c64d47264f3`，tree
`a139efb83bd68a7b589c2fdb2b4ad996e0c0f802`；候选 SHA-256
`3966eab6bbde57717d350a45c1ee0c9f59b1533298023de730a679ee07dfd8be`。
本地 `LeanTTY-release/formal-verification/1.7.0-automatic-ecdsa-20260922/` 自动报告
为 `awaiting-operator`：24 阶段通过、复用本轮新候选 1 项，人工两项零执行。
原报告、候选及独立冻结检出均不修改；本轮修复改变产品字节后不得移植其通过结论，
未来正式验收重新评估身份和范围。本段取代下方旧批次的继续正式验收授权。

## 流程整改状态与开发来源

核心整改已交付，不再把已完成批次和持续规则列成开发任务：

- [PR #208](https://github.com/wandcs/leantty/pull/208)：源码清账、规则收敛及既有续验能力核对。
- [PR #209](https://github.com/wandcs/leantty/pull/209)：命令完成观察及脱敏失败证据，
  离线、ARM64 构建和定向真机验证通过。开发证据不等于正式工具资格。
- 已实践受影响验证、同链整批交付、自动工作先行和共享资源交接；未重跑 1.6 正式矩阵。
  证据见[流程整改记录](test-release-efficiency.md#2026-09-16开发流程整改基线清账与既有续验核对)
  及[命令完成修复](test-release-efficiency.md#2026-09-16命令完成观察链修复与定向真机验证)。

2026-09-17，维护者授权按逐文件审查结果整理根目录：143 个本地差异中，132 个已在主线、
已被主线更新取代或只有格式差异；独有内容为六个原型设计/进度文件、三个验收历史文件及
两个旧待办快照。全部原文件已按 SHA-256 验证备份；逐路径清单与备份位于本地忽略的
`build/verification/workspace-audit-20260916/` 和 `build/verification/workspace-sync-20260917/`。
研究源码、原始证据与冻结验收继续保留在本地，不进入公共提交。

后续开发从最新干净 main 开始，不从旧混合区复制工具或重复采用过时补丁。根目录清理前
核对备份与文件是否变化，同步后以提交、树、ahead/behind 和工作区状态证明一致。

2026-09-22 再次完成[本地/远端清账](design/workspace-sync-20260922.md)：起始 main
及 13 对同名分支完全一致，60 个本地分支头中 59 个已由主线或已合入 PR 覆盖。
旧命令完成工作区是 PR #209 的早期草稿，不回灌；两处维护区的 14 个改动文件及
index/working patches 已备份并保留原状。ECDSA 补丁 #227 已完成定向真机验证并合入；
PUT/GET 映射补丁 [PR #228](https://github.com/wandcs/leantty/pull/228) 已完成真实枚举、
下载/上传和独立清理核对并合入 `2613710`；这些诊断不等于发布验收。远端三个依赖升级
PR 当时保留独立评审；09-24 已完成评审并明确范围，见本页开头，不为清空差异而升级。
冻结发布检出及历史证据不移动、不删除。

## 独立工具维护：剩余可执行事项

2026-09-16，维护者授权将以下工作交给既有任务 **优化验收工具并隔离执行**。
这是当前执行清单；效率记录保存问题和证据，不另建第二份待办。按以下顺序执行，
条件未满足的项目保留待触发，不用重跑产品验收来制造需求。

### 1. 图标工具 sharp 安全告警：本批实现与验证完成

- [x] 在干净 main 上复核 Dependabot #6 和上游修复范围，完成最小必要的安全升级。
  既有记录为 sharp 0.35.3、首个修复版 0.35.4；执行时再确认，不盲追最新版本。
  范围优先限于 `tools/package.json`、锁文件及必要的工具回归，保留无关依赖变更。
  验证锁定安装、实际版本/许可证及固定 SVG 生成的七张 PNG；输出留在隔离目录，
  比较尺寸、像素/文件差异，不自动改 AppScope/entry 的正式图标。
  **完成条件：** 漏洞范围已排除、生成器定向检查通过，差异和剩余风险清楚，独立 PR 交付。
  不跑漏洞利用样本，不构建 HAP、不占用真机，不通过手工关闭告警代替修复。
  依据：[告警与升级记录](test-release-efficiency.md#2026-09-16sharp-图标工具安全升级)。

**本批执行（23:00 起，独立分支；定向验证已通过）：** 以 main `9f85ff6d` 为基线，仅将 sharp 最低版本
和锁文件更新至首个修复版 0.35.4，核对相关预编译依赖及许可证。用原生成器、同一固定
SVG 在两份忽略目录做锁定安装和七张 PNG 的尺寸/像素/文件比较，不改正式图标。
预算 30–45 分钟加 CI；本批已验证 sharp 0.35.4、libheif 1.23.2、七张 PNG 完全相同和 audit 零告警，随本 PR 交付。若需升级无关依赖、修改产品资源、处理不明视觉差异或达到
两种修法/90 分钟检查点，先重审范围，不盲重试或扩大到 HAP/设备。

### 2. ASCII 输入法前提：显式方案与命名场景验证完成

- [x] 基于 PR #209 的失败样本，确定英文输入模式的最小可靠确认、必要切换和原状恢复办法。
  本轮中文模式转换标点导致提交前拒绝，约 94 秒；这不是历史自然丢字的完整归因。
  优先复用现有交互和观察入口；比较显式准备与小型前置检查的总成本后再决定是否自动化。
  **完成条件：** 明确执行前提、观察证据和 finally 恢复；无法确认时在 Enter 前停止，
  不猜测模式、不盲按切换键、不把非 ASCII 一律归因环境。实际补丁需受影响离线反例及
  一次已授权的命名真机场景；仅有方案时标为待真机验证，不称已修复。
  设备不可用时完成离线部分并交接，正式使用相关自动场景前完成前提确认。
  不改 xterm/产品输入语义，不新增通用输入法管理框架。
  依据：[本轮样本和限制](test-release-efficiency.md#2026-09-16命令完成观察链修复与定向真机验证)。

**本批离线准备：** 预计 20 分钟，只核对现有失败、观察入口和恢复责任，
比较显式准备与新增 helper 的成本；不使用共享设备。出现可靠模式观察缺口即保留待
真机验证，不猜测切换协议或扩建通用管理器；沿用两种实质修法/90 分钟重审点。

**离线结论：** 已选择显式观察、必要切换、确认及 finally 恢复方案，见
[取舍记录](test-release-efficiency.md#2026-09-16ascii-输入法前提的离线取舍)。
现有三份布局不足以当模式 getter，不新增切换 helper。09-19 已验证系统菜单英文准备
及中文拼音恢复；09-24 当前开发任务独占设备，以新候选 password-success 正式 QH
验证显式英文前提：前后系统菜单、原生焦点、3 次精确输入/单次提交、独立清理均通过。
本轮原模式已是英文，依规则零切换并核对原状；复用历史模式切换证据，不为了覆盖率
修改当前设置。此项关闭的是准备/观察/恢复办法，不宣称历史自然丢字根因已查清。

### 3. AT16-07 PUT/GET：映射修复已完成定向验证

- 2026-09-22 本地/远端清账：将原维护区的已有三文件代码补丁移植到最新 main，
  独立草稿 PR 保存；原工作区、文档快照和补丁已按摘要备份且不改动。仅重验现有
  离线反例及相关工具检查；设备正在充电，真实 HDC 枚举、PUT/GET 和清理仍待验证。
  此次推送不是正式采用，不热更新冻结工具、不重跑正式矩阵。
- [x] PR #228 的 9 项离线反例、合并后的设备工具回归和源码检查通过。设备接回后，
  `fport ls` 实际列出 `rport` 创建的 `[Reverse]` 映射，移除后回到空列表。
  131,089 字节 GET→Downloads 自动编号→PUT 的 SHA-256 一致；原文件保留，
  临时目录/服务器/映射独立审计不存在，Downloads 原权限恢复。首轮因授权关闭在准备
  阶段失败，随后复用现有权限助手运行；保留失败记录，不改产品或冻结工具。
  旧基础场景遗漏临时主机信任自动清理，已通过一次产品命令删除并独立确认；本次
  清理有明确补充操作，不宣称脚本独立覆盖完整收尾。证据在
  `build/verification/workspace-sync-20260922/put-get-device-r2/`，问题交接见
  [工具记录](test-release-efficiency.md#2026-09-22putget-定向验证的前提与收尾)。
  不运行完整文件传输或发布矩阵。

### 4. 下次正式候选启动时触发：工具采用与发布交接

- [x] 当前开发任务按维护者第 5 项授权采用已合入的 #209/#228 工具，来源、定向
  证据、未验证项及输入法/PUT/GET 清理责任已记录；新候选 QH 正式通过，冻结
  工具提交/树与 HAP。旧维护区及冻结轮次不移动、不复制补丁、不热更新。
- [x] 新候选完整行为矩阵及 C4 真实生产包验收标记排除检查已完成，最终证据见本页顶部。
  PR #209 的开发验证不能替代这些门禁；不热改正在运行的工具、不改写旧失败，
  不重开已发布 1.6。发布任务直接使用既定材料位置和入口，区分 GitHub 已发布、
  production APP 已交付与维护者已提交 AppGallery；自动化不操作商店。
  **完成条件：** 当轮工具/产品/材料身份与证据适用范围明确，责任交接可追溯。
  下一次采用基础 PUT/GET 时，明确 Downloads 授权准备/恢复与临时主机信任移除的执行
  责任；可复用本轮受控辅助步骤，不能仅依据旧脚本的传输 Pass 声称清理或正式资格通过。

## 持续执行规则（不作为未完成 TODO）

沿用[质量策略](quality-strategy.md)和[编码指南](coding-guide.md)，不新增治理框架：

- 重试先说明新证据或改变的前提；使用入口已有的 `-PreviousAttemptId` 关联原失败。
  漏传时保留原报告并补充关联说明，不补写原字段、不因此再跑一轮。
- 缺少受支持续跑时，比较有限修理、等价人工取证和重跑成本；不手工拼接 Pass。
  正式长矩阵先核对环境、观察及清理通道，开发只跑受影响检查和最小稳定冒烟。
- 第三方限制须有定位证据；原因未知不能豁免。达到两种实质修法或 90 分钟未解决
  检查点时，实际选择简化、替换、延期或新假设，不只补记录后继续原循环。
- 自动工作和准备先完成，真正就绪才请求维护者操作；提示音与语音使用系统默认设备。
  已有实时提示 wrapper 的离线证据可复用，旧轮次身份不可照搬；不自行管理 WSL 生命周期。
- PR 按完整事件链交付，简记中断、重复成本和人工介入；达到证据目标即停止扩展。
  无数据记未知，不为流程优化另建指标平台或要求清零所有历史工具问题。

## 维护任务的执行与回报边界

实施前在对应活动项记录本批范围、预计投入和停止条件；先做无需设备的工作。
共享 HDC、真机、WSL、端口、签名或构建资源须与正在执行的任务明确交接，
独立目录不等于资源隔离。1.7 产品开发由当前开发任务负责，工具维护交接不授权接管或抢占资源。
依赖升级和输入法修理属于不同事件链，分开审查；同链的前提、观察和清理一起交付。
保留根区和冻结验收，Git/PR 使用既有身份与流程，验证后仅在下一轮启动边界采用。

维护任务收到交接后确认采用来源、当前执行项和受条件限制项；有成果时回报提交/PR、
实际测试及未运行项、共享资源清理和下一轮采用条件，同时更新此处对应状态。
本次文档交接不等于任何补丁已实施、正式资格通过或新发布获授权。

## 其他既有工作（不属于本轮流程整改）

### AppGallery 提交与状态回报

- [x] 1.6.0 商店提交与审核交接闭合：维护者于 2026-09-20 明确确认已过审上架；
  本地归档 production APP 哈希与既有交付记录一致。
- [x] 据维护者回报记录 AppGallery **Released** 并同步当前状态文档。09-20 是确认
  日期，不推定具体提交/审核/上架时刻；自动化未操作商店。身份与边界见
  [1.6.0 发布记录](release/1.6.0-status.md)。

现有发布与签名材料固定先查维护者已指定并记入本地记忆的发布根目录；不再重复询问位置。
口令和证书留在本机，不复制到源码、公开附件或日志。文件缺失、不可用或需要新授权
时再询问维护者。GitHub 已发布版本被拒后按规则递增 PATCH，不移动 tag 或替换旧包。

窗口反馈保留为非阻塞观察：异常恢复提示后，外边框曾无法拖动缩放。本轮同包单 Pane
正常/异常恢复对照均能拖动，尚未复现原现场，不称已修复。等待原窗口模式、边缘、
输入设备条件或再次发生时捕获；不重复强杀、重跑矩阵或添加窗口 workaround。

### 重启恢复 warning：纳入 1.7，取消常驻文字、保留恢复

- [x] 09-24 核对触发链：仅以上次 runState=RUNNING 判定，不能区分崩溃、系统回收
  或强停；SessionViewModel 将说明写入终端历史。维护者明确选择“取消常驻 warning，
  保留恢复机制”。这是对先前只登记边界的后续范围决定。
- [x] 移除该输出及仅供它使用的状态，保留布局恢复、checkpoint、正常退出标记、
  日志和真实连接/运行时错误提示。核对正常退出与恢复后的提示/布局，不把远端会话
  和历史内容说成已恢复；不增加 toast、设置项、退出原因推断或另一套恢复逻辑。

### Host/Key 列表：纳入 1.7，统一多行字段输出

- [x] 09-24 维护者确认同一多行格式：长文本自然换行，完整标识符/指纹不截断，
  宽窗、窄窗与双 Pane 使用同一输出合同；不增加 --wide/--no-trunc 等选项。
- [x] Host 展示完整名称、目标地址、Identity，以及已配置的跳板/超时/保活；
  IPv6 与端口可区分，自动 Identity 明确标识为自动，字段沿用已有配置解析语义，
  详情复用 `ssh -G`。Key 展示完整名称、类型、完整指纹、口令保护状态和 comment；
  空 comment/无口令明确表示，只投影现有元数据，不为列表解密私钥或显示内部私钥路径。
- [x] 在现有命令输出所有者统一排版与字段投影；保留控制字符转义，覆盖长 Host/key、
  IPv6、空/长/Unicode comment、窄/宽窗口、双 Pane、重启持久化及列表/详情一致性。
  具体示意和延期边界见[本轮范围决定](design/dependency-scope-review-20260924.md)。

09-24 实施：KeyCommandService 直接投影已有元数据；Host 的超时/保活显示生效值（含默认值），
与 ssh -G 同源，避免新增“是否显式配置”的状态或遗漏显式默认值。警告专用状态与输出已移除，
恢复/checkpoint/正常退出链保持原实现。直接所有者测试通过长名称、IPv6、继承配置、完整指纹、
空/长/Unicode 注释与控制字符转义；policy/arkts 14 项检查、指南字节一致性及浏览器 63 项检查通过。
已审阅本次中英文宽/窄段落截图；指南 rev 12 与实际字段一致。标准 ARM64 签名开发构建通过。

开发包源码 `f3a000b`（与产品提交 `b49db40` 整树相同），SHA-256
`72fa0daee7911cf598083b6367eabd9e1879f7176958b19b464327f7dbd73474`。
真机宽视口/双 Pane 截图确认完整 Host、IPv6 端口、Identity、跳板/生效参数、长 key、完整指纹、
中文及空注释。双 Pane 的窄视口作为自然换行的代表，不再重复等宽窗口尺寸组合；口令保护投影
由直接软件合同覆盖，未为这一只读字段再次设置口令。强停后日志 `unexpected=true`，恢复三 Tab、
双 Pane 且仅显示提示符；正常系统关闭确认 close preparation 后，下次 `unexpected=false`、单 Pane
无 warning。重新列出的 Host/Key 与重启前一致，没有连接远端或恢复旧终端内容的扩称。
临时 Host/Key 通过产品入口删除，独立文件/配置检查及再次重启确认无残留，恢复原两个空闲 Tab；
各段屏幕超时设置已还原。一次性脚本路径错误已修正，长 UiTest 输入短写被提交前校验拦截，
改用精确通过的短配置命令；不抹去初次失败或称完整矩阵一次通过。
开发证据 `build/verification/local-command-presentation/`，通过 PR #239 交付；
本轮未卸载、重置用户数据、运行模型或恢复正式验收。

### 兼容性复杂度与长期维护成本审查

- [x] 系统盘点现有实现及在研方案中，为兼容额外环境、异常条件或低频场景而增加的
  后备实现、适配层、特殊分支、依赖和状态协调。覆盖终端渲染、输入与窗口、SSH/Mosh、
  平台集成和构建工具；优先检查并行实现、失效后仍保留的补丁，以及缺少维护者确认的措施。
  以曾未经确认引入的“GPU 不可用时使用 CPU 渲染”为警示案例；该方案已按 09-19 决策
  撤销，本项不重新打开 CPU 兜底开发。
  先列事实，再分类：逐项记录代码/方案位置、引入背景、目标场景与真实需求证据、维护者
  确认依据、当前是否仍生效，以及新增复杂度、长期维护和验证成本。区分必要的标准支持/
  核心正确性、已明确批准的兼容措施、缺少依据或确认的额外兼容、已经失效的历史补偿；
  找不到确认依据时标为待核实，不把所有恢复逻辑一概视为冗余。
  **完成条件：** 交付有证据的清单和保留、简化、删除或待核实建议，说明不支持对应场景的
  实际影响、更简单的替代方案及清理所需的定向验证，按用户价值和维护成本排序。
  按[产品原则](project-principles.md)第三节核对兼容性准入；涉及撤回已发布合同或改变支持
  边界的建议先交维护者决定，不能借审查静默删减能力，也不能为验证兼容性继续增加机制。
  **执行边界：** 首次仅登记；09-19 维护者随后授权执行充电期间的有界只读审查。
  已交付[兼容性成本清单](compatibility-complexity-review-1.7.md)，区分必要正确性、已批准的
  过渡实现、待核实依据和可删除候选；未改产品/工具代码或使用测试机。
  最大清理收益在原生替换门通过后统一退出旧 Web/DOM、补丁及恢复链，归下方既定替换任务；
  原生 GPU 有界恢复、字体栅格化、Mosh 临时页和旧数据迁移保留。旧缓存导入/部署包装收益低，
  暂不启动清理，待缓存预热或入口维护实际触发时核对调用者与成本后再纳入本清单。
  审查完成不等于清理完成，也不证明每项历史措施都曾获得单独确认；不启动新的兼容机制。

### 1.7 原生终端原型与产品交付

2026-09-17，维护者确认原型调研通过，授权进入 1.7 开发。实现从干净 main 的
`codex/1.7-native-terminal-runtime` 分支开始；原型结论作为接入依据，正式替换门禁仍需
产品证据。此授权不推断 1.6 的 AppGallery 状态，合入 main 前仍核对既定发布顺序。

**首批范围：** 移除 SessionViewModel 对 Web Bridge 消息格式的依赖，将输入、有效尺寸和
交互就绪投递改为终端回调，保留当前输入路由、认证、SSH/Mosh resize 和 Surface 过期事件
隔离。这是原生接入的最小会话准备，不对旧 Web 输出/快照系统做架构改写。
先验证真实 ArkTS 所有者的事件路由与过期/非法消息反例，再做受影响编译检查；不为纯内部
边界提取运行完整设备矩阵。预计一批完成；若必须改变输入来源语义、会话生命周期或
恢复合同，停止扩大此提取，随原生运行时的对应事件链实现和验证。

- [x] 首批终端事件边界实现与定向单元验证：SessionViewModel 不再解析 Bridge 消息；
  Surface 提供输入、尺寸和交互就绪回调。6 项真实所有者合同测试及 `policy,arkts` 分组
  通过（202 项 ArkTS 单元、零失败），既有 Web 终端策略回归及 ARM64 debug 构建通过。
  本地证据：`build/verification/1.7-terminal-session-events/`。未部署设备、未通过完整替换
  验收；本批没有用户可见功能，版本源和 Changelog 暂不递增或预告未实现能力。
- [x] 接入固定 Ghostty 源码与可重建 ARM64/API 24 构建、Pane 所有的原生 VT、消费屏障和
  有界队列；随后接通同一核心的合成输出与单 Pane SSH、原生 GPU 和输入。原始研究目录
  仅用于证据与代码审查，不能成为产品构建的隐式依赖。

**已完成整批（09-17，维护者要求粗粒度连续执行）：** 固定依赖构建 → 原生有界运行时与
消费屏障 → 官方文字/GLES 表面 → 单 Pane 本地输入/SSH，连续完成受影响验证后回报。
同一串行执行域拥有 VT、字体与 GPU，Surface 解绑保留 VT；不把刷新完成当解析确认。
先以开发构建接入，完整交互/恢复门未闭合前保留 Web 实现。真机只跑该链命名场景，
不跑发布矩阵；原型恢复证据复用为设计依据，不能冒充新产品运行证据。出现两种实质
修法失败、90 分钟未闭合故障或需要改变已接受合同，执行既定重审，而非不断加补丁。

本批实现及证据（09-17）：

- 固定官方 Ghostty/Zig/传递依赖摘要，独立于研究目录构建 ARM64/API 24 原生库；
  同步保留 Ghostty、uucode/Unicode、zlib、Zig、LLVM 的完整许可。
- Pane 所有的串行 VT、消费屏障、有界输出队列与回复归属接入；官方字体/GLES、
  XComponent 和系统 IME 在 debug 启用，release 暂保留 Web。
- 真机已验证本地 help、中文 IME 预编辑/提交、英文精确输入、Ctrl+Shift+T/W、
  单 Pane SSH 主机校验/临时密码认证、远端标记和受控输入、正常退出尾部及返回
  ltty；远端 115 字节输出全部进入 N-API，最终投递失败为零。Tab 解绑/重挂保留
  当前 VT 内容。此为定向开发证据，不代表 Mosh、完整交互、GPU 恢复或发布验收。
- 首次启动故障已定位并修复：aboutToAppear 请求未挂载焦点导致 150003，改为成功
  出帧后兑现焦点意图。临时诊断脚本的 Action 参数遮蔽曾产生无效输入证据，改名
  后实际快捷键通过；不把该工具问题当产品缺陷。另一次缩略图漏读被原尺寸裁剪和
  逐像素比较推翻：切换 Tab 前后底部零差异，异步结果/提示符原本已显示，脏帧
  调度和提交正常；未添加 workaround，已移除所有临时阶段日志。
- 构建工具唯一必要适配是 Mosh 输入拒绝注入的 writeMoshBytes 新归属参数锚点；
  既有 tooling 回归通过。原生关闭在 worker join 后释放 TSFN、IME 和 renderer，
  避免 TSFN callback → ArkTS controller → native handle 的存活引用环；异步工作
  分配/排队失败也释放已创建的引用和任务。
- `policy,tooling,arkts,terminal-native` 通过：202 项 ArkTS 单元、6 项会话边界、
  5 项原生 ArkTS 所有者合同及真实 Ghostty/C++ 合同，warning 基线 47 不变。
  本地证据：`build/verification/1.7-native-terminal/software-final-2.json`。
  最终 ARM64 debug 构建、签名、安装和原生创建/本地 help/关闭 smoke 通过；最终包
  SHA-256 为 `7D39F7533D84F554DA988C5B8C3B3ACC17B7C1C7D327704835391709C8DDA25F`。
  本批 SSH 证据包与最终关闭清理包的差异及复用范围见本地 `batch-evidence.json`。
  临时 SSH 进程/凭据目录与反向转发已清理，测试 Host 记录经只读查询确认不存在，
  测试 Tab 已关闭，恢复原有三个 Tab、第二 Tab 焦点和中文拼音。未发布或提交商店。

维护者已确认官方 Ghostty、API 24、原生 GLES 和最小会话解耦方向。设计见
[平台预研](design/ghostty-native-terminal.md)及[架构边界](design/terminal-session-boundaries.md)。
09-16 独立原型已支持技术可行性；追加 GPU 诊断修复了初始化残留、失效 Surface 和 swap
完成计数问题，21 幅回读与参考的最大通道差异为 1。完整记录和证据身份保存在研究文档，
完成项不继续列为活动 TODO。没有观察到自然驱动持续故障，不把原故障页提案当成已接受
合同，也不把解析微基准当整体性能收益。

- [x] 原生既有交互与系统效果开发批：补齐键盘编码/IME 候选定位、选择复制粘贴、
  历史滚动与搜索、主题/字体样式，以及既有安全策略下的标题、链接、OSC 52 和注意力
  语义；按真实事件链做所有者测试、受影响编译与命名物理场景，不提前启用发布替换。

本批实现及定向证据（09-17；完整替换门仍未通过）：

- 原生 worker 接入上游键盘、鼠标、选择、粘贴与搜索 API；历史搜索含结果计数、方向、
  空结果和主/备用屏隔离。ArkTS 保留输入焦点、异步剪贴板与 Session 代际边界。
  补齐 IME 光标候选定位、预编辑、固定主题、字形装饰和彩色 emoji；完整 OSC 序列才可
  进入既有链接、OSC 52、通知与注意力策略，远端标题不能改写 Session 标题。
- 真机通过中文 IME 候选定位和提交、搜索及键盘焦点返回、历史翻页、双击选词 → 复制
  → SSH 原字节粘贴、固定样式/中文/组合字符/彩色 emoji 输出、OSC 52 emoji 粘贴、
  远端标题隔离、后台 BEL 标记与返回确认。未将安装、启动或主机测试代替物理行为证据。
- 双击选词曾导致 worker 失败：PRESS 错用了只属于 DRAG 的 GEOMETRY/RECTANGLE 字段。
  真实 VT 测试先复现，再按事件类型修复；真机选词复制后的 23 字节回显完全一致。
  原失败保留，没有改写为通过。
- `policy,arkts,terminal-native` 定向检查通过：202 项 ArkTS 测试、6 项 Session 合同、
  8 项原生控制器合同和 11 组真实 VT 合同；既有 47 项 ArkTS 告警无增量。
  `tooling` 复用同批已通过且未受后续产品修改影响的结果。
- 12:20 准入的独立组阻塞修复已闭合：`terminal-native` 漏选 DevEco 初始化，导致加载
  TypeScript 前出现空路径。只补齐环境依赖选择；原失败命令独立复验通过，没有安装或
  扩大产品检查范围；受影响的构建工作流测试也通过。失败与修复后证据分别保留。
- 11:57 重审后，停止用非精确 UiTest 文本注入诊断 Unicode 输出；经本批准入，在旧轮
  清理后采用既有 SSH 夹具无参数的 376 字节固定公开样本，离线边界断言通过。该路径
  证实原生输出及系统剪贴板 emoji；自然 emoji 输入和自动注入差异仍不记为通过。
- 最终签名开发包 SHA-256：
  `ECD6E889ABC13A5FA33CF8F12A8157DFD9B58D1791C1648B96DABDC62B6D9346`。
  SSH、测试 host key、映射和临时凭据已清理，关闭测试 Tab 并恢复原有三个 Tab、第二个
  Tab 焦点及中文拼音输入法。证据、构建间复用范围和剩余缺口见本地
  `build/verification/1.7-native-interaction/batch-evidence.json`。

- [x] 完整替换前补齐交互物理缺口：Ctrl+点击普通 URL/OSC 8 的系统打开结果。键盘、
  Mosh/双 Pane 主路径及自然 emoji 已在下述批次定向验证；链接元数据和路由主机测试通过，但自动点击
  未取得系统打开证据；不扩大临时日志或工具框架，将此可恢复开发缺口合入下一批，
  发布构建继续使用 Web。完整交互验收条件保持不变，不将缺口记为第三方豁免或通过。
  - 09-19 后续人工体验：维护者按本轮体验说明反馈“alt 矩形 URL Ctrl 点击这些都正常”。
    据此记录普通 URL/OSC 8 的实体操作为维护者确认通过，关闭开发期人工缺口；上述
    未取得结果指此前自动注入。没有补造浏览器截图、逐事件日志或正式候选验收记录。

- [x] 09-18 原生键盘主路径与 IME 删除方向：物理 Backspace（2055）错误发送 Delete
  的 `ESC [ 3 ~`，最后正确边界是系统 IME 删除请求，首个错误边界是 TerminalInput
  的回调注册。与官方 C API 合同核对后，真实注册语句反例先失败；互换两处映射后通过，
  不改键编码器或 IME 前后分派。回归纳入既有 `terminal-native` 组。
  - 最终签名包的 23 个物理样本逐个通过服务器精确字节比对：方向、Enter、Backspace、
    Delete、Insert、Home/End、PageUp/PageDown、Escape、Tab/Shift+Tab、Ctrl+Left/P/C、
    Alt+B、F1/F5/F12 和 Ctrl+V。Backspace 为 `7f`，Delete 为 `ESC [ 3 ~`；粘贴等于
    夹具通过 OSC 52 写入的 17 字节。应用光标模式由本批真实 VT 合同验证，不伪称真机模式切换。
  - `policy,terminal-native` 五项检查通过，普通 ARM64 签名包构建、安装和实际交互通过；
    ArkTS 源码未变，复用上一批 202 项单测。产品源码仅两行 IME 映射变化；工具仅加入
    同边界回归。原准备失败、错误 Web 日志判据及产品红例均保留；不增加产品日志。
  - 自有 SSH 正常退出 0；临时 Tab、信任、映射、服务器及凭据目录已清理，恢复原三个
    Tab、第二 Tab 焦点和中文输入。证据：`build/verification/1.7-native-keyboard/`；
    HAP SHA-256：`92149CD97E766B055D064391316D44CDC023A4D84E1D6440AB21D21BE75BE082`。
    这是定向开发证据，链接系统打开和完整替换门仍未通过。

**本批完成（09-17，Mosh/双 Pane 与表面恢复）：** 复用既有受控服务器，以原生布局、
服务端精确快照、搜索、截图及日志完成定向开发取证。既有 Web 矩阵未冒充原生资格。

- 修复 Mosh 首帧早于 connected 时清屏污染本地历史的问题。真实 Surface 合同先复现
  `write → page` 错序，再改为首帧确保临时页，connected 幂等，不改协议通知时序。
  真机原失败搜索为 0/0；新包退出后同一本地标记为 1/3，本地历史恢复。
- 接通 `surface-unavailable → Pane recycleSurface`，重建限为每 30 秒最多一次，
  过期 Surface 销毁回调不能解绑替代者。原生 VT 与 Session 保留，出帧前不接收输入。
  现有编译期验收重建入口增加原生分支；新包启动前已清理旧设备轮次。
- 新包在 Mosh 备用屏中实际重建 XComponent（121 → 208，同一 Pane），保留屏幕标记和
  同一 Mosh 服务端进程；后续精确命令 `afterrecovery` 及正常退出通过。活动 Tab 切换
  按策略保留已挂载表面，只有上述身份变化可作为本轮重建证据。
- 同轮旧包验证双 Pane 各 77 列、独立 Mosh/SSH 显示与路由、Tab 切换和 Mosh 退出后
  另一 Pane 的 SSH 输入；新加双运行时 VT 合同覆盖重建和一侧关闭后的另一侧继续。
  SSH 测试标记中的字母/下划线到达，末尾数字未到达，故不声称完整输入标记通过。
- `policy,arkts,terminal-native` 通过：202 项 ArkTS、7 项 Session、9 项原生控制器及
  12 组真实 VT 合同，47 项告警无增量；受影响构建工作流检查和 ARM64 签名开发包通过。
  最终包 SHA-256：`A43F1B81108D0F0C49B4E2DE2DD29676C717CF2FB32DAE8249DE15413413DDF8`。
- 自有 SSH/Mosh、测试 Host/信任、转发及临时凭据已清理，恢复原有三个 Tab、第二个
  Tab 焦点和中文拼音。锁屏使用 `docs/dev-environment.md` 已记录的无人值守解锁入口；
  不因普通锁屏要求维护者再次操作，不读取或记录口令。完整证据和复用边界见本地
  `build/verification/1.7-native-continuity/batch-evidence.json`。

**本批完成（09-17，数字输入与自然 emoji）：** 本批按 45–60 分钟定向诊断范围执行；
固定数值探针定位后删除，未保留终端正文或秘密诊断，也未扩大软件/设备矩阵。

- 同模式、同焦点下两个 SSH 夹具均复现 `a17b → ab`。数值日志确认字母走 IME insert，
  数字仅回到组件按键处理且无修饰键。首个错误边界是 Pane 忽略 IME 未消费的普通键。
  NativeTerminalPane 现区分 IME 前/后阶段，仅后者补接未消费的文字和编辑键，优先使用
  平台 Unicode；真实方法反例先失败，修复后通过。
- 最终包中 `a17b0123456789` 在 Password 和 Navigation SSH 均精确到达。中文拼音
  `weixiao` 实际形成候选，数字 2 选择 😊 后服务端仅收到 `f0 9f 98 8a`，没有泄漏候选
  数字。彩色 emoji 回显另有截图。ASCII 命令缓冲不支持 Unicode，最初空快照是无效
  判据，保留原失败；随后改用已有 Navigation 原字节记录，未修改夹具产品。
- `policy,arkts,terminal-native` 通过：202 项 ArkTS、7 项 Session、10 项原生控制器与
  12 组真实 VT 合同，47 项告警无增量。最终 ARM64 签名开发包 SHA-256：
  `D08F02695C50A77AB01C4CF540CBA7FAF66D194DD90394F72D86E0E5EC173BBC`。
- 自有 SSH、信任、反向转发、临时凭据已清理，测试 Tab 已关闭，恢复原有三个 Tab、
  第二个 Tab 焦点及中文拼音。证据身份和复用边界见本地
  `build/verification/1.7-native-input-links/batch-evidence.json`。

后续继续链接、完整交互与恢复替换门：

- [x] 修复后 Mosh/双 Pane 主路径定向验证（09-17 至 09-18）：沿用 A5B12039 签名包、
  PID 64386 和既有受控 stock mosh-server，按原生布局操作既有命名场景，不改公共矩阵。
  本批没有产品源码变更；核对上一批九个相关文件摘要一致，复用既有软件与构建证据。
  真机 preflight、实际交互、服务端结果、原生搜索及清理均另行留证。
  - Bash、tmux 的真实程序检查和正常退出通过；Vim 保存的 `editor_native17vim`
    与预期完全一致。512 字节持续输入及 32 个输出帧通过，完成标记搜索为 `1/1`。
  - 中文宽字符与组合音符可见；242 行输出末尾搜索 `1/1`、首行 `0/0`，符合 Mosh
    零历史临时页面合同。less 首页/末页及正常退出通过；备用屏进入、退出和状态投影
    保留的标记均为 `1/1`，不将其解释成 SSH 字节流式历史。
  - 分屏后 Mosh PTY 从 154 列变为 77 列；左右完整数字标记分别通过服务端核对。
    双向搜索证明自身输出可见、另一侧标记不存在。关闭左侧 Mosh 后，其两个服务端
    进程退出；原右侧 SSH 回到 154 列，继续精确执行 `AFTER170123456789`，正常退出 0。
  - 本地证据：`build/verification/1.7-native-mosh-mainpath/batch-evidence.json`。
    两个临时 Host、信任、转发、服务器和凭据已清理，原三个 Tab、第二 Tab 焦点、
    中文拼音与窗口状态恢复。此为主路径开发证据，未跑正式矩阵，未通过完整替换门。

**本批完成（09-17，真实窗口创建拒绝后的会话恢复）：** 将显示故障与终端核心失败
分开，限定修复 NativeWindow 创建拒绝，不修改输入、协议或重试预算。

- 真实 `TerminalRenderer::attach` 方法的反例先以 `terminal_window_failed` 失败：
  创建窗口被拒绝后异常逃出 Display 命令，使整个 VT worker 退出。修复后返回显示
  不可用，由 N-API 投递既有恢复事件；非法参数、字体、VT 与事件队列失败仍为错误。
- 现有验收重建入口现经过不存在的 Surface 和真实 NativeWindow API 拒绝，再调用
  生产恢复链。入口仅由版本化脚本在验收构建期间注入，源码还原和发布包排除测试通过。
- 真机同一 PID 64386、同一 Pane 的 XComponent 身份从 121 变为 208；原 SSH 会话
  持续存在，服务端快照精确为 `recovery17beforeAFTER24`，原内容搜索 `1/1`，正常退出
  为 0。保留内容和真实表面替换均已观察，不把函数调用成功当恢复通过。
- `policy,arkts,terminal-native` 及受影响构建工作流通过，202 项 ArkTS、7 项 Session、
  10 项原生控制器、12 组真实 VT 加实际 attach 方法测试，47 项告警无增量。工作流
  测试临时改写源码后，串行补验 `policy,arkts`，避免复用与源变换重叠的读取结果。
  最终签名包 SHA-256：`A5B12039005ABE148EEE4F2376C2AAD6EBD14DC0EE12619E4A27E354A5FD6935`。
- 自有夹具、凭据、信任、转发与测试 Tab 已清理，原有三个 Tab、第二 Tab 焦点和中文
  拼音已还原。证据位于 `build/verification/1.7-native-link-recovery/batch-evidence.json`。

- 链接：两种 uinput 修饰键维持方式均在正确网格到达 `mods=0`，没有生成链接激活。
  停止该注入路径；不能据此判断产品链接失败或第三方豁免。续批按 25 分钟、一次诊断
  修正准入补齐 SDK 测试入口并完成签名构建。首次包未包含自定义 runner，显式引用后
  再试仍仅得到测试框架 `App died`，没有注入或系统打开证据。停止该工具路线，模块级
  卸载已验证，生成目录删除、配置还原，期间没有创建 SSH 夹具状态。后续先采用已具
  运行资格的输入入口或直接物理 Ctrl+点击，不继续扩展临时 runner；需要证明普通 URL
  和 OSC 8 的系统打开。详见测试效率文档本日追加记录。
  09-19 维护者本轮不便实体点击，保持待验；本轮不再扩展自动组合点击工具。
- 恢复：已证明 Surface 重建后的 Mosh 连续性、受控 EGL context-loss 后的 SSH 连续性，
  以及持续初始化失败解除后用户显式重试恢复同一 SSH。正常生命周期仍按替换门核对；
  GPU 持续不可用时接受 App 无法使用，故障提示或重试按钮本身不算恢复成功。

- [x] 09-19 维护者明确收敛 GPU 故障范围：接受 GPU 持续不可用时整个 App 无法使用，
  保持单一 GPU renderer；取消 CPU 故障出口接入与后续诊断，删除新增后端、缓冲区管理、
  专用测试和验收标记，保留正常 Surface/context 生命周期的有界恢复。该决定取代本节
  历史批次中“持续 GPU 故障期间仍须可用”的要求；不免除普通使用中的恢复与资源责任。
  可行性探针和中止接入的原始证据分别留在本地 `build/research/native-cpu-window-20260919/`
  与 `build/verification/1.7-native-cpu-recovery/`，不再作为待办或替换门。
  中止前 CPU 缩放触发重建的原因尚未闭合，不将原脚本成功视作该能力通过。
  同步[产品原则 §4.8](project-principles.md#48-终端-renderer-选择)、设计和质量策略；
  后续为扩大兼容性而增加复杂度，按产品原则第三节先取得维护者明确确认。
  清理后 `policy,terminal-native` 定向检查通过，五个恢复文件与接入前备份逐字节一致。
  单一 GPU 签名开发包已重新部署，真机 `help` 输入、出字及返回提示符通过；原三个 Tab、
  第二 Tab 焦点和中文输入保留。包内无 CPU 兜底标记及 NativeBuffer 依赖，旧夹具凭据
  目录已清理。本批仅核验撤销后的主路径，复用既有正常恢复证据，不重跑故障矩阵。
  证据：`build/verification/1.7-native-cpu-recovery/removal-evidence.json`。

- [x] EGL 恢复分支整批验证（09-18）：执行真实 C++ draw/release 方法，覆盖 context
  loss、坏 Surface、未初始化 display、有限重试、耗尽后静默等待新 attachment 和资源
  释放。受影响软件检查及构建工作流通过，202 项 ArkTS、47 项告警基线不变；本批未改
  产品恢复算法。新增受控触发、真实方法测试及包隔离证据，不为等待自然重置重复休眠/强杀。
  - 仅验收构建的 `Ctrl+Alt+Shift+L` 单次注入 swap 边界的 context-loss 结果，再运行
    生产释放/重建/交换路径。真机 PID 32733 的日志顺序为 armed → context-lost →
    released → created → swap-ok；同一 Pane、XComponent 身份 121、原有内容和会话均
    保留，原内容搜索 `1/1`，服务端缓冲精确为 `egl17beforeAFTER0123456789`，正常退出 0。
    这是驱动返回值受控注入后的真实资源恢复，不是自然驱动重置或持续故障恢复证据。
  - 首次编译失败来自 PowerShell 注入包装函数遮蔽调用者 `$source`，修复专用变量名并
    补回调反例后构建成功；源码在成功/失败路径都还原。设备已有日志过滤器不含新增
    NativeTerminalController 标签，使用当前 PID 的定向日志取得原始标记，未重复触发。
  - 实际验收 ELF 包含故障标记，关闭注入后重新编译的 ARM64 ELF 不含标记；发布包
    策略反例同时覆盖 C++/ArkTS 标记。未构建正式发布候选。签名 HAP SHA-256：
    `BB343B211853A1C0B6177EC3EAAAB271DE2DF428C487027EE542F2943A3A5918`。
  - 证据：`build/verification/1.7-native-egl-recovery/batch-evidence.json`。临时 Host、
    信任、转发、服务器与凭据已清理，恢复原三个 Tab、第二 Tab 焦点和中文拼音。
- [x] 持续显示失败解除后的显式恢复（09-18）：原控制器在自动重建限流返回后没有用户
  恢复入口。增加故障状态中的本地化“重试显示”，复用 Pane 重建并保留 VT/Session；
  重复点击在新表面结果返回前不再准入，关闭或核心失败不能借此复活。没有后台重试循环。
  - 首轮真机中鼠标重试有效，已聚焦按钮的 Enter/Space 未激活，预 IME 快捷键可到达。
    真实 Pane 方法反例先失败；明确接入预 IME Enter/Space，保留 Tab 走焦及工作区快捷键。
    旧轮 SSH 正常退出、测试 Tab 关闭后采用新包，原失败证据保留。
  - 最终包 PID 61137、同一 Pane 72-4：首次和自动重建合计四次初始化拒绝后停止；
    Enter 手动重试恰好新增两次后再次停止。故障期间输入未到达 SSH。解除注入本身没有
    恢复表面；再次按 Enter 后出帧、焦点返回，XComponent 身份依次为 121 → 135 → 154 →
    172。原认证标记及故障期间服务端延迟输出各搜索 `1/1`；同一 SSH 精确收到
    `AFTER0123456789`，正常退出 0。持续故障由验收构建注入，不是自然驱动失效实测。
  - 受影响软件和工作流检查通过：202 项 ArkTS、47 项告警基线不变、12 项原生控制器/
    Pane 合同；原生 VT/attach/draw 检查同批通过，键盘修改后仅复验受影响 ArkTS。
    实际验收 ELF 含故障标记，关闭注入重新编译的 ARM64 ELF 不含标记，源码还原已核对。
    最终签名 HAP SHA-256：
    `4161F0B30EDCB2C1624BC5B60F80D5B569E5B73EE65D702363E4C4BE8D5DDCA9`。
  - 证据：`build/verification/1.7-native-display-retry/batch-evidence.json`。临时 Host、信任、
    转发、服务器与凭据已清理，恢复原三个 Tab、第二 Tab 焦点和中文拼音。完整生命周期
    仍按替换门核对；持续 GPU 不可用的要求已由 09-19 决策收敛。

- [x] 09-18 原生后台生命周期：由窗口/工作区所有者经现有 Surface owner 直接通知 VT
  worker，在隐藏时暂停绘制、闪烁与拖动滚动，继续消费输出和协议回复；返回重验 Surface，
  新显示代际成功送显后恢复输入。焦点与可见性独立，隐藏故障不抢前台重建焦点。
  - 首包证明了 Tab 路径，但最小化的组件 Prop 更新直到前台才传播：窗口 Watch 在
    02:14:13 已执行，原生 hidden 到 02:14:21 才提交。最后正确边界为 Index 窗口 Watch，
    首个错误边界为依赖 UI 重绘的属性链。实际 Index 方法反例先失败，再移除 Prop 链，
    从窗口 Watch、工作区同步和 controller 就绪直接通知，保持既有所有者而不增加抽象。
  - 最终同包真机重跑 Tab、最小化、系统挂起/唤醒/本地凭据自动解锁：同一 PID `13618`、
    Pane `74-4`、Surface `6764573493262` 与 SSH Session。三次隐藏 7 秒，worker 提交处
    成功送显计数分别保持 `156→156`、`294→294`、`449→449`；最小化 hidden 在窗口
    Watch 后约 6ms 提交。三条延迟输出和原认证历史均搜索 `1/1`，三次返回精确输入通过。
    双 Pane 中左侧 SSH 未聚焦仍实际显示延迟输出，右侧保持本地页面。
  - 真实 VT 的隐藏输出/回复、无绘制/闪烁、恢复、独立焦点和隐藏关闭合同通过；14 项
    实际 controller/Index/Pane 合同、202 项 ArkTS 与策略检查通过，47 项告警基线不变。
    验收计数仅为成功 swap/可见提交元数据；源还原、包策略拒绝及实际无探针 ARM64 ELF
    分离检查通过。不据此推断 CPU/PSS 改善，也不扩展成完整生命周期或替换验收。
    最终签名 HAP SHA-256：
    `F3958B5D28ED4BEE76B7FFADAA097CC3A5FA18210721509DAFC0503DBD12BADD`。
    证据：`build/verification/1.7-native-lifecycle/batch-evidence.json`。
  - 关闭临时右 Pane 后左侧输出搜索 `1/1`、精确输入通过，SSH 正常退出 0；临时 Host、
    信任、转发、fixture 进程和凭据目录已清理。恢复原三个 Tab、第二 Tab 焦点与中文拼音。

- [x] 09-18 同机启动/空闲资源批次：在已准入的 60–90 分钟观测修复范围内，补齐仅验收
  构建的 worker 消费序号和成功 swap 关联，复用冷/热启动入口；不把空帧、旧代际或无关
  输出算作提示符/首字回显。实际 Controller 反例、源码还原、包排除、`policy,arkts` 和
  构建工作流检查通过，未修改产品行为或引入依赖。
  - 同源码/PC/窗口/字体设置，Native/Web 各 20 次冷启动、20 次热返回，共 80 个有效
    样本。冷启动 P50/P95 从 Web `1579/1641ms` 到 Native `949/985ms`，约缩短 40%；
    热返回分别为 `487/509ms` 和 `458/493ms`，差距较小。首字输入往返没有明确改善，
    热返回 Native 中位数高 10ms，保留观察，不为微小差异新增调度特例。
  - 三个保留 Tab、一个可见本地 Pane，静置 35 秒后的三点含图形 PSS：Native
    `158.2–158.3MiB`、Web `379.4–381.2MiB`；映射 PSS 约 `80/209MiB`。进程分别为
    1/4 个；固定约 15.4 秒 CPU tick 增量为 0/172，不据此宣称零 CPU、续航或无泄漏。
  - 四组签名包、原始日志、截图与 306 文件源清单位于
    `build/verification/1.7-interaction-performance/`；详见
    [方法、数据与边界](native-terminal-performance-1.7.md)。已恢复不带启动探针的普通
    Native 签名包、原三个 Tab、第二 Tab 焦点、中文拼音。实际无探针 ARM64 ELF 分离检查通过。
  - 剩余性能验收集中于持续输入/输出完整性、负载 CPU/PSS 和关闭后的资源回收；本批
    不关闭完整交互、恢复或替换门。相关代码/证据未变时复用这 80 个启动样本。

- [x] 09-18 持续输出与资源观察批次：复用受控 SSH 的 `ltty-perf-prepare/run` 公共行序列。
  Web 已有全正文/顺序 oracle，Native 缺少消费后全正文校验及末包送显关联，准入仅诊断
  构建的最小观察器；不更改产品缓冲、背压或调度。记录只含 case/计数/耗时，不保留实际
  终端内容或凭据；成功/失败还原及发布包排除须通过。限 60–90 分钟完成离线反例及首轮
  资格验证；若可信观测仍未建立，重评方案，不增加测试框架。随后同 PC/包类型/窗口/
  输出夹具做 Native/Web 各三次 12000×80 行校验，另各一次 12000×160 行、每 16KiB
  节流 50ms 的约 6 秒公共输出，用于负载资源采样；关闭测试 Pane 后固定
  预算回收观察；另核对尾行搜索、后续精确输入和正常关闭。不把夹具节流吞吐当峰值，
  不把短期回落当无泄漏。自有 Host/信任/映射/fixture 与测试 Pane 在 finally 中清理。
  - 专用原生正文探针、15 项实际 Controller 合同、源码还原及包标记排除已通过；
    `policy,arkts`、构建工作流与两个签名诊断包通过。产品缓冲、背压、调度未改。
  - Native/Web 各四轮共 9,792,000 字节全部有序，八轮后续精确输入通过；原生末行、
    历史行搜索均 `1/1`，两条路径尾部截图正确，SSH 正常退出 0。脚本编号过长、
    夹具退出名、Web 节点身份/网格预期错误已分开记录，只补缺项，未重跑有效输出。
  - 关闭后 70 秒含图形 PSS：Native 261.1 MiB、Web 460.1 MiB；对应输出前为
    190.6/449.1 MiB。已完成固定预算观察，原生残留尚不能区分诊断分配、缓存与长期
    持有，不将本批提升为“完整回收”。负载 CPU 只记录不同长度区间的原始 tick。
  - Host、信任、反向映射、夹具进程与临时凭据已清理；普通 Native 包、三个 Tab、
    第二 Tab 焦点和中文拼音已恢复。证据在 `build/verification/1.7-native-output/`，
    [方法、结果及限制](native-terminal-performance-1.7.md#09-18-持续输出与关闭观察)。

- [x] 09-18 续批完成受控负载中真实键盘输入对照及普通包重复关闭观察。
  - Native/Web 各两轮、40 个负载中输入回显和 3,888,000 字节完整有序正文通过；
    输入接收至回显帧的 P50/P95 分别为 28/38ms、35.3/42.2ms，不包含此前 IME 派发。
    首轮派发时间超出负载窗口，只作资格证据；修正已测得的按键间隔后采用相同节奏。
  - 普通 Native 包三轮关闭静置后含图形 PSS 为 197.9/215.1/226.7 MiB，匿名分类持续
    增长、线程保持 52；未证明趋稳。空 Tab 对照的增量来自原生堆/图形，匿名分类基本
    不变。不能把两个现象合为同一原因，也不能将完整回收记为通过。
  - 系统拒绝 smaps 与临时 ARM64 ELF 执行，未改权限；隔离 VT 六轮没有运行。误传
    文件及临时运行库已清理，停止独立 ELF 路线。诊断工具反例、16 项 Native Controller
    合同、policy/arkts 和构建工作流通过。156 个产品源文件/配置未变，复用既有证据。
  - 证据、边界与清理在 `build/verification/1.7-native-memory-input/`，详见
    [本批记录](native-terminal-performance-1.7.md#09-18-续批负载中输入与普通包内存)。

- [x] 09-18 闭合重复输出/关闭后的大幅匿名增长：签名包三轮所有者计数均回基线，
  后续只读 slab 计数确认 SMP 保留映射；未读堆内容、未强制 GC，临时插桩全部还原。
  - 修正 VT 的 libc 声明，使用上游默认系统 C allocator；ARM64 ReleaseFast 运行库
    由最终 SDK 链接负责。没有 allocator 调参、自定义桥或 Zig ABI 补丁。
  - 普通修正版三轮关闭静置后匿名 PSS 为 3267/3270/3290 KiB，总 PSS 为
    173.5/175.5/174.9 MiB，同一进程且线程保持 52。修正前相同三轮匿名分类为
    29567/44126/57941 KiB；原大幅增长未复现。不外推为长期零增长或所有内存归零。
  - 严格 ARM64 链接、原生宿主合同、policy 通过；定向正文 1,944,000 字节完整有序，
    20 次真实按键均在负载中回显，P50/P95 29/36ms。首次临时用例名未触发约定慢速
    发送，记录为资格错误，清空输入后只重跑该项；产品源码未为此修改。
  - 采用修正；回滚时撤回 `build-terminal-native.ps1` 的 libc/SDK 运行库声明，并从
    固定归档重新解压生成的 VT 源码后重建，避免复用已叠加的缓存构建文件。
    旧启动/输入性能数据只作修改前历史证据，不标成修正版精确指标；完整交互/恢复
    替换门仍开放。诊断、失败边界和普通包对照见
    [资源诊断与修正记录](native-terminal-performance-1.7.md#09-18-资源所有者诊断与-libc-构建修正)，
    本地证据 `build/verification/1.7-native-libc/`。
  - 自有会话、Host、信任、转发、夹具和凭据已清理；恢复普通修正版、原三个 Tab、
    第二 Tab 焦点和中文拼音。306 个源码/配置哈希一致，未插桩 ARM64 库重建通过。

- [x] 09-18 构建配置修复（维护者已授权六项审查建议）：将 VT 固定下载输入移出 Clean
  输出目录并纳入正式预热；校验/恢复生成源码和工具链，按当前配置重建叠加；VT 独立
  入口使用已有可重入构建锁；统一 SDK 解析；Rust 缓存纳入构建参数/脚本和工具链身份。
  Unicode 运行时依赖单独传递优化选项，保留主机表生成器 Debug 策略。只运行对应工具
  反例、原生宿主合同、ARM64 严格链接和签名包定向 Unicode/正文/输入真机验证。
  用独立可丢弃目录证明 Clean 后离线输入可用，不删除当前 build 内历史证据、不运行
  正式发布矩阵。涉及 SDK 路径变化须验证 WSL 链接器能够处理空格路径；若失败，先
  定位实际路径/参数边界，不引入新的机器专用默认目录或 ABI 补丁。
  - 固定归档保存在 `.cache/native-terminal`，正式预热接入 `-PrepareOnly`；展开的
    Ghostty/Zig 文件逐次按归档及当前叠加恢复，未变文件保留时间戳。缓存检查约 8 秒。
  - 构建工作流及输入反例通过，独立 VT 进程实际等待同一构建锁；ARM64 编译命令确认
    uucode 运行模块为 ReleaseFast、主机表生成器为 Debug，宿主合同和严格链接通过。
    Rust 使用含空格的 DevEco SDK 路径重建成功，第二次构建跳过且哈希一致。
  - 真机 Unicode 样例显示正确；1,944,000 字节正文有序且零不匹配，20 次物理按键均
    在负载中回显，P50/P95/最大 25/37/38ms。恢复普通签名包及原三个 Tab、第二 Tab
    焦点、中文输入；自有 SSH、Host、信任、映射、夹具和凭据已清理。
    详见 `docs/native-terminal-performance-1.7.md` 及本地
    `build/verification/1.7-build-config-fix/`；本批不代表完整替换或正式发布验收。

- [x] 修复跨 Pane 的 Host 配置刷新：本批在测试 Pane 新增 `native17cfg` 后，原第二
  Pane 执行删除报告 Host not found，而持久化配置仍含该 Host；新 Pane 加载后删除
  成功。证据见本批 `cleanup-host-result.jpg`、`device-cleanup.json`。后续沿配置状态
  所有者检查跨 Pane 可见性及写入一致性；本次构建修复未改产品 Host 实现。
  - 09-18 继续执行：预期任一 Pane 成功修改后，其余已打开 Pane 的查询、补全、连接
    解析及后续保存使用同一已提交配置。最后正确边界为 DurableStateManager 完成持久化；
    首个错误边界为旧 CommandBarViewModel 仍持有初始化副本。假设：两个真实命令模型
    交替新增/更新会复现漏读和覆盖。采用应用内共享 SshConfig 所有者，保留独立命令行/
    历史；保存失败必须撤回内存修改，异步导入授权后按当时最新配置合并。
    范围为这条配置状态链：先宿主反例，再 ArkTS 构建和双 Tab 真机新增、补全、查询、
    更新、删除及重启持久化；不触发真实 SSH、密钥或无关设备矩阵。
  - 23:30 取证路径重审：两轮在提交成功后取得缺少应用窗口的布局，下一次只读捕获又
    看到同一 PID/Pane 的原焦点；均已核实配置原样恢复。目标是配置一致性，不是瞬时
    布局可靠性。临时场景改用已有 `Get-LeanTTYDeviceLayout` 的两次空布局捕获预算，
    保留唯一焦点及 Pane 身份断言，不再更换产品实现或增加重试预算；若仍中断则保留
    实际证据和未完成项，不继续全场景重跑。
  - 23:34 闭合：应用内共享配置，保存失败恢复已提交内存状态；实际命令模型的三组
    回归先失败后通过，覆盖交替写入、SSH/Mosh/传输解析、失败回滚及导入导出授权等待。
    `policy,arkts` 14 项检查通过，202 项 ArkTS 单测通过；47 项既有警告仅发生行号
    位移，逆向还原后指纹完全一致。新增测试已进入 `arkts` 组。
  - 同一签名 ARM64 开发包完成两个既有 Tab 的新增、物理 Tab 补全、交替保存、更新
    查询、删除可见性及强制停止后重启读取；逐项截图复核通过。临时 Host 全部移除，
    完整配置 SHA-256 恢复测试前值；保留原三个 Tab、第二 Tab 焦点和中文输入。
    未建立远端连接。前三轮中断及清理证据保留，最终轮完整通过，不覆盖原失败。
    本地证据：`build/verification/1.7-shared-host-config/batch-evidence.json`；签名 HAP
    SHA-256 为 `8505CC3153C3868C08867DEF471F1A55CA131024B515089B11B7A5FF386C1EDD`。

- [x] 原生替换既有需求迁移审计：首轮源码与既有证据对照（09-19 维护者新增）：以已经交付的
  xterm/Web 行为、设计决策、历史修复和验收证据为基线，逐项对照当前原生路径，找出
  遗漏、退化及仅有实现却缺少验证的需求；不以“常用终端能运行”代替完整迁移。
  明确包含视觉等边 Padding、字符网格余量分配，以及围绕该 Padding 设计的窗口缩放/
  resize 补偿；沿布局 → 有效终端尺寸 → 行列数 → SSH/Mosh PTY 尺寸核对，连同字号、
  DPI、双 Pane、鼠标命中、选区和 IME 候选定位的坐标一致性取证。继续覆盖已交付的
  输入/快捷键、复制粘贴、搜索/历史、样式/透明度、标题/链接/OSC、安全策略、焦点和
  生命周期等契约。输出“原需求与证据 → 原生实现位置 → 已迁移/缺失/退化/待验证”清单，
  区分用户体验要求与已经不适用于原生的 Web 补偿手段；不能因后端变化默默删掉要求，
  也不机械复制旧实现。缺口按风险并入此开发清单，在删除旧 renderer/关闭替换门前处理。
  首轮源码与既有证据对照已完成，见[迁移审计](native-terminal-migration-1.7.md)。确认 Padding、
  旧度量吸附、滚动条、链接视觉提示及全部搜索匹配装饰存在缺口；历史容量和 DPI 保持待证。

- [x] 原生替换既有功能与 UI/交互体验遗漏复查：本轮离线对照（09-19 维护者要求重新加入）：
  在首轮审计基础上，系统对照此前 xterm/Web 已交付的功能、界面与交互，继续查找遗漏和
  退化；首轮审计完成及已知缺口修复，不代表迁移已经完整。复用已有结论和有效证据，
  重点补查首轮只核对实现存在、未核对实际体验的部分，不只检查已被指出的例子。
  **已知例子：** 此前的 Padding/视觉留白遗漏沿下方几何项继续闭合；本次维护者反馈
  原生搜索框 UI 相比旧版明显变丑，纳入明确的视觉退化问题。对照旧版搜索框的布局、
  尺寸、间距、对齐、字体、颜色、边框，以及输入框、计数、导航和关闭控件的组织与状态；
  同时核对焦点、键盘操作和不同窗口/分屏下的表现。搜索能用、匹配高亮或滚动概览通过，
  均不能代替搜索框 UI 的迁移质量；具体差异待对照取证，不在此预判实现原因。
  **完成条件：** 更新既有[迁移审计](native-terminal-migration-1.7.md)，列明旧版基线、
  原生表现、遗漏/退化与证据，给出补齐建议，并将需处理项归入本清单。搜索框须有可供
  维护者评审的新旧界面对照；保留既有体验要求，允许使用更简单的原生实现，不机械复制
  Web 补偿逻辑，不借复查新增功能或兼容后端。首次只登记；09-19 随后授权执行离线审查。
  - 已在[迁移审计](native-terminal-migration-1.7.md#2026-09-19-充电期间离线复查)记录新旧真实
    截图、控件合同、源码差异和剩余验收。确认搜索条密度/分组/提示遗漏、新增 256 字符限制，
    并以当前生产运行时的宿主最小诊断复现空查询/清空后切屏缺少关闭通知。
    证据：`build/verification/1.7-offline-migration-audit/`；没有操作测试机或修改产品实现。
    本项只关闭本轮审查交付，修复、同条件 UI 评审及完整替换仍由下列未完成项承担。

- [x] 恢复原生搜索条的既有视觉与提示体验：沿现有 Surface 搜索所有者，恢复紧凑密度、
  导航共边框/独立关闭、键位 Tips、可见 hover/focus/disabled，以及窄 Pane 可收缩布局。
  以旧版设计和本地真实截图为基线，不机械换算 CSS px/vp 或复制 Web 媒体查询；先做局部
  实现和受影响键盘合同，测试机回来后做新旧界面、空/有/无结果、中英文与单/双 Pane 对照。
  不改变 PTY 网格、不新增搜索状态所有者，未完成视觉评审不能由匹配高亮通过代替。
  - 同批真机发现搜索 Next 已获焦点，但 Enter 后结果仍为 1/13；最后正确边界为按钮
    焦点，首个缺口为 Pane 仅显式处理输入框 Enter，按钮交给系统默认激活。沿已验证
    的显示重试按钮模式，在搜索所有者显式消费 Enter/Space 两阶段，只激活一次；
    先真实 Pane 方法反例，再同包验证导航和独立关闭，不扩展全局按键分派。
- [x] 核对并收敛搜索查询长度差异：原生 UI 限制 256 字符，VT 接口限制 4096 字节，旧
  Web input 没有 maxlength；本轮未找到容量缩减确认。先用长文本/Unicode 样本明确实际
  边界与反馈，给出保持旧能力的最小方案；不直接去掉限制使超长查询进入终端通用失败路径，
  若需保留更小的已发布能力边界，先提交取舍确认。
  - 09-19 本批实测 4096 ASCII / 1365 中文 / 1024 emoji 可进入 VT，超过 4096 字节
    被拒绝；控制器边界反例确认直接移除 UI 限制会进入终端通用失败。维护者明确确认
    “采用 1 MiB 上限和明确提示”；据此统一 UI/控制器/N-API/VT 的 UTF-8 容量，超限
    清除旧匹配并提示，输出占用队列时提示重试，不复制历史或新增搜索队列。
    首个容量包打开空搜索时退出：系统 JS_ERROR 精确指向空字符串 encodeInto 返回
    undefined 后读取 byteLength。零字节查询不需要编码，直接按长度零处理；宿主桩同步
    采用已观测的平台返回，先失败后修复。只重验本次搜索链，不加入编码兼容后端。
- [x] 修复搜索打开状态与 VT 切屏的生命周期缺口：预期空/非空/清空查询都在主/备用屏
  切换时关闭面板；最后正确边界是 VT 活动屏已改变，首个错误边界是 `refreshSearch`
  用 `search_ != nullptr` 决定是否通知 UI。当前宿主探针分别观察到 0/1/0 次关闭通知。
  搜索面板状态归 Surface，活动屏归 VT；下一最小验证沿真实运行时和控制器证明切屏事件
  不依赖匹配对象，再定向真机核对关闭、焦点和查询不进入远端。不建第二套搜索状态机。
  - 09-19 测试机充电后本批执行：同时处理搜索条密度/控件样式和切屏事件链；VT 在屏幕
    变化时始终通知当前搜索代次，Surface 在首次打开空搜索时登记代次，重开保留原查询。
    先运行真实运行时/控制器反例，再做定向构建与同包 UI、焦点及切屏检查；查询长度
    差异另行测量，不将移除 256 上限直接接到通用失败路径。预计一批约 45 分钟，
    不重跑协议矩阵或扩展实体修饰键注入工具。
  - 本批搜索行为已闭合：22 项控制器合同、29 组真实运行时合同通过。1 MiB ASCII/emoji
    边界在真实 VT 验证；真机 300 字中文不再截断，1 MiB + 1 ASCII 显示中英文局部提示，
    关闭后同进程仍能精确输入并执行本地命令。平台 TextInput 默认只留 1,000,000 字符，
    因而显式允许合同上限加一个字符，让超限提示可见；不把无上限输入交给通用故障路径。
    临时编译触发器经生产 VT 输出切换主/备用屏，非空、空、清空查询均关闭并恢复焦点；
    双向和远端输入隔离由受影响宿主合同覆盖，不冒充实体远端操作矩阵。
  - 搜索条实现及定向真机检查已完成：344×34 vp、导航分组、独立关闭、hover/focus/disabled
    和键位提示；Next 按 Enter 从 1/13 变为 2/13，关闭按钮键盘激活通过。594 px 窄 Pane
    内面板收缩至 370 px，输入和按钮仍可见，未改变 PTY/Padding。旧/新截图日期、窗口
    不同，保留来源和比较边界；维护者随后明确回复“接受当前外观”，视觉项闭合。
    对照：`build/verification/1.7-search-parity/search-ui-review.md`；本批记录见同目录
    `batch-evidence.json`。只运行受影响合同和普通包冒烟，不重跑完整发布矩阵。

- [x] 核对并恢复原生光标的既有体验（09-19 维护者新增）：维护者反馈当前看到空心方块，
  不符合此前 xterm 阶段专门优化过的形状，作为 Padding、搜索框之后的又一迁移遗漏例子。
  旧版 `terminal.html` 明确设置 `cursorStyle: 'bar'`、`cursorBlink: true`；
  [会话状态隔离设计](design/terminal-session-state-isolation.md)还要求 Session 结束后恢复
  LeanTTY 默认的可见闪烁竖线，同时尊重活跃远端应用通过标准序列指定的光标。
  先区分活动 Pane、本地提示符、失焦 Pane/窗口、搜索打开/关闭，以及 SSH/Mosh 返回后的
  实际状态；当前原生 renderer 在 `!focused` 时强制空心方块，这只是定位线索，尚未证明
  本次现象由失焦、焦点状态错误、VT 初始默认值或会话复位遗漏中的哪一项造成。
  **完成条件：** 对照旧实现及已有优化证据，明确并恢复默认形状、粗细、闪烁、失焦表现和
  会话边界语义，以对应状态的真机截图/交互验证；不能只证明几种光标都能画出来就算迁移
  完成，也不能把所有远端光标强制改成竖线或增加光标设置。本次先记录，不直接修改实现。
  - 继续执行：确认原生 `create()` 未设置 Ghostty 默认光标，实际继承不闪烁方块；
    竖线按 cellWidth/6、空心边缘按 1 物理像素绘制，也未迁移旧 xterm 的 1 CSS px
    （对应 1 vp）粗细。旧随包 WebGL 的闪烁间隔为 600 ms，原生为 500 ms。
    最后正确边界是旧配置/平台 density，首个缺口为 VT 初始化和 renderer 尺寸策略；
    VT worker 负责默认/远端状态，UIContext 负责物理像素换算，renderer 只画当前状态。
    使用固定上游已有 default-cursor API、实测 1 vp 和既有计时循环修复，不增加设置、
    Shell 特判或后备路径。先运行初始/默认 reset/远端覆盖/临时页面的反例，随后检查
    普通包活动/失焦、搜索返回、实际闪烁和受控 SSH 退出；不重跑完整协议矩阵。
  - 已通过反例→修复、22 个控制器合同、30 组 runtime 场景、既有注入/精确还原和
    ARM64 普通包构建。包 `1.7-cursor-parity/entry-signed.hap` 已安装，SHA256
    `583859241525F71CB2784EFF4EFD0C16BF8942245AC03C26F5514527FA76471D`，PID 54719。
    初始/搜索返回的真机帧同时观察到亮灭，竖线 2×40 px；搜索失焦为空心 18×40 px、
    2 px 边框。受控 SSH 的标准隐藏光标/备用屏幕有效，正常退出后恢复闪烁竖线。
    默认 reset、RIS、六种远端形状/闪烁和 Mosh 临时页为真实 VT 合同；未重跑完整 Mosh
    矩阵，600 ms 间隔由旧包代码与同一计时循环对齐，多帧仅证明实际亮灭、不宣称精确
    周期测量。原双 Pane/右侧焦点、窗口与中文拼音恢复，临时信任、fixture 和映射已清理。
    一次自动锁屏和两次调用错脚本参数的无操作样本不计通过；按既有解锁与核对后接口完成。
    证据：`build/verification/1.7-cursor-parity/`。

- [x] 迁移几何合同：恢复四边 8 vp 最小视觉留白、余量居中，并让绘制、鼠标、选区、链接、
  IME、SSH/Mosh 行列与浮窗整格吸附使用实际原生度量。沿既有所有者实现，不新增兼容后端。
  预期：缩放后留白对称，点击/候选位置和 PTY 网格一致；最后正确边界是 Surface 物理尺寸，
  首个错误边界是 renderer 按无留白尺寸计算行列并从零原点绘制。几何由 renderer 负责，
  窗口位置由 EntryAbility 负责；先以实际几何及带偏移输入反例验证该假设，再做定向真机。
  - 09-19 第一批已补齐原生留白和坐标：`TerminalGrid` 用实测物理单元格计算最大整格及
    对称余量；绘制/光标、选择/链接/鼠标共享原点。另以真实 VT 反例修复重新居中后
    鼠标静止时的 wheel 坐标，未增加新后端或平台依赖。原生合同和验收源码注入/还原通过。
  - 同一签名包真机完成普通窗口、最大化、字号增减、三次本地 `help` 精确输入/回到提示符，
    中文预编辑及候选定位；原三个 Tab、第二 Tab 焦点、字体、窗口与中文输入均已恢复。
    证据：`build/verification/1.7-native-geometry/batch-evidence.json`。未新建远端会话。
  - 09-19 第二批已补齐实测窗口吸附：用原生 attach 回传的实测 cell/inset；度量保留在
    当前 Pane 的 Surface 所有者，拒绝过期字体/DPI/Surface，不新增全局同步缓存。
    保留既有窗口对边固定与分割比例；双 Pane 各自居中，不强行调整分割比例追求同时零余量。
    原生合同、policy 和 ARM64 debug 构建通过，同一签名包真机验证左/上拖边的对边固定。
    SSH 实际收到单 Pane 153×46、较大字号 137×42、分屏 75×46；双 Pane 放大字号后
    左 SSH 为 68×42、右 Mosh 真实 PTY 为 67×42，最大化后分别为 85×46 和 84×46，
    恢复后均为 75×46。列差来自右 Pane 既有分隔间距，不调整分割比例。
    证据：`build/verification/1.7-native-window-grid/batch-evidence.json`。
    几何项仅剩系统 DPI 切换与完整指针/IME 组合的定向实机证据，未宣称完整替换门通过。
  - 09-19 第三批执行：先在当前同包、同一 PC 核对系统 DPI 切换的 Surface/字体变化，
    再验证双 Pane 的选择与中文预编辑位置；通过已有设置和输入入口操作并恢复原状态。
    预计一批 30–45 分钟；先取证，不提前加配置监听或兼容分支。若系统切换入口不可用，
    保留该项待验并推进其他迁移项，不为测试增加产品后门或扩大系统支持范围。
  - 第三批已取证：系统设置将 density 从 1.9 切至 2.2125 再恢复，同一 PID/Pane 保留，
    字体与网格重排；两个密度下右 Pane 双击分别准确选中 `help`/`mosh`。较大密度下
    右 Pane 候选贴近光标、左 Pane 底部候选向上避让；恢复后 `help mosh` 输入正常。
    不外推为“DPI 变化但没有 Surface 通知”分支通过，不新增配置监听。首次仍在预编辑
    的选区操作及左侧落在空白的操作不计通过。矩形/跨边缘组合仍归后续定向验收。
    证据：`build/verification/1.7-native-dpi-input/batch-evidence.json`。本批发现的 Escape
    输入归属问题独立修复如下，未以候选位置正常替代输入正确性。
- [x] 补齐滚动条的可见位置、拖动入口及右侧命中范围；复用当前 VT viewport，不复制历史。
  - 09-19 执行：沿 VT worker 的 scrollbar/绝对行滚动接口和现有 GLES 绘制实现；
    共用物理几何，右侧 8 vp 命中、居中 2 vp 滑块，轨道点击翻页，拖动按当前历史范围
    定位。没有历史或处于备用屏时隐藏；失焦、隐藏、换页及 Surface 变化取消拖动。
    不增加 ArkTS 历史副本、事件同步或后台轮询。验证真实 VT 的端点/追加输出/备用屏/
    鼠标协议隔离及失焦，再构建并做同包真机滚动、拖动、输入回底和双 Pane 冒烟。
  - 首包真实 VT/ARM64 构建通过，真机已显示滑块；浮窗右侧实际落入系统 resize 热区：
    底角操作缩小了窗口，中段拖动无视口变化；恢复原窗口后，最大化的同一包拖动到顶
    成功。临时脚本最初另有 Action 参数遮蔽，已修正并保留无效证据，不计产品通过。
    在再次尝试前重审：目标是浮窗和最大化均可用，不能以最大化通过替代浮窗；不新增
    平台窗口适配。最初提出额外预留边缘空间；09-19 维护者明确沿用 xterm 的占位，
    尽量四边视觉等宽，并保留最大内容空间。核对旧实现：左/上/下为 8 px，右 Padding
    为 0，由 8 px 滚动条补齐，整格余量居中。原生已有对应 8 vp 总留白；不在右侧
    叠加 Padding，不扩大四边 inset，不减少可容纳的完整行列。该布局决策已确认，
    不再等待额外空间审批；边缘命中问题仍需在此占位内核对事件归属，不能直接扩大留白。
  - 最大化单 Pane 的顶部/底部拖动、轨道翻页及普通输入回底已取证；左 Pane 的右缘
    又命中 splitter，完整双 Pane 拖动不计通过。两处边缘均需从布局所有者处理，
    不以 VT 合同通过替代真机可用性。测试 Pane 已关闭，原三个 Tab、第二 Tab 焦点、中文
    输入与原浮窗均已恢复。记录 `build/verification/1.7-native-scrollbar/batch-evidence.json`。
  - 本批只读定位：`Index.ets` 的中央 splitter 宽 14 vp、向左平移 7 vp，且 zIndex=2；
    因而覆盖左 Pane 8 vp 滚动区域中的 7 vp，包括居中的滑块。冲突发生在 App Shell
    命中分配，不能通过修改 VT 拖动算法修复。API 24 的 `setResizeByDragEnabled`
    只能整体开关窗口拖拽缩放，不能据此宣称能调整边缘热区；本批不禁用窗口缩放，
    不把缩窄 splitter 命中或挤占文字区当作已经接受的无代价修复。两处问题仍待闭合。
  - 当前批：先在当前普通包测浮窗 gutter 的内侧命中，观察真实滑块和窗口边界；
    以现有布局分配中央鼠标热区的方案提交维护者确认。范围限当前 8 vp 留白和 App
    Shell 命中，不关闭系统 resize、不增加列损失或设备特殊分支。预计一批 30–45 分钟；
    若现有空间内不能可靠分配，先记录测量与取舍，不继续叠加事件转发 workaround。
    维护者本轮明确确认“采用右侧 12 vp 热区”。当前包浮窗对照：距 Pane 右缘 13 px
    起拖可以回到历史顶部，窗口仍为原边界；原居中滑块距右缘约 8 px。因此只把可见
    2 vp 滑块及搜索标记移到已有 gutter 内侧，保留全部命中几何/行列数；中央以系统
    mouseResponseRegion 分配右侧既有空白。定向验证两 Pane、浮窗/最大化、分屏拖动
    和窗口边缘缩放，不新增事件转发或窗口模式分支。
  - 本批闭合命中：滑块和概览标记在原 8 vp gutter 内靠内侧绘制；原生命中仍为 8 vp，
    最外缘继续服从系统窗口热区。中央仅新增系统鼠标热区属性，视觉线和 Pane 布局不变。
    同一普通签名包、PID 36773，浮窗/最大化及左右 Pane 均可从可见滑块拖到顶/底，
    各次拖动前后窗口和 Pane 边界一致；轨道翻页使滑块顶部 1028→138 px。
    中央右侧 6 vp 和 10 vp 起拖均能改变分割位置；外窗右边缘实际 2911→2947 px，
    再恢复 2911，证明原窗口 resize 仍可用。搜索 ssh 为 1/26，内侧标记可见；
    关闭搜索后 `help mosh` 精确输入/单次提交通过。18 项控制器合同、ARM64 构建和
    policy/diff 通过；VT/网格没有改变，复用上一批真实运行时合同，不重跑协议矩阵。
    原有两个临时 Pane及后续清理 Pane 已关闭，原三个 Tab、第二 Tab 焦点、中文拼音
    和浮窗恢复；测试分割比例回到视觉中点附近，仍有 3 个物理像素的舍入差异，行列数
    不变。小幅拖动受手势阈值限制，不为精确归零增加产品入口、脚本驱动或继续试探。
    首次工具假设分隔条标签会出现在布局中，实际没有；尚未拖动即停止，随后改由当前
    两个具名 Pane 边界定位。原样本保留，未因此改产品。证据：
    `build/verification/1.7-scrollbar-hit-ownership/batch-evidence.json`。
- [x] 修复原生终端中文候选期间 Escape 被提前消费：09-19 DPI 场景中，候选位置正确，
  但注入 Escape 后 `nihao` 候选仍在；切换英文后消失。最后正确边界为系统候选已显示，
  首个错误边界为 `NativeTerminalPane.handleNativeKey` 在 IME 前直接提交 Escape 并返回
  true。假设：把普通 Escape 留给 IME，仅处理其未消费返回，可取消候选且保留终端 ESC。
  沿现有前后阶段分派修复，不新增 composition 状态；先真实 Pane 方法反例，再定向
  构建和同包真机候选取消、普通 ESC 及本地输入检查，不重跑无关协议或完整矩阵。
  - 真实 Pane 方法反例先失败后通过；17 项控制器合同、policy 和 ARM64 签名构建通过。
    真机 `nihao` 候选经 Escape 消失，无候选时终端补全由 active=true 变为 false，
    日志确认为 ESC 动作；普通 `help mosh` 输入通过。沿用现有 IME 后回调，未添加状态。
    临时 Tab 已关闭，原三个 Tab、第二 Tab 焦点、中文输入、density 1.9 和浮窗
    `[127,7][2911,1953]` 均已恢复；不触碰剪贴板，未新建远端连接。
    证据：`build/verification/1.7-native-ime-escape/batch-evidence.json`。
- [x] 链接修饰键 hover：下划线/指针/目标预览已实现；09-19 后续维护者按体验说明确认
  URL/Ctrl 点击正常，开发期实体交互缺口随上方人工反馈闭合。以下保留历史自动化边界，
  不把维护者反馈改写为代理亲眼观察的手形或浏览器证据，不再扩展失败的自动注入工具。
  - 链接提示链已核对：旧实现同时响应鼠标 hover/leave 和修饰键按下/松开；只在
    mouse-move 时计算 Ctrl 会遗留提示。原生 `handleNativeKey` 目前提前排除修饰键，
    `linkAt` 仅返回 URL，没有绘制范围与 hover 清理事件。后续应由 VT worker 统一
    URL/OSC 8 命中与修饰键门槛，沿已有输入所有权回传预览；覆盖松键、离开 Pane、
    输出/滚动/resize、失焦和搜索打开时清除，不改变链接打开或 PTY 鼠标协议。
  - 当前批执行：VT worker 派生当前链接的可见格范围及目标；复用 URL/OSC 8 命中和
    Ctrl（鼠标报告时 Ctrl+Shift）规则，GLES 下划线、ArkUI 目标预览和标准指针同步。
    增加修饰键及离开事件，不新增正文副本或系统打开入口；以真实 VT/控制器合同覆盖
    静止鼠标松键、输出/滚动/搜索/失焦及旧输入所有者。签名构建后验证可自动观察的
    界面路径；实体修饰键仍沿已记录待验项，不扩展失败的注入工具。
    首轮控制器测试按 accepted[0] 消费输出，新增 hover 清理通知使该位置不再代表写入；
    产品输出所有者未变，将测试改为按 write 类型定位，仍断言输出字节完全消费。
    真机修饰键自动注入已有平台限制，按原则 §4.6 使用本批编译期开关保护的短期触发：
    固定公开 URL/OSC 8 数据、实测网格坐标进入生产 pointer/hover，不发起系统打开。
    原源码在构建 finally 恢复，证据保存注入副本；只证明实际 GPU/UI 提示和清理，
    不替代实体修饰键与点击验收。不新增持久验收菜单或输入驱动。
    首包在交互前/新建 Tab 时退出，系统记录 JS_ERROR，尚未运行链接场景。最后正确
    边界为初始 Surface 已显示，首个错误候选为新增 `getModifierKeyState(['Logo'])`；
    本机 SDK `component/common.d.ts` 明确仅接受 Ctrl/Alt/Shift，其他参数抛 401。
    修饰键状态归平台事件，删除不合法查询，不增加替代状态或兼容分支；测试桩同步
    拒绝未知参数，先复现该回调失败，再修复和重建。首包行为证据不计通过。
    修复包正常建 Tab，URL/OSC 8 预览文本和松键/搜索清理通过。布局显示预览位于
    Stack 默认右上角，`Text.align` 只对齐内部内容；期望沿用旧预览的左下位置。
    按 SDK 已支持的 position Edges 明确 left/bottom，不增容器状态；只补验位置与
    关闭清理，复用未改动 VT 的目标、绘制范围和输入合同。
  - 已完成原生目标/范围、静止鼠标修饰键、鼠标报告覆盖及状态清理合同；18 项控制器
    合同、显示恢复、policy 与 ARM64 签名构建通过。SDK 非法参数反例已先失败后通过。
    测试包真机显示 URL 与 OSC 8 各自下划线及正确目标，释放修饰状态和打开搜索后
    预览节点、下划线像素均归零。最终定位为左下角，未改变终端网格或 Padding。
    手形 API 已接入；截图没有物理指针形状证据，不将触发式显示验证算作实体键鼠通过。
    已换回无本批 F9–F12 入口的普通开发包，`help mosh` 输入冒烟通过，三轮临时 Tab
    全部关闭，原三个 Tab、第二 Tab 焦点、中文拼音与原浮窗恢复；未操作剪贴板或远端。
    证据：`build/verification/1.7-native-link-hover/batch-evidence.json`。保留首包错误和
    修复包边界，未运行完整发布矩阵。后续沿既有实体操作待验项，不重新试验注入驱动。
- [x] 补齐原生搜索的全部可见匹配装饰、当前项边框和滚动概览标记。
  - 09-19 本批补齐搜索概览：原搜索所有者在结果完成时提取并去重匹配行号，绘制只
    读取该派生结果，在现有右侧 8 vp 区域画位置标记。输出/resize/查询替换时失效，
    搜索完成后更新；关闭/换屏/临时页面切换时清空。不在每次光标闪烁时复制全量匹配，
    不存终端正文、不另建搜索状态所有者。先真实 VT 的离屏匹配/去重/失效合同，再同包
    真机检查概览、导航及关闭；滚动条占位和点击语义保持不变。
    复查发现短页面保留轨道后，`TerminalScrollbar::fit` 在 range=0 时仍计算滑块比例；
    期望所有几何有限，首个错误边界是 thumbY 的除零，所有者为共享 scrollbar 几何。
    以无历史页面的有限值断言验证；range=0 时直接置于轨道顶部，不改变命中或绘制条件。
  - 09-19 本批先闭合搜索可见匹配显示：上游搜索已拥有结果，最后正确边界是匹配计数和
    当前 selection，首个缺失边界是 renderer 只画 selection。直接在同一 VT worker
    的 paint 调用借用搜索结果，生成仅当帧可见网格的颜色标记；所有匹配使用旧配色，
    当前项加蓝色边框。验证 Unicode/折行/视口裁切、滚动、查询替换、关闭和屏幕切换，
    再做定向 ARM64 构建与真机搜索/导航/关闭。当时链接提示和滚动概览单独保留，
    不复制历史、不另建搜索所有者，不用扩大 Padding 解决边缘命中。
  - 可见搜索装饰已完成：真实 VT 合同覆盖全部/当前项、查询替换、Unicode 折行、
    视口裁切与重排，worker 合同覆盖追加输出和备用屏清理。17 项控制器合同通过；
    显示恢复测试三处新参数漏传已修正，仅重编译该失败项通过，未重跑固定依赖准备。
    ARM64 签名包真机显示 ssh 13 处、key 29 处高亮，Enter 1/13→2/13 后边框移动；
    无结果、查询清空和 Escape 关闭均清理高亮，关闭后 help mosh 输入正常。追加输出后
    Shift+Enter 1/16→16/16 跨历史视口，装饰跟随实际文字；不以此替代概览标记或链接验收。
    证据：`build/verification/1.7-native-search-highlights/batch-evidence.json`。
  - 概览真实 VT 合同及 17 项控制器合同通过，ARM64 签名首包真机检查 `help` 与
    `help mosh` 跨屏历史：Shift+Enter 从 1/16 跳到 16/16，正文视口变化而右侧
    标记不动；同一列像素集合一致。清空查询和 Escape 关闭后，标记与正文高亮归零。
    四边留白、列数和滚动条命中未改变；没有加入第二份正文或新的搜索所有者。
    无历史页面的 thumbY 有限值反例先失败，补零范围判断后原生运行时合同全部通过；
    只重编译该受影响测试，并重建最终包补验短页面，不重跑无关矩阵。
    最终包短页 `help mosh` 搜索 ssh 为 1/3，三个右侧标记可见，Escape 清理并回到
    终端焦点；长历史的 range>0 路径未变，复用首包导航/清空证据。两轮临时 Tab 已
    关闭，原三个 Tab、第二 Tab 焦点、中文拼音及浮窗均已恢复。源码哈希与最终包
    构建快照一致。证据：`build/verification/1.7-native-search-overview/batch-evidence.json`。
- [x] 对照既有历史合同查明 8 MiB 预算的宽行、Unicode/样式缺口；经维护者确认，改为
  上游约 10,000 行语义并完成定向容量、搜索、隔离与 ARM64 内存观察。
  - 09-19 已完成固定 VT 宿主容量测量：46 行视口、10,046 条不折行记录，153/256/512
    列 ASCII 和 CJK 历史仅保留 5,745/3,449/1,720 行；逐格 TrueColor 压力样本仅
    881/537/446 行，VT 无处理错误。80 列纯文本为 10,001 行。确认字节预算不等价，
    未修改产品容量。固定上游已有 `SCROLLBACK_MAX_LINES`，无需自行复制历史；但其整页
    淘汰会低于设置值。去掉字节上限、设置 10,000 行并写入 14,046 条记录，80/153/256/
    512 列分别留下 9,955/9,745/9,997/9,997 行，三种内容结果一致；专用历史行数接口
    与 scrollbar 差值一致，上游 PageList 合同也明确整页淘汰会少于上限。不能直接视为
    精确迁移。本轮随后提交容量取舍，维护者明确确认“接受约一万行，复用上游机制”；
    按此更新原生开发合同，不引入自维护 VT 分支或第二套历史。
    探针和结果：`build/verification/1.7-history-budget/`，不外推为 ARM64 内存指标。
  - 当前批补齐决策证据：只读核对固定上游的整页淘汰、公开 API 与窗口命中所有者；
    用同一 ARM64 VT 库和独立临时程序测量 8 MiB/10,000 行设置下的历史范围、RSS/PSS
    及释放结果，不修改产品容量、上游实现或安装包。预计 30 分钟；执行通道受限即保留
    缺口，不创建额外驱动或应用框架。维护者确认后，regular VT 改用上游 10,000 行
    限制并移除 8 MiB 字节截断；temporary VT 仍以零字节禁用历史。
    独立可执行程序未取得有效样本，HDC 目标路径与预期不符；原始错误及临时目录清理
    证据保留，不推断平台不支持执行。改为此前已验证的签名 HAP 编译期公开样本触发，
    每批等待生产消费屏障，不额外实现测试驱动；测后还原源码和普通包。
  - 本批实现与验证：旧预算在新增容量反例失败，新实现通过全部 25 组真实运行时合同，
    覆盖 153/512 列逐格样式、历史搜索及 temporary 零历史和返回隔离。ARM64 签名
    测试包公开样本搜索为 1/9792、1/9855（包含当前屏），导航为 2/9855；随后
    `help mosh` 精确输入/单次提交通过。使用既有测试触发，不把这批当 SSH 完整性测试。
    排除图形的进程 PSS 从空测试 Pane 的 112,859 KiB 增至纯文本 154,430、彩色
    184,096；关闭约 43 秒后降至 123,306。原三 Tab 为 103,528，尚未完全回到基线；
    单次 App 观察包含缓存，不能推断独立 VT 峰值、零泄漏或已存在泄漏。详情见
    [迁移审计](native-terminal-migration-1.7.md)，没有为观测差异新增 GC 或回收补偿。
    证据：`build/verification/1.7-history-lines-arm64/`。既有 8 MiB 长历史内存数据不再
    代表新容量上限；正式候选依受影响范围复核，不重跑本批未改变的完整协议矩阵。
    普通签名包已恢复，精确 `help mosh` 与搜索 1/3 冒烟通过；两个包各自的临时 Tab
    已关闭，恢复原三个 Tab、第二 Tab 焦点、中文拼音及原窗口。没有远端连接或剪贴板
    变更；临时源码哈希已还原，policy/diff 检查通过，未发布或启用 release 替换。
- [x] 迁移余项实体验收：实体 Alt 矩形选择由维护者于 09-19 自行体验后确认正常；
  Ctrl 链接/系统打开归已有链接项。以下为此前自动化证据及其边界，不作为本轮人工录像。
  DPI、字号/双 Pane 坐标、字符裁切、光标形状、普通跨 Pane 拖动与释放后停止已有下述
  定向记录，复用有效证据，不再重复列为全套待验；精确闪烁周期沿既有低风险延期决定。
  本轮搜索新增缺口按上方独立项闭合，不以旧搜索装饰证据代替。
  - 当前批验证选择及光标：真实 VT 合同覆盖正反向矩形、Unicode 宽格、越界拖动松手
    停止，以及 DECSCUSR/显示隐藏/历史视口的光标状态。再以公开固定 VT 样本检查真机
    字符格裁切和光标形状；必要的短期触发沿 §4.6 编译期开关进入同一生产输出/交互链，
    不替代实体修饰键验收，不操作剪贴板或远端。只运行受影响原生合同与定向签名构建，
    最后恢复普通开发包和原设备状态。
  - 09-19 选择/光标本批合同通过：正反向矩形均复制完整中文、emoji 和选中列，偏移网格
    不改变结果；越界拖动松手后，以实际 viewport offset 确认停止自动滚动。六种
    DECSCUSR、DECTCEM 显示隐藏及历史视口排除实时光标，均通过真实 VT 状态检查。
    同一 ARM64 签名测试包真机像素确认方块 18×40、下划线 18×2、竖线 3×40、隐藏
    后零光标像素；矩形覆盖三行四列，右缘宽字完整占两格，仅余一格时换至下一行。
    中文、emoji、斜体和组合字符样本可见，均未画入右侧留白。分屏后真实失焦呈现
    18×40 空心轮廓，重新点击聚焦变回实心，布局与像素证据一致。
    上述矩形通过短期触发进入生产 pointer 链，只证明选区计算和绘制，不替代实体 Alt
    拖动；跨 Pane 边界捕获/实体越界拖动及闪烁时序仍待对应物理证据。首次空心像素
    检查沿用聚焦颜色，因既有非活动 Pane 调暗而未命中；按实际轮廓及调暗后颜色核对
    112 个边缘像素，非产品失败，未修改绘制代码。证据：
    `build/verification/1.7-native-selection-cursor/batch-evidence.json`。
    本批只增加运行时合同和证据；16 项产品源码哈希与普通包基线一致，复用该签名包
    恢复，未重复无变化构建。普通包 `help mosh` 单次精确输入/提交通过；临时 Tab 和
    两个测试 Pane 已关闭，原三个 Tab、第二 Tab 焦点、中文拼音和浮窗已恢复。
    未触碰剪贴板、未新建远端连接；policy 与 diff 检查通过，未运行完整发布矩阵。

- [x] 正式替换前，核对正常 Surface 生命周期及可恢复 context loss 的产品证据；不能将
  故障页当作恢复成功。按产品原则 §4.8，GPU 持续不可用不要求 App 保持可用，不建设
  CPU 兜底或继续扩大该故障矩阵。
  - 当前批按证据复用收敛：复查既有 Mosh/SSH 重建、受控 context loss、后台暂停及
    返回记录和当前所有者；当前普通包只补本地历史/搜索跨 Surface 重建、最小化返回
    及跨 Pane 拖动。已有会话连续性与 GPU 真机证据保留原包身份，不冒充当前包重跑。
    选择与恢复若出现产品失败，先记录最后正确边界和状态所有者，再做最小反例；
    不启动新的兼容机制或持续 GPU 故障试验。
  - 09-19 核对完成：`1.7-native-link-recovery`/Mosh 批次证明真实表面替换及会话连续；
    `1.7-native-egl-recovery` 证明受控 context loss 后实际释放、创建、swap、历史及
    同一 SSH 输入；`1.7-native-lifecycle` 证明 Tab、最小化和休眠返回的隐藏帧暂停、
    延迟输出及会话保留。均保留原始候选与清理身份，不声称自然驱动故障已发生。
    当前生产 attach/draw/release 方法和 18 项控制器合同再次通过；同一普通包、PID 6038
    的 Surface accessibilityId 165→268，前后本地 ssh 搜索均为 1/13；最小化返回
    保留同一 Pane/Surface/窗口，搜索仍为 1/13，`help mosh` 单次精确输入通过。
    结合已有 DPI/字号/双 Pane 几何与资源证据，此项不再重复列为开放生命周期矩阵；
    后续影响该链的变更按受影响范围重验，完整替换/正式候选门另列。
  - 同包补齐已激活 Pane 的系统鼠标跨边界选择与越顶滚动：跨 Pane 松手后，选区
    只在左侧；越顶拖动向上展开四行历史。两者释放后的整 Pane 像素及选区 mask
    均保持不变，另一侧没有选区。首次未激活 Pane 被共享焦点遮罩挡住，先点击聚焦
    后重做；一次复合 uinput 命令没有改变选区，改用已验证的单一 drag 命令取证，
    原无效样本保留。未改变焦点遮罩合同、未引入鼠标兼容分支。
    实体 Alt 矩形与 Ctrl 链接仍待验。闪烁模式状态、后台暂停合同及各光标形状已有
    证据；按 §4.9 将精确闪烁周期采样作为低风险延后项，不再单独阻塞替换；若出现
    停闪或后台持续出帧，再追加定时取证，不把未采样时序写成通过。
    本批没有产品代码/签名包变化，不重建或重跑协议矩阵；记录
    `build/verification/1.7-native-lifecycle-review/batch-evidence.json`。
    两个测试 Pane 已关闭，原三个 Tab、第二 Tab 焦点、中文拼音和窗口恢复；没有
    新建远端或操作剪贴板。policy/diff 检查通过；选择场景保留无效前置与修正后结果，
    自动化稳定性记为 flaky-harness，生命周期场景为 stable。
- [x] 记录并定位拖动后的左 Pane 大面积空白（已知 zsh/满宽提示符限制，非原生独有回归）：09-19 维护者自行体验时报告“随着我的拖动，
  左侧页面会出现大量空白”。当前原图和布局已只读取证：左 Pane 上方大面积空白，远端
  提示符及少量内容集中在底部；右 Pane 本地提示符正常。维护者进一步确认拖动对象是
  中央分屏分隔线，完整变化过程尚未取证。
  单张截图不能判断是新增空行、历史视口位置、resize 重排还是绘制遗漏，不预设根因。
  证据：`build/verification/1.7-left-pane-drag-blank/reported.png`、`reported-layout.json`。
  本轮只登记和保留现场，未发送输入、拖动、滚动或重建，未改产品代码；不覆盖用户
  当前远端会话。后续沿中央分屏 resize、真实网格/VT 视口/绘制边界做最小诊断，处理前
  保持完整替换门开放，不用此前几何或生命周期通过项抵消这个新报告。
  - 继续诊断：独立 `zsh -df`（不加载用户配置）的满宽两行提示符，在 100↔76 列十轮
    缩放后新增十个空行；同一实际 PTY 字节流在生产 Ghostty VT 和随包旧 xterm 中均复现。
    短提示符稳定；没有 Shell 重绘的纯 VT 六十次缩放也稳定。因此已定位一个与现场相符
    的 Shell 重绘/重排机制，不能再按 GPU 漏画或原生独有回归盲修；尚未采集维护者实际
    Shell 的完整事件流，不把构造反例写成其环境的最终归因。证据同目录
    `capture-zsh.py`、`model-probe.log`、`xterm-probe.log`；不读取远端配置、不注入集成。
  - 同链发现独立产品缺口：Surface 每次像素变化都发送相同行列数，旧 Web 路径有去重。
    预期首次网格及实际行列变化才通知 Session，像素/字号仍更新 VT 与渲染几何。
    最后正确边界是 Surface 实测网格，首个错误边界是 Runtime 无条件发出 resize；
    状态所有者为 VT worker。先用真实 worker 证明重复通知反例，再统一网格更新入口；
    不加防抖、Shell 特判或兼容后端。受影响 native 合同和 ARM64 构建即可完成软件证据，
    真机验收待安全的会话切换边界；当前用户 SSH 会话保留，不直接重装。
  - 重复通知反例已先失败、修复后通过；首次默认网格、实际行列变化、仅像素变化、
    Surface 重挂及字号像素更新均有实际 worker 合同。`terminal-native,policy` 通过
    （22 个控制器合同及 30 组 runtime 场景），ARM64 debug 测试签名构建通过。
    保留包 `1.7-left-pane-drag-blank/entry-signed.hap`，SHA256
    `0FC0752809FC9F111EA54FDD5C668BFF0D45E73B9F6F72A716DB036EFA2A843C`。
    经维护者明确允许中断会话后已安装（PID 42185）。独立 SSH fixture 实测中央分屏
    76→85→76 列逐格通知，无连续重复；边界从 1526→1533 物理像素而网格不变时，
    服务端 resize 计数保持 21。第一次 2 px 手势没有改变布局，已排除，后续真实像素
    变化才计入。左 SSH 精确输入匹配、右本地命令正常，两次受控连接均正常退出。
    临时信任、端口映射、fixture/临时目录及测试 Tab 已清理；原双 Pane、右侧焦点、
    中文拼音和窗口保留，中央边界复位残差 3 px、不改变网格，按 §4.9 不追逐无收益的
    子像素自动化精度。证据 `device-result.json`。重复通知修复完成，但不能据此宣称
    原提示符空白增长已解决；仍不启动 Shell 兼容层，不推进未闭合的完整替换门。
  - 维护者在最新普通包反馈：未连接时正常，`ssh wsl` 后仍会出现。只读新截图显示远端
    zsh 5.9 的两行提示符及已有大块空白，退出消息已紧接提示符；两条问题确实独立。
    同机 WSL 版本/地址吻合，仅读取提示符结构确认 Starship 的 format 含 fill、time、
    line_break，fill 为普通空格，未启用 custom 模块。当前假设：铺满首行在窄化时重排，
    zsh 重绘只回退旧行数，因而积累空行。预期缩放不持续引入空白；最后正确边界为有效
    网格通知，下一诊断区分 Shell 输出/VT 重排与原生独有错误：在隔离目录以 `zsh -df`
    加已安装 Starship 及配置只读副本复现，对照仅移除 fill 的副本，分别回放实际原生 VT
    和旧 xterm。原配置、用户会话不改动，不加入 Shell 集成或兼容实现；预计 20–30 分钟。
  - 该对照已完成：zsh 5.9、Starship 1.25.1，实际提示符配置的只读副本，在仓库外隔离
    目录运行且不加载用户启动文件；100→76→100 列十轮后，原生和旧 xterm 都从 4 行
    增至 14 行，新增十行均为空白，非空内容保留。仅移除 format 中 `$fill` 后，两者
    都保持 4 行。两套导出仅有行尾空格裁剪差异，不能声称原始字节相同；去掉行尾空格
    后逐行一致。首个仓库内样本触发 Starship git_status 格式警告，已排除并保留，
    不修改用户配置来迁就测试。原配置哈希未变，两个临时 PTY 和目录已清理。
  - 归属判断：满宽提示符先被终端重排，zsh 随后按原提示符行数向上清除/重绘，留下
    额外空白；固定 VT 与旧版行为相同，且不需要 GPU 或 SSH 传输即可复现。
    [Ghostty 作者对该机制的说明](https://mitchellh.com/writing/ghostty-devlog-001)
    提供主来源佐证。按 quality-strategy 的第三方限制规则记为非阻塞观察：完成定位，
    不宣称现象消失，也不把未采集的原现场完整字节流写成已取证。此项不再单独阻止
    原生替换；不新增 Shell 集成、resize 特判或修改用户 Starship。若要进一步支持该
    复杂提示符行为，须按既定兼容复杂度规则另行确认。证据
    `build/verification/1.7-ssh-wsl-resize/comparison.json` 和 `diagnosis.md`。
- [x] 补齐原生会话退出的输出定位：光标验收独立复现，备用屏退出后本地 close/prompt
  与最后内容之间产生大量空白。旧 Web 在统一 reset 后执行 `positionSessionOutput` 并回到
  底部；native 分支只发送包含 `CSI 999;1H` 的 reset，直接写本地输出，漏掉定位步骤。
  这与原报告的分屏/zsh 机制分开处理，不冒充原现场最终归因。预期本地消息接在当前底部
  页面最后有文字的行之后，保留历史和字面空格；最后正确边界为统一远端模式复位，首个
  缺口为 native reset→本地输出之间没有定位。VT worker 是网格/视口所有者；复用上游
  active-grid 读取和标准定位序列，在既有有序命令队列中补齐，保持 Session 的模式复位
  所有权。先建立空页、短输出、满页、宽字/空格和历史视口反例，再做受控 SSH 退出实机
  验证；不加远端程序识别、不改变活跃远端的光标或滚动语义。
  - 实际 Runtime 加载统一 reset 的反例确认短输出后产生 5 个额外空行。现由同一有序
    队列在 reset 消费后扫描 active page，定位至最后一个含 codepoint 的行，再写本地
    输出；字面空格保留、背景清除单元格不算内容。定位时回底，后续输出沿用上游跟随。
    23 项控制器、31 组真实 VT、8 项 Session 事件合同通过；历史场景明确先观察到
    非底部视口，再验证退出回底、内容和历史保留。未改变 Mosh 临时页恢复路径。
  - ARM64 普通签名包已安装，SHA256
    `D60B1DDED2F3DF123C99EA7321AE0499EA029B5554CB9C435E9986EFEA246C7A`，PID 641。
    受控 SSH 精确输入、备用屏/隐藏光标、正常退出均通过；截图中 close 和本地提示符
    紧接原内容，竖线恢复。本地删除临时信任的命令完成，测试 Tab、fixture、端口映射
    清理后恢复原双 Pane/右侧焦点、窗口和中文拼音。证据
    `build/verification/1.7-session-output-anchor/`。不据此关闭上面的原始分屏/zsh 报告。
- [x] 替换前关闭边界修复：只读审查发现 N-API 关闭任务创建/入队失败时，`Close` 只释放
  work/ref，Binding 的 TSFN→controller→handle 引用环可能仍在。预期关闭失败也释放已拥有
  的 VT/输入/渲染与回调；最后正确边界为资源归 Binding，首个缺口为异常返回未走完成清理。
  先编译实际 Binding/close 方法并对 N-API 分配/入队注入失败，确认后统一关闭清理入口；
  不增加新后端、重试队列或长期状态。预计 30–45 分钟，仅受影响合同、ARM64 构建和普通
  真机 Tab 关闭冒烟；不伪造平台资源耗尽、不重跑协议矩阵。
  - 实际 Binding/close 编译反例先失败；统一到 Binding 的资源清理入口后，五个 N-API
    准备/入队失败点及正常完成/未执行即取消均通过，work/ref/sink 无余额。正常路径
    保持异步 join；异常路径无人接手时同步收尾并保留原错误，不增加重试或后备实现。
    `terminal-native,policy`、最终 ARM64 签名构建通过。普通包同一进程完成两个新 Tab
    的精确命令输入和关闭；原三 Tab、第二 Tab 焦点、窗口及中文拼音恢复。
    首次中文预编辑在 Enter 前被缓冲检查拦截，取消并清理后显式准备英文；保留无效
    样本，未作为产品失败或通过。证据：`build/verification/1.7-close-ownership/`。
    没有在设备上制造 N-API 资源耗尽；异常释放为宿主边界证据，普通关闭为真机证据。
- [x] 按已通过的原型方向，一并交付最小会话解耦、原生终端集成及同机验收。显示层只接触
  终端交互合同；认证、`ltty>` 路由、Mosh 页面与退出策略归会话所有者。先单 Pane SSH，
  再 Mosh/双 Pane、完整既有交互及恢复；验证尾部输出、历史、关闭和系统效果语义。
  合成数据源、SSH、Mosh 应驱动同一核心与 GPU 表面而不修改渲染代码。以正确性、恢复、
  实际性能与资源证据决定替换；删除旧 renderer 前完成上述可用性门。
  - 当前批先做 release 原生分支的构建预检：仅在受控构建区间将 release 的
    `NATIVE_TERMINAL` 改为 true，保持 `ACCEPTANCE_TESTS=false`，构建 ARM64 包并运行
    既有验收标记排除检查，finally 按原始字节还原配置。预计 15–25 分钟，不改版本，
    不部署此包、不发布、不删除 Web，不把构建预检记为正式候选验收；实体键鼠缺口另列。
  - 09-19 预检通过：release 原生分支、API 24 ARM64、关闭验收开关的构建和 signed/
    unsigned HAP 验收标记排除检查通过。构建配置已按原始字节还原，release 默认仍为
    Web；测试机保留上一批普通原生包，没有安装预检包。证据：
    `build/verification/1.7-release-native-precheck/`。仍不关闭完整替换门。
    维护者选择自行体验，本轮不安排即时人工操作；同目录 `experience-guide.md` 提供
    实体 Alt 矩形、普通 URL/OSC 8 Ctrl 点击的固定公开样本和日常体验说明。等待实际
    反馈，不把清单交付或笼统“能用”当作这些物理场景通过。
  - 最新光标/退出定位修复后的收口批：复查当前原生与共享会话边界，更新迁移审计中
    已被维护者或后续证据闭合的状态；按原受控构建方式重新验证 release 原生路径及
    验收标记排除，finally 精确还原配置。预计 15–25 分钟，仅软件/包检查，不安装
    预检包、不提前删 Web 或改变正式替换门。最初分屏空白仍等待当前包的现场反馈，
    不把构造 zsh 反例升格为维护者环境的最终归因。
  - 最新 release 原生预检通过，签名 HAP SHA256
    `B024FE7A5480394872981E8E6A7B8DEA4222F54CC960837D769D91BFC1CFF82C`；
    API 24/AArch64、原生库系统依赖、包内验收标记排除和配置逐字节还原均已核对。
    证据 `build/verification/1.7-native-final-readiness/`；未部署该包，设备仍保留
    已验收的普通原生开发包。当前 migration 表已同步后续通过项，历史批次记录不改写。
  - 后续顺序已明确：原始分屏空白已按上述对照定位为既有第三方限制；汇总进入条件后，
    统一 debug/release 原生路径，成批删除 TerminalPane 的 Web 分支、Bridge/
    TerminalOutputBuffer/快照回放及 xterm 专用资源、依赖、补丁和失效验收入口。
    保留本地指南 HTML、指南生成/预览测试、共享 URL/文件安全和 Session 生命周期合同，
    不能因其当前位于 tools/web-terminal 就整目录误删。移除后做受影响软件门、ARM64
    构建及终端主路径真机验收，再进入既定版本/合入流程；不先重构旧 Web 或另建适配层。
  - 09-19 单一原生实现批：已移除产品的 Web 分支、Bridge、TerminalOutputBuffer、
    快照回放、Web 心跳/主题序列化及 xterm 资源、npm 锁文件和补丁；debug/release 不再
    使用 renderer 开关。本地指南与字体保留，CI 指南检查和原生启动/输出探针继续使用。
    受影响 `arkts,web,policy` 通过（186 项 ArkTS、23 项控制器、6 项 Session 事件）；
    构建工具注入/异常还原检查通过。47 条 ArkTS 警告内容未变，仅 UiText 行号移动，
    已对照原日志更新精确基线。证据 `build/verification/1.7-native-only/`。
  - debug/release ARM64 签名构建通过，两包都没有 xterm 资产且保留原生库/本地指南；
    release 验收标记排除通过。安装包 SHA256
    `760FBB9C11048D12514141CB57F4415CA0022612D06E36E1F3A39E1B4E4C04C9`，
    release 预检包为 `3D12660D3AE11B2AC581F9ACABA130BDAB86220CFD3CDF2335A32310708F11B1`。
    安装前发现维护者新开 SSH，经本轮明确同意后安装，PID 31380。已验证本地精确输入、
    搜索 1/13→2/13/关闭焦点、受控 SSH 精确收发、分屏本地隔离、切 Tab 后远端继续输入、
    备用屏/隐藏光标后的正常退出与提示符定位。临时主机信任、fixture、端口映射已清理。
    错误 Pane 标识及未切换焦点的测试快捷键导致两条公共文本进入本地，服务器前置核对
    拦住 Enter；取消并清理后按当前完整标识验证。无效样本保留，不记为产品通过。
    补测快照删除后的后台恢复：独立本地 Tab 输出 `help ssh`，切走超过 30 秒后布局确认
    原显示节点已卸载；返回仍可搜索 `ConnectTimeout` 得到 1/1，ready 明确为 recovered。
    这是同一进程 VT 保留证据，不是持久化恢复。测试 Tab 全部关闭，恢复原单 Pane 和
    原英文输入状态。最初准备英文时一次目标枚举瞬态为空，未注入动作；只读复查仍为
    唯一 Connected USB 后继续，未重启 HDC 或切换传输。
  - [x] 2026-09-19：共享输入和搜索验收工具迁移到原生组件。输入前后以控制 ID、
    window/accessibility ID 核对唯一目标，拒绝重复身份、组件重建和跨 Pane 输入；
    去掉 Web owner 和坐标兜底。删除 DOM 输入归因入口、解析器与旧测试，保留明文/
    隐藏输入、系统 IME、分屏隔离和服务端凭据核对。搜索改用原生控件 ID，闲置回收
    观察节点真正移除，Surface 重建观察恢复 ready。Agent 搜索和卸载恢复的布局
    选择器同步迁移，仅做软件验证，没有执行模型调用或卸载用户数据。
    本批按共享定位、搜索生命周期和直接调用者限定范围，用时约一小时；没有改产品
    实现。14 个输入身份/焦点反例、搜索/Agent/恢复选择器及实际报告深度检查、认证
    隐私/失焦/提交/清理证据检查、输入诊断自检通过。普通签名包
    `760fbb9c11048d12514141cb57f4415ca0022612d06e36e1f3a39e1b4e4c04c9`
    的五项搜索、password-success、32 字符明文/隐藏输入、英文→中文拼音→ASCII、
    分屏/关闭左 Pane/再次分屏的输入保留与隔离均通过，临时状态清理通过。证据位于
    `build/verification/1.7-native-harness/`。
    IME 初次数字异常已定位为 uinput 将未按 Shift 的 1 填为 Unicode 33；UiTest
    数字事件为 Unicode 0，产品原有映射正确。仅修改诊断使用的按键通道，临时探针
    源码和安装包均恢复，不加入产品兼容分支。详细来源、失败轮和首轮搜索 JSON 深度
    限制见 `test-release-efficiency.md` 的 2026-09-19 记录。
  - [x] 2026-09-19：原生性能验收工具收口。按 45–60 分钟的当前批准入范围，
    复用已有输出探针接入 debug 验收注入，删除独立嵌套注入包装器和旧 Web 记录解析；
    保留一次提交、逐字节有序、worker 消费、当前 Surface 显示完成及设置恢复。
    修正 review smoke 的两个旧 Web 布局样本；共享 hilog 查询在既有两组内加入
    原生标签，遵守每组最多 10 个标签。没有修改产品运行逻辑或新增产品 API。
    25 项原生探针合同、真实日志查询/18 个错误记录/3 个未知提交边界、性能探针
    生产隔离、构建工具、共享输入/认证证据和 policy 检查通过；ARM64 debug 签名
    构建通过。签名包 `c1e445aa6f9806363c76422f3d4ccba79cb023ca83f3b063bb711255652ea815`
    的五档透明度共 15 次输出均完整有序，每次 12000 行、984000 字节，零不匹配；
    五张截图尾行 11999 和提示符正确。透明度恢复 Medium，临时信任、夹具和映射
    清理通过。首两轮日志采集失败及补做清理保留，详见 test-release-efficiency。
    证据 `build/verification/1.7-native-performance-harness/`；恢复普通包
    `760fbb9c11048d12514141cb57f4415ca0022612d06e36e1f3a39e1b4e4c04c9`。
  - [x] 2026-09-19：原生 Mosh/恢复验收工具迁移。按既定 60–90 分钟范围，修复输入
    入口、消费确认、页面指纹、搜索定位与 ready 观察对旧 Web 的依赖。实际所有者仍为
    `TerminalRuntime` 的 regular/temporary VT 和控制器的 consumed 事件；仅验收
    构建在真实 begin/end 队列边界计算带历史、样式、光标/模式及视口的摘要，正文不
    离开 worker。按 Pane、页面序列和结束次序关联，源文件 finally 精确还原；不增加
    产品查询 API、快照回放、第二套 VT 或兼容分支。
    搜索核对原生控件、精确查询和当前已完成代际，拒绝旧 0/0；消费时间不再命名为
    画面呈现时间。原生 worker 摘要、24 项控制器合同、解析器/搜索/存活 Pane 反例、
    构建注入还原及生产标记排除、完整 device helpers 通过。tooling 登记的构建、
    device helpers、Host Identity、Agent helpers、指南预览五个入口已分别通过。
    收口额外发现 Agent 调用者的 14 个旧 Web 布局样本，按追加最多 15 分钟的测试
    样本范围迁移到原生身份，保留失焦、重建、窗口及重复目标拒绝；123 项调用链检查
    通过，零设备/模型调用。初次错误 WSL 身份及旧断言失败日志保留，不归为产品缺陷。
    ARM64 debug 签名包 `f4cb0d23f484490a9761ad7fc0de1a9bba25d3e9b16c7e4278b4e4593db5503d`
    在同机通过四项：显示组件重建、关闭 Mosh Pane 后另一 Pane 重连、同进程整页重建、
    注入客户端异常退出。均证明原页面摘要恢复、临时页隔离、输入就绪及完整清理；
    异常退出轮还独立核对 Mosh 内搜不到原本地历史。前三轮不补写这项新增搜索证据。
    证据 `build/verification/1.7-native-mosh-harness/`，最终恢复普通包
    `760fbb9c11048d12514141cb57f4415ca0022612d06e36e1f3a39e1b4e4c04c9`，精确本地命令、
    单 Pane 焦点及无端口映射均已核对。未执行预测/网络/长期资源或正式发布矩阵，
    本批通过不改写它们的既有证据边界。
  - [x] 完整替换交付前总审：结合迁移表与当前差异逐项核对原生路径、删除范围、验收
    工具和证据身份，处理确实遗漏的合同或失效入口；复用上述受影响检查，不自动重跑
    全部设备矩阵。完成后才关闭本节总交付项，再按既定版本/合入流程推进；正式候选、
    发布及 AppGallery 状态仍分别判断，不以四项 Mosh 定向通过代替。
    本轮有界收口：安全模型仍描述已删除的 Web/Bridge，设计文档仍链接旧文件；按当前
    原生所有者更新，不改变安全合同。另发现验收搜索日志在 busy 后可能把清空旧查询
    的 admission 记为原查询成功：最后正确边界是原查询返回 0，首个错误边界是 fallback
    后的探针日志。先用实际控制器构造队列满反例，再将观察移到原 admission；预计
    15–25 分钟，仅工具反例和受影响注入资格，不改产品或重跑完整物理矩阵。
    09-20 收口：控制器队列满反例先失败后通过，25 项实际控制器合同及输出探针、
    构建注入/异常还原/生产标记排除、完整 device helpers 和 policy 通过。安全、隐私、
    开发环境、职责/透明/GPU 原则及验收说明已按当前单一原生路径同步；历史设计保留
    时点，修正死链接、旧 8 MiB 历史描述和 Mosh 报告中的 ArkWeb 标签。没有修改产品
    运行逻辑或扩大兼容范围。复核迁移表、输入/回写所有者、队列/关闭、搜索/系统效果、
    显示/IME 和固定构建输入，未发现新增阻塞项；现有低风险延期及第三方限制保持原判。
    四份 Mosh 报告和普通 debug/release、Mosh 诊断包哈希相符；旧负向搜索结果布局没有
    busy/超限错误节点，页面恢复另有独立 VT 摘要，不将探针修正追记成旧包代码。
    本批未安装、重启或改动测试机。总审证据 `build/verification/1.7-final-audit/`，
    开发期完整替换交付闭合；正式候选与发布门仍未执行，不将多批开发证据合成为正式通过。

独立原型本身不修改产品实现；本次开发已完成原生替换和上述开发期定向验收，尚未进入正式候选验收。不为旧 Web 实现另做
完整架构改写，不加入本地 shell、HSL 直接接入、系统进程启动或通用 Transport 框架。
原始研究源码、包与证据保留在本地 `build/research/`，共享设备已由原型任务清理交还。

维护者于 2026-09-20 确认 1.6.0 已过审上架，[版本规则](versioning.md) 中下一高版本
产品合入的前置条件已满足。1.7 后续进入变更整理、代码审查与 PR 合入流程，再准备独立
的正式候选；当前开发期证据不替代发布验收。

- [x] 1.7 变更整理、代码审查与 PR 交付：本批核对完整源文件范围、固定依赖、既有
  验证身份和公开源码边界，复用总审与定向真机证据，仅补提交/CI 发现的实际缺口。
  1.6 上架确认按发布规则独立为状态 PR，随后合入原生替换；不混入新功能，不重跑设备
  矩阵，不据 CI 通过宣称正式候选验收。预计 30–60 分钟；未知检查失败先定位事件链。
  09-20：状态 PR [#214](https://github.com/wandcs/leantty/pull/214) 已合入，原生替换
  由 [#215](https://github.com/wandcs/leantty/pull/215) 交付；合入状态与精确提交以该 PR
  为准。首轮四项 CI 均通过；规则集仍要求旧检查名，因此保留 `Web terminal policy`
  这一检查身份，实际仅运行离线指南检查。未改保护规则或增加产品兼容分支。同步最新
  main 后再验证 PR head；正式候选尚未开始。

- [x] 1.7 正式候选准备（09-25 闭合，身份和证据见顶部）：按版本与发布流程准备独立发布分支、版本元数据
  和干净提交，再执行 C1–C4 适用门禁。先核对现有发布工具资格与材料，不把开发期多包
  证据拼成正式通过，不提前创建版本 tag 或发布资产。
  **09-22 新 R4 当前执行范围：** #227/#228 已合入，起始主线为 `2613710`；按统筹
  交接继续新候选准备及自动验收。先将本次必要状态整理经 PR 合入，再把同一已推送干净
  提交固定到独立 production/review/harness 克隆。依赖、锁文件和原生产品字节已变化，
  明确采用 R4，旧 C1–C4 不复用为新候选通过，旧失败报告及候选保留。
  先执行零模型就绪演练，再冻结 C0、运行 C1/C2、QH 和 `-Phase automatic`；预计准备
  20–40 分钟加正式自动阶段。沿用既有 release-mode 样本检查标记排除，明确只证明演练
  输入，不能代替新候选/C4 包检查。模型仅既定 9 项必要用例，零自动重试；不发布标签、
  GitHub Release 或操作 AppGallery。自动阶段通过后按同一候选停在 awaiting-operator，
  人工锁屏/合盖待维护者实际准备好后走 `-Phase operator -Resume`。
  当前 27 项注册阶段不调用基础 `verify-put-get-pc.ps1`，故不扩大矩阵或为 #228 新建
  工具框架；它的 Downloads 准备/恢复及测试信任补充清理由实际调用者负责。正式默认
  ECDSA 阶段复用已验证的现有权限/密钥恢复助手。工具采用仅在本轮启动边界，冻结后
  不热改；缺设备则完成独立准备并记录缺口。阻塞/未知或清理失败停止包围矩阵，先记录
  最后正确/首个错误边界并按质量策略重审，不探索性调用模型或自动完整重跑。
  以下为既有批次历史，不撤销上述当前执行范围。
  本批先在 `release/1.7.0` 准备版本与指南，预计 45–75 分钟。新增 native OHPM 版本
  未进入 `build-all` 正式版本校验；用实际校验块的错版反例确认后补入，限 15–20 分钟，
  不扩建版本工具。指南按 Changelog 逐项核对并做双语浏览器审阅；中文另作自然表达复核。
  独立维护区仍有未提交 PUT/GET 补丁，暂不复制；现有 main 工具在新候选上独立取得资格。
  正式 clean 会删除所在 checkout 的 build 目录，因此沿用独立验收克隆，保护主开发区
  的历史证据与登记工作树。到工具资格/实际场景再判断维护补丁是否确实需要，不提前修理。
  09-20：七处版本元数据及 Cargo/OHPM 生成锁文件已对齐 1.7.0/1007000；原生包版本
  缺失检查的真实错版反例先失败后通过，构建流程回归通过。指南修订 10 完成独立中文
  复核、双语事实核对和 1280/800 宽度浏览器审阅：63 项检查、32 张截图，源文件与
  打包资源逐字节一致，100124 字节；指南静态/34 项预览检查及 policy 通过。证据位于
  `build/verification/1.7-release-preparation/`。设备控制预检通过，尚未重启或安装。
  下一步先做独立克隆的零模型发布就绪演练，再冻结合入提交，进入正式候选构建。
  就绪演练首轮在 `native-startup-probes` 停止，未创建候选或调用模型。最后正确边界是
  冷启动探针已写入；首个错误边界是嵌套输出探针查找被冷启动探针拆开的输入锚点。
  权威所有者仍是原生控制器的 `onInput(text)` 调用；两探针都应观察而不生成输入。
  本批追加 15–25 分钟，仅让输出探针使用两路径都保留的唯一输入调用作锚点，并执行
  冷/热启动与验收注入嵌套的真实控制器合同、成功/失败精确还原。现有嵌套用例先复现
  失败，再验证修改；不改产品源码、时序、兼容分支或已冻结证据。随后从头重做就绪演练。
  定向结果：原嵌套用例复现失败；缩小为唯一输入调用锚点后，冷/热及各自嵌套注入的
  四组实际控制器检查各 24 项、输出探针 25 项和构建流程回归通过，源码精确还原。
  演练没有分段续跑入口，预计重复两分钟；复用现有入口整轮重做比增加续跑工具成本低。
  PR #218 已合入，C0 固定为 `4730616`；C1 完整 33 项通过，C2 干净 ARM64 构建与
  签名安装通过，候选 SHA-256 为 `fdd616dcd9c378c05f04c269dca6f7d1bc958621207531f36f4eeca9313e7f81`。
  正式 QH 通过；自动轮在密钥口令报告发现 depth 7 截断深层提交记录，立即停止后续验收。
  已进入的密钥备注场景中断，临时密钥、信任、映射、夹具及屏幕超时已独立清理；模型调用 0。
  原报告和中断状态保留，不将软件操作成功提升为完整物理验收。外部证据位于发布区
  `formal-verification/1.7.0-candidate-20260920/` 与 `1.7.0-automatic-20260920/`。
  本批追加 20–35 分钟修复唯一口令报告写入者：期望提交对象完整保存；最后正确边界为
  内存中的命令观察，首错为 `Write-BehaviorEvidence` 的 depth 7 序列化。复用已有原子
  JSON 写入器与深度警告拒绝，先用实际报告函数复现字段丢失，并验证超限不覆盖旧结果。
  不改产品运行逻辑。没有可靠的中断阶段续跑证据，本轮 C3 全部废弃。最初拟 R3 复用
  候选，但五个验收入口的固定跨工具名单不覆盖这两文件；为此次修复扩展名单及验证
  的代价高于约十分钟候选重建。选择既有 R4 路径重建，再重新 QH 和自动物理轮，
  不扩建取消/续跑框架或兼容名单。60 项实际报告/清理测试通过；首轮工具软件资格
  误在沙箱身份调用 WSL，已分类为执行环境失败，改用桌面身份复核，不修改产品。
  桌面身份下完整受影响工具资格软件检查通过；后续以修复 PR 合入提交重建候选。
  PR #219 合入后，`764f8c4` 的 C1/C2 再次通过，候选 `e74ecb74…707d` 的正式 QH、
  密钥口令、备注重启、ECDSA 导入重启和受控 Host 绑定通过；8 条深层提交记录完整。
  自动轮在系统 OpenSSH 密码认证失败后停止，场景清理通过，模型调用仍为 0。
  有界诊断发现 `uinput` 将公开数字 `0123456789` 注入为 `)!@#$%^&*(`，释放 Shift
  不改变结果；同机 UiTest 数字和原生 TextInput 对照均正确。上游注入器
  `InputManagerCommand::KeyCodeToUnicode` 固定返回 transitioned 字段，与现场一致。
  期望秘密原样送达；最后正确边界是生成的数字，首错为注入器 Unicode；输入权威仍为
  系统 IME/KeyEvent，不能为坏注入改写产品键盘逻辑。追加 15–25 分钟删除 Host 验收
  的特殊逐键分支，复用已有隐藏文本输入；实际函数反例验证焦点、隐藏审计及单次提交。
  候选与产品不变；修复文件已在现有工具兼容名单，按 R3 新 QH 和完整自动轮执行，
  不扩展跨轮复用框架。旧轮与清理证据保留在发布区 `1.7.0-automatic-20260920-r4/`；
  数字对照在 `build/verification/1.7-release-preparation/openssh-diagnosis/`。
  此为 test-release-efficiency 中 09-19 已知限制的遗漏调用者；记录本次新事实，不增加
  平台兼容分支。实际秘密函数 40 项检查通过（原版两项失败）；同步移除共享检查中
  强制旧逐键实现的断言，保留秘密不暴露、账号设置和清理合同。
  精简后真实 OpenSSH 六项（精确公钥安装、绑定认证、重启、移除绑定的密码路径、恢复）
  与清理通过，完整受影响工具软件资格通过。此诊断使用未提交工具，不导入正式 C3。
  五处实际兼容名单均接受本次四文件，已保存核对；候选字节继续保留，下一轮按 R3
  使用干净合入工具、重新 QH，旧轮不续写通过。
  PR #220 已合入，R3 固定工具 `3e93a3c`、候选仍为 `e74ecb74…707d`。系统 OpenSSH
  首次随机端口命中旧信任，产品正确拒绝变化指纹；清理并独立审计该端点后按现成
  R1 入口续跑，保留前五项结果，旧失败报告另存。随后三组 Host、七组后台通知、异常
  恢复和长任务通知均通过，正式前缀共 16 阶段。Agent 中 Codex direct/tmux 通过；
  OpenCode direct 未完成：最小化前 SSH 已关闭，恢复后原生终端显示不可用，搜索失败。
  原始 cleanup failed（临时端点信任删除未确认）保留；补充清理已确认端口 31296
  的信任、映射和夹具不存在，原 Tab/权限由本轮清理恢复。未重试模型请求。
  本批进入有界原生故障诊断：预期 Agent 输出与窗口隐藏后 VT/SSH 持续有效；最后正确
  边界为 OpenCode 交互布局，首错为 03:21:28 的 terminal ownership released，随后
  才隐藏窗口。权威所有者是原生控制器/VT worker，不能用放宽搜索或通知断言绕过。
  先用现有零模型交互入口区分 TUI/输入/窗口链，再视证据增加仅诊断的数值/枚举观察，
  不留终端内容或秘密。预计 30–45 分钟；第三种修复或 90 分钟未决先重审问题定义。
  证据在发布区 `1.7.0-automatic-20260920-r3-pr220/` 及开发区
  `build/verification/1.7-release-preparation/agent-stop-diagnosis/`。SSH/Mosh 与 C4 尚未执行。
  零模型 OpenCode 原生输入/IME/缩放基线通过。仅数值故障位置的诊断包已生成，源码
  精确还原；空闲 TUI 最小化/恢复、随后 IME/缩放和退出及清理均通过。原 Qwen 焦点
  探针的 1004 前提不适用于 OpenCode 空闲界面，失败证据与补充清理单独保留，不算
  产品失败。当前不再假定单纯最小化必现；下一最小诊断在同一零模型窗口路径加入
  15 秒受控 PTY 光标刷新，以区分持续输出与失焦交叠，仍不请求模型或修改产品行为。
  该诊断完成 2,928 次光标刷新，窗口隐藏/恢复、后续输入、IME、缩放、退出和清理均
  通过；因此未证明输出与失焦足以触发故障。保留原产品失败，停止增加同类无模型
  变体。下一步准备包含固定原生异常位置及平台数值错误码的临时诊断包，并请求额外
  一次 OpenCode direct 原场景调用的授权；既有固定模型预算不扩展为探索性重试。
  在授权前仅准备探针和核对源码还原，不发请求，不将上述诊断提升为正式 C3 通过。
  完整数值诊断包构建/安装完成（SHA-256 `4b1be07a…e1bdf`）；原生与 ArkTS 源码
  已逐字节还原，单次重现入口已准备并禁止覆盖既有运行。维护者随后回复“继续”，
  按紧邻待确认事项执行额外一次 OpenCode direct 诊断；不扩展到更多请求或完整矩阵。
  单次诊断通过：原生通知、返回、搜索、输入和重连均成功，资源清理通过，额外模型
  请求已用 1 次；未捕获原失败，因此不宣称修复。重审诊断：空闲窗口、持续输出及
  冷进程单次请求都不足以复现；下一项仅用已有零模型入口连续执行 Codex/OpenCode
  direct/tmux，复查同一 Pane 的会话/临时 VT 页切换与隐藏恢复。预算 10–15 分钟，
  若仍通过则停止扩大复现，保留原始阻塞与诊断包，转为明确证据缺口的处置。
  连续四组零模型场景及最终清理全部通过，同一 Pane 从 Codex direct/tmux 切换到
  OpenCode direct/tmux 未出现失效。累计假设仍未成立；本轮停止继续重跑，原问题保留
  为“已观察到、根因未定位”的发布阻塞。诊断包留在测试机，正文不落日志；新增仅本地
  的只读故障码采集入口，下一次自然复现时先取证再重启。报告、包身份和源码还原核对
  均保存在同一诊断目录；正式 C3 不因本轮诊断通过而续写或标绿。
  继续排查的重审：当前进程无新故障码，不重复 Agent 请求或窗口动作。SDK 与上游
  `inputmethod_imf@929a2cd3` 证明系统 OnDetach 只把 NDK proxy 标为 detached，光标
  更新可返回 `IME_ERR_DETACHED`；当前 TerminalInput 抛错、控制器 fail、Session 断开
  把输入资源状态扩大为 VT/连接失败。改用一次零模型确定性平台解绑诊断，验证“输入法
  解绑不应销毁 VT/SSH”这一既有所有权合同。最后正确边界为平台解绑完成，待辨首错
  为后续 cursor 通知；API 返回与控制器/Session 状态联合观察。仅测试包临时 F12 入口
  调用系统 stopInputSession 后通知光标，不模拟返回值、不复用已释放指针，源码精确
  还原。预算 20–30 分钟；即使重现，也只证明该生命周期缺陷，不冒称原现场根因。
  **待具体批准：** 自动审批拒绝该临时 F12/解绑探针的构建执行，按项目兼容性规则
  将其归为需维护者事先确认的实现探针；本轮“继续”未覆盖此新方案。准备脚本仅在
  忽略目录，尚未执行构建或安装，产品源码未改。已请求批准一次探针及安装重启，
  或选择等待自然复现；答复前只保留上游和当前代码的只读证据，不实施替代触发。
  维护者随后明确回复“批准”：授权上述一次临时解绑探针及安装重启，继续零模型执行。
  已批准探针完成：平台 stopInputSession 成功后，实际光标通知返回 **12800016**，
  并非原假设的 12800009。SDK 与同一上游提交的错误转换确认其含义为“客户端不可
  编辑”；当前代码对所有非零结果抛错，控制器随即 fail 并断开 SSH。现场队列为空，
  没有容量或超时触发。由此确认一个 IME 生命周期缺陷，仍不能证明与原 OpenCode
  现场同源。负例及原始 invalid/interrupted 报告保留在 `ime-detach-negative/`；
  其临时信任删除由独立补充清理确认，原 Tab、通知权限、屏幕超时、映射和夹具均已
  恢复。本轮模型调用 0，所有临时产品与工具注入已精确还原。
  **已批准的最小修复：** 仅将光标通知的已解绑/不可编辑状态与致命错误区分，保留
  VT/SSH，复用现有失焦/重新聚焦时的 IME 绑定；其余错误保留原处理，不加重试、
  后备路径或新状态机。永久错误码分支按第三节单独确认，本次探针批准不自动扩展。
  批准后先对实际 cursor 实现做成功、生命周期状态、其他错误及资源释放的反例/正例，
  再以同一零模型平台触发验证连接存活、重新聚焦后的英文/中文输入、缩放与退出。
  预算 30–45 分钟；若现有重新聚焦不能恢复 IME，先重审，不追加自动重绑机制。
  维护者随后批准最小修复和回归安装，按上述边界执行；探针前的原诊断包已恢复，
  F12 触发入口已从测试机移除，负例包仅保留为本地证据。
  软件反例在实际 cursor 方法复现抛错；修复后成功/解绑/不可编辑、其他错误、过期
  所有者、空 editor/proxy 和资源释放均通过，现有原生合同、23 项控制器检查及构建
  工作流回归通过。首轮 Zig 因沙箱无法访问用户缓存而中断，桌面身份反例/正例分别
  失败/通过；未把环境失败当产品反例。产品仅一处错误分类，未增加重试或所有权状态。
  修复包的零模型真机正例通过：同一平台停止操作实际返回 12800016，终端未失败，
  同一 SSH 会话随后接收英文与中文输入，缩放、备用屏退出及最终清理全部通过。
  证据为 `ime-detach-positive/result.json`（探针 HAP `2ca7a254…4ed6`）；12800009
  仅由真实方法的软件合同覆盖，不声称物理复现。临时产品和验收脚本均已还原。
  该已知生命周期缺陷已修复；原正式 OpenCode 故障缺少错误码，仍未证明同源。
  产品源码已变，后续正式验收必须按 R4 新候选重启，不能续用旧候选或将本轮诊断
  提升为 C3。当前仅完成已批准的零模型回归，不扩大模型预算。
  常规修复包 `6d0a14c0…2e27` 已构建、签名并安装；包内 ArkTS/native 字节检查确认
  F12/数值故障探针均不存在，本地单次 help 输入及可见输出正常。该轻量检查遇到
  下载权限提示后选择不授权，再核对原命令结果，没有重发；不把指南访问算通过。
  最终证据汇总为 `ime-lifecycle-fix-disposition.json`，工作区保留最小产品改动与定向
  回归用例，原失败报告及补充清理证据不变。
  继续执行：先将已验证的 IME 最小修复经 PR 合入，再在独立验收克隆冻结新 C0，
  按 R4 执行 C1/C2、QH 和自动物理轮；原始失败与旧候选证据保持不变。新一轮
  验证修复后的完整产品，不预先认定原 OpenCode 故障同源；再次出现未知结果立即
  停止并取证，不自动重试模型。固定正式轮最多 9 次必要模型调用，人工锁屏/合盖
  留在自动轮通过后的既有 operator 阶段。IME 条目恢复已有输入/焦点合同，未改变
  用户操作或设置；指南按新增 Changelog 条目复核后保留已审阅的双语资源字节，
  不为内部错误分类新增用户操作说明。独立工具维护分支不在本次候选中。

  PR #221 经维护者批准合入 `c7080d4`；独立 production/review/harness 克隆冻结同一
  提交。就绪演练及 C1 的 33 项软件检查通过，新 C2 `13fdfe25…b27b9` 构建、签名、
  安装及 QH 通过。09-21 自动轮保留在发布区 `1.7.0-automatic-ime-20260921/`，
  前 17 个注册检查点通过或复用，Agent 前七项完成（包含既有通知适用性限制）。
  OpenCode direct 的通知、输入、搜索及重连通过，仍不倒推旧现场与 IME 缺陷同源。
  最后一项 Qwen tmux 在脱离后的 `shell-ready` 等待超时；信任删除未确认导致阶段为
  invalid/interrupted。原 Tab/权限已恢复、映射和夹具已清理；原失败报告保持不变。
  SSH/Mosh 尚未启动，备用网络由维护者确认为 `CU_lin`；不追加模型请求或启动 C4。
  **本批有界诊断（20–30 分钟）：** 先独立清除本轮端点信任并审计剩余状态，再用
  已保留的脱敏 PTY 摘要、进程/命令边界和工具源码定位。预期 tmux 脱离后受控 Shell
  重新就绪；最后已知正确边界是远端任务完成标记及随后搜索，首个缺失边界是脱离后的
  Shell 标记。Shell PROMPT_COMMAND 拥有该标记，不能用终端布局代替。下一最小核查
  区分远端进程提前结束与 tmux 脱离/输入未到达；不先认定产品故障或增加等待/重试。
  清理问题与同一退出链一起评估；若需实现，先记录具体证据和最小修复范围。
  达两种实质修法或 90 分钟未决检查点先重审，不能把本轮七项手工拼成 Agent 通过。
  独立清理已确认本轮端点 `36518` 信任、映射及夹具不存在，原报告哈希未变。随后
  两项零模型诊断及清理通过：原有 Qwen tmux 英文/中文输入、缩放和退出；以及临时
  复用正式输入/脱离步骤的隐藏恢复、大段输入、tmux 脱离、SSH 重连和重新附着。
  产品包不变，测试脚本已逐字节还原；不把这些诊断算正式 Agent 通过或原故障修复。
  **重审结论：** 普通交互与脱离不足以复现，现存正式摘要也未记录失败前后的进程/
  输入计数；停止叠加无模型变体或猜测性延时。已离线准备单次 Qwen tmux 原场景
  诊断，在输入前后、脱离后和失败时记录无正文的 PTY 计数、tmux 进程/客户端状态及
  Shell 标记，语法与限定调用范围已核对。预计 5–8 分钟，额外最多 1 次请求、零重试，
  需维护者批准后执行；本轮固定预算不自动授权探索性模型调用。准备与证据位于
  `build/verification/1.7-release-preparation/ime-r4-agent-exit/`。原正式失败仍保留，
  SSH/Mosh、人工阶段及 C4 未执行；后续恢复范围依清理与 R1–R4 规则重新决定。
  维护者随后回复“继续”，按紧邻的单次诊断方案执行：额外最多 1 次 Qwen tmux 模型
  请求及诊断安装，记录上述边界后恢复测试脚本；不扩展为正式矩阵重试。
  单次诊断及清理通过（`one-model-qwen-tmux/result.json`）：输入字节从 936 增至
  5,089，包含中文；脱离后 tmux 客户端列表为空、Pane 仍存活，Shell 就绪标记出现，
  随后重连、重新附着、退出均成功。Qwen 0.23.0 的受控任务已完成但未发出通知信号，
  完整正常退出证据满足既有 `not-emitted-by-agent` 条件；不称系统通知通过。
  本次额外请求 1 次、零重试，临时 verifier 已按 SHA-256 确认还原；产品无改动。
  **当前结论：** 原脱离超时未复现、根因未定位，不能宣称修复，也不能把这项诊断与
  原轮前七项拼成正式通过。停止扩大同类探针和模型调用。C0–C2 保留；原轮清理曾失败，
  不能直接使用 R1/R2 续验。若安排下一正式轮，应先明确 R3 的完整物理重验成本与模型
  额度，再启动；本次单请求诊断批准不自动扩展为新轮。SSH/Mosh 和 C4 继续待验。
  维护者随后明确批准“继续 R3 正式重验”。复用原 C0–C2 `c7080d4` / `13fdfe25…b27b9`，
  已重新核对冻结 harness 干净且 HAP 摘要未变；新轮重新 QH 与完整自动物理矩阵，
  固定最多 9 次必要模型请求、零自动重试。证据另存发布区
  `1.7.0-automatic-ime-r3-after-qwen-20260921/`，原失败与单次诊断不改写、不拼接通过。
  本轮验证当前产品，不预先宣称原脱离故障已修复；人工锁屏/合盖仍在自动轮通过后进行。
  R3 再次在 Qwen tmux 脱离后的 `shell-ready` 超时停止，前 17 个注册检查点和 Agent
  前七项完成；临时信任删除未确认，原始阶段仍为 invalid/interrupted。没有启动 SSH/
  Mosh 或追加模型请求。**重审：** 单独同场景通过、完整序列连续两次同点失败，不能再
  把同包整轮重跑作为下一诊断。先独立清理本轮端点 `33614`，保留原报告；把问题改为
  前序会话/共享 tmux 状态是否影响最后一次脱离。预期前序退出后新会话键盘协议与
  tmux 客户端状态独立；最后正确边界是 Qwen 搜索，首个未证实边界仍为脱离/Shell
  就绪，分别由终端键盘状态、tmux 客户端及远端 Shell 拥有。
  下一批仅零模型串联 Qwen direct → tmux，复用已验证的输入/隐藏/脱离诊断，比较单独
  tmux 的正例；必要时再核对更早 Agent 共享的 tmux 状态。预算 20–30 分钟，不新增
  产品状态清理或重试；得到可区分证据再决定修复，未定位则保留阻塞，不追加完整矩阵。
  有界诊断结果：`ime-r3-agent-exit/zero-model-qwen-sequence/result.json` 中 direct
  通过，随后 tmux 在同一 `shell-ready` 边界失败；全程零模型。这把复现条件缩小到
  Qwen direct → tmux，不再需要整轮或更早的三个 Agent。两轮遗留临时信任已分别
  独立清除，映射和 fixture 均不存在，原失败报告摘要未变；诊断脚本逐字节恢复。
  原生软件对照 `ime-r3-agent-exit/keyboard-reset-probe.json` 使用真实 Ctrl+B 参数
  （text=`b`），得到初始 `02`、开启 Kitty 后 `62`、现有会话重置后仍 `62`、协议
  pop 后恢复 `02`。源码确认原生 key event 没有设置 `unshifted_codepoint`，增强模式
  把 Ctrl+B 退化为普通 `b`；现有会话重置序列也没有清除 Kitty 键盘状态。最初空文本
  探针观察到零输出，但不对应界面实际参数，已由上述正确参数的结果替代。
  **当前归因边界：** 两处软件缺陷已有证据，尚未证明它们就是这次真机序列失败的
  完整原因，不能标记正式故障已修复。下一步以零模型序列核对主/备用屏协议状态及
  tmux 实际收到的 Ctrl+B 字节，再选择键盘事件字段/会话状态所有者处的最小修复；
  保留传统 Ctrl、增强 Ctrl、Shift 字符、IME 提交与跨会话隔离回归。不增加 Qwen
  特判、自动重试或后备键盘路径。本批未修改产品代码，也未启动新正式轮。
  维护者继续后，先运行零模型键盘边界观测，同时修复已由真实原生核心证明的键盘
  事件字段遗漏：复用 `KeyCodeMap` 的未 Shift 字符映射，把字符码随原事件传入 Ghostty，
  不新增键盘映射或协议分支。预期增强模式 Ctrl+B 输出 CSI-u，传统模式仍输出 `02`；
  最后正确边界是 ArkUI 的 code/modifiers/text，首个错误边界是缺少未 Shift 字符码的
  Ghostty event，状态分别由平台事件和 VT 编码器拥有。验证选 native/controller 定向
  回归、ARM64 增量签名构建及零模型 direct → tmux；正式矩阵不自动重跑。
  同时闭合已有会话隔离合同：在原重置序列中分别清空主屏和备用屏的键盘栈，保留
  主屏内容/历史。协议规定两屏独立，因此只清当前屏不足。主机测试读取实际生产
  序列，覆盖从主屏/备用屏退出及遗留多层模式，不维护第二份重置规则。
  2026-09-21 真机对照已定位按键错误：`zero-model-key-boundary-shared/key-boundary.json`
  中旧包 Ctrl+B 后 PTY 新增字节 `98`（普通 b），tmux 仍在 `root` key table；修复包
  `zero-model-keyboard-fixed/key-boundary.json` 中没有错误字符透传，tmux 进入 `prefix`，
  direct/tmux 两项、脱离重连/重新附着及清理均通过。最初观测因 Windows 独占读取
  正在写入的捕获文件而中止；临时诊断改用共享只读句柄，未修改正式 harness。
  补充 Shift 回归发现还须标记生成大写/符号时消耗的 Shift，已在同一事件入口补齐，
  保留 Ctrl/Alt 的完整修饰键及 Shift-Space。最终软件报告 `key-final-software.json`
  通过，另有 6 项 Session 合同、构建流程合同及 ARM64 签名构建通过。
  最终诊断 HAP `1e56529e…baa8288` 的 `zero-model-keyboard-final/` 同序列及清理全部
  通过；`Ctrl+B` 无错误字符透传、tmux 进入 `prefix`，并完成脱离、重连、重新附着。
  本批全部诊断零模型，原正式失败报告不改写。产品变更后下一正式候选按 R4，不复用
  旧候选 C3 通过或把本批诊断提升为正式验收；本轮先交付可审阅修复与定向证据。
  维护者随后回复“继续执行”，按紧邻批准方案将 PR #222 squash 合入 main，合入提交
  `678736a`，三个独立发布检出已固定到同一干净提交。开始 R4：新 C1/C2 证据目录为
  `1.7.0-candidate-keyboard-20260921/`，后续重新 QH 和完整自动物理矩阵，固定最多
  9 次必要模型请求、零自动重试，备用 Wi-Fi 沿用维护者指定的 `CU_lin`。旧 R3 与零
  模型修复诊断不提升为正式通过；人工锁屏/合盖在自动阶段通过后独立恢复。
  新 C1 完整软件门禁 33/33 通过，C2 干净 ARM64 签名构建、安装、启动及保留通过；
  新候选 SHA-256 `3a6db18f…ee6514`。已启动 `1.7.0-automatic-keyboard-20260921/`
  的 QH 与自动物理阶段。人工后缀入口已绑定新提交/HAP，四条现场提示的离线自测
  通过，未执行人工动作或发声；自动阶段未通过前不会启动该后缀。
  R4 的 QH、通知及完整 Agent 阶段通过（前 18 个注册检查点）；Qwen tmux 在完整
  顺序中完成分离、重连、重新附着和清理，原键盘故障未再出现。随后 SSH 的
  `terminal-key-input` 等待旧 Web 的 `OSC 52 clipboard write success` 日志超时，
  矩阵已停止，SSH 自有清理通过，Mosh/人工后缀/C4 未启动；本轮没有追加模型请求。
  **本批工具阻塞修复（20–30 分钟，零模型）：** 原生 Controller 已执行剪贴板写入，
  但不再产生上述 Web 日志；09-18 定向键盘证据已记录同类旧判据问题。预期受控 OSC 52
  写入后 Ctrl+V 的远端字节准确；最后已证实边界是 SSH 连接，首个错误判据是等待不存在
  的日志。系统剪贴板拥有写入状态，夹具拥有最终收到的粘贴字节。先以同包最小按键
  诊断区分实际粘贴故障与失效日志判据；仅修复 SSH 同链的 17 字节/1 MiB 判据并保留
  精确服务器结果，不加产品日志、延时重试或兼容路径。新工具先做软件及定向真机验证，
  原冻结 harness 和失败报告不改动；之后按真实支持的续验边界另作 R1–R4 成本决定，
  不自动重跑模型矩阵。若出现新产品缺陷或预算内无法得到可靠判据，停在诊断结论。
  同包两项零模型诊断及清理通过：`native-clipboard-oracle/keys/` 验证 17 字节，
  `transport/` 验证 1 MiB、传输、分屏与重连；`test-device-regression.ps1` 通过。
  **续验成本决定：** 原轮已通过前段共约 58 分钟，重新整轮还会重复 9 项模型用例。
  现有跨工具提交入口仅支持 Agent 失败；手工补报告不满足正式证据格式。依质量策略
  交付效率授权，本批再用最多 30 分钟复用现有 continuation 校验，限定增加 SSH 原生
  剪贴板工具修复的 R2：原同包、完整通过前段、失败首个 SSH group 且清理通过、未启动
  Mosh 后缀、严格工具文件范围与原报告哈希，重新 QH 后完整 SSH 和余下阶段，新增模型
  用例为零。不新增通用跳过机制、不导入诊断通过、不改写原报告。若无法在预算内通过
  正反例资格测试，保留工具修复并重审，不能自动退回额外模型整轮。
  上述受限入口已完成离线资格：真实编排测试 60 项通过，保留前 16 个行为阶段及候选，
  新 QH、完整 SSH、6 项自动 Mosh 与人工后缀按原序执行；新增模型用例为零。SSH 源码
  用 AST 限定为两项剪贴板用例和两个专属函数，其他认证/密钥/清理字节必须不变；共享
  工具、Agent、产品源码变化、不同故障及未清理状态均拒绝。实际修复 diff 范围通过。
  `policy` 检查、桌面用户 `tooling` 组及最终编排资格通过；首次组合软件调用误在沙箱
  内触发 WSL 身份拒绝，已按主机身份规则重跑工具组，不是产品/真机失败。原报告继续
  为 failed；新 R2 尚未运行，须使用合入后的干净工具检出和 10 分钟内的真实状态审计。
  2026-09-22 维护者批准合入并继续；PR #223 已 squash 合入 `14a8591`，独立工具检出
  固定到该干净提交，产品候选仍为 `678736a` / `3a6db18f…ee6514`。新鲜只读审计确认
  本轮夹具、监听端口、临时信任和 HDC 映射均不存在，单 Tab/Pane 与原通知关闭状态
  已恢复；八月的三个无关历史目录未改动。新 R2 已在发布区
  `1.7.0-automatic-clipboard-r2-20260922/` 启动：严格校验并复用候选及 16 个独立行为
  阶段，重新 QH、完整 SSH、6 项自动 Mosh，最后接人工锁屏/合盖；新增模型请求为零，
  零自动重试。原失败报告及全部前段证据哈希保持不变；C4 仍以完整物理门禁通过为前提。
  R2 的新 QH、SSH 诊断、17 字节按键粘贴及 1 MiB 传输主路径通过；随后在 `ssh-escape`
  的“备用屏启用焦点报告后应立即收到 FocusIn”工具假设处停止，清理通过，Mosh 未启动。
  **本批阻塞修复（20–30 分钟，零模型）：** 原生 `TerminalRuntime::Focus` 仅在实际
  焦点变化且模式开启时发送事件，符合 XTerm FocusIn/FocusOut 的事件定义；现有场景没有
  制造焦点变化，却把其副作用作为行首判据。用户合同是备用屏上的 SSH escape、远端输入
  和会话隔离；不要求启用模式立即回报。最后正确边界为夹具发出 dirty 序列，首个错误
  判据是强制等待 `[I`；输出由原生 VT 拥有，行缓冲由受控服务器拥有。
  最小修复复用现有精确原生标记搜索（将函数名称收敛为输出标记观察），确认备用屏输出
  后发送一次换行，以新鲜服务器空行快照证明边界，再保留全部 escape/输入/隔离检查。
  不增加焦点兼容分支、延时或重试；先做真实脚本反例与同包 `-Only ssh-escape` 诊断，
  不热改冻结工具或继续原矩阵。若需产品变更或扩展续验入口，先按新证据重审成本。
  **后续续验成本决定：** 已有 R2 入口锚定最初失败报告，其独立前段未执行 `ssh-escape`。
  本批最多再用 15 分钟将 AST 比较的允许范围增加这一个阶段，原首轮失败/清理/候选/
  前段哈希及零模型条件不变；不支持从新失败手工跳过剩余 SSH 子项。用真实源码证明
  其他认证/清理代码逐字节相同，并以离线正反例限定允许范围。下一轮仍从原首轮报告
  创建新 R2，重新 QH、完整 SSH 和后缀，本轮失败报告另行保留和关联；诊断不充当正式
  通过。若限定入口无法通过资格检查，则不自动退回重复模型的整轮。
  修复后同包 `escape-diagnostic/device-ssh-auth.json` 的完整 `ssh-escape` 诊断通过
  （164.5 秒），覆盖备用屏帮助、远端输入、主动断连、重连及双 Pane 隔离；清理通过、
  Preferences 摘要前后一致，零模型。标记观察失败、快照缺失/非空的真实脚本反例通过，
  续验策略 74/75 项及真实编排 60 项通过；实际原/新 SSH 脚本在五个明确所有者之外
  逐字节相同。新边界拒绝认证路径修改、重复阶段所有者和语法损坏。协议依据为
  [XTerm FocusIn/FocusOut](https://invisible-island.net/xterm/ctlseqs/ctlseqs.html#h3-FocusIn_FocusOut)。
  本轮正式报告仍为 failed；修复后的正式续验尚未启动，须合入后冻结新工具并重新审计。
  维护者随后批准 #224，并授权后续范围内 PR 按既定检查、合入流程自主推进，不再逐个
  请求合入确认；发布/签名标签和 AppGallery 权限不因此扩大。#224 已合入 `47d54e1`，
  冻结工具检出干净；原产品候选仍保持 `678736a` / `3a6db18f…ee6514`。恢复上次诊断
  留下的空闲双 Pane 后，新鲜审计确认单 Pane、通知关闭、端口/夹具/临时信任清理。
  新轮 `1.7.0-automatic-escape-r2-20260922/` 已启动，续验清单同时固定原首轮失败和
  中间 R2 失败报告哈希；重新 QH、完整 SSH 与 Mosh，新增模型请求为零、零自动重试。
  新 R2 的 QH、完整 SSH 前三组均通过（传输/性能、全部认证方式、生命周期恢复），
  第四组 `bell-attention` 在重复提示等待 `Pane attention set:` 超时，清理通过，Mosh
  尚未启动；此前活动 Pane、后台 Tab、分屏提示及返回清除均通过。
  **重审与本批诊断（20–30 分钟，零模型）：** 目标是可靠验收原生终端，不能每修一处
  判据就重复前面约半小时的 SSH。先定位 BEL，而非自动重跑或扩大续验白名单。最后正确
  边界是夹具记录两次 flood BEL 已发送；首个缺失边界是 AppViewModel 的新 attention
  跃迁。定时 5 秒不足以证明两声都在失焦后；同时 NativeTerminalController 的独立
  attentionPending 可能与 AppViewModel 真相脱节。先用真实控制器软件样本验证前台
  BEL → 失焦 → 新 BEL，以及返回确认后再次失焦的状态链，再以服务器发送时间和
  当前 Pane 焦点的零模型诊断区分定时前提与产品丢弃；不先增加延时或修改产品。
  如产品需改按 R4；如仅本地工具前提错误，优先使用已有组级恢复边界，提交前明确
  跨工具证据复用成本和资格范围。保留本轮三组通过及失败原始证据，不手工填 Pass。
  软件复现确认前台 BEL 将 native gate 置为 pending，即使 AppViewModel 没有待处理提示；
  随后失焦的新 BEL 被抑制。零模型真机时间诊断同点失败且清理通过：第二条命令提交后
  37 ms 第一声 BEL 到达，右 Pane 焦点已于 `1790012721427` 确认，第二声于
  `1790012724812` 到达（晚 3.385 秒），仍没有新 attention。证据在 `bell-boundary/`；
  临时服务器时间与焦点观测代码已按哈希恢复。原测试定时使第一声在前台，但第二声确在
  后台，因此不能仅延长等待或把它归为测试前提问题。
  **最小产品修复：** 在真实离开焦点时重新允许下一次 BEL 回调；重新获得原生焦点时
  复用现有确认操作。离开时不清 AppViewModel attention，重复 blur 不重新放行，保持
  同一后台焦点区间内的重复 BEL 合并和 owner 校验。范围限控制器两处焦点生命周期，
  不加新状态、重试或兼容路径；先完成软件红绿和同场景新签名包回归，再按 R4 准备候选。
  软件红绿确认旧代码失败、新代码通过；受影响 policy/arkts 组通过，包含 25 项原生控制器
  合同与 186 项 ArkTS 测试。增量 ARM64 签名构建、安装通过；同一 BEL 命名场景及
  后台通知回归均按零模型执行，结果在通过后记录。后续正式 R4 仍采用固定 9 项必要
  模型用例、零自动重试，人工锁屏/合盖留待自动阶段通过且维护者可操作时执行。
  全量重新验收的原因是产品字节变化，不能将同包工具续验的通过项移植到新包。
  新包 `a0dd00e9…2e7e62` 的原 BEL 场景全部通过（82.5 秒），包括前台短暂提示、后台
  Tab/分屏提示、重复合并和返回清除；Preferences 不变，夹具/端口清理通过。后台系统
  通知发布、卡片返回及清理也通过，通知设置与屏幕超时恢复。定向证据分别为
  `bell-boundary/fixed/` 与 `notification/`，均不是正式候选通过证明。
  PR #225 四项云端检查通过，已 squash 合入 `36de654`，本地与远端 main 一致；三个
  独立发布检出已冻结到该干净提交。R4 的 C1 完整软件门禁 33/33 通过，C2 干净
  ARM64 签名构建、安装和保留通过，新候选为 `0e1b7878…eb9f5a`。启动前只读审计
  确认夹具、端口、临时信任与 HDC 映射清理，单 Tab/Pane 和原通知关闭设置恢复。
  `1.7.0-automatic-bell-20260922/` 已启动新 QH 与完整自动矩阵，固定 9 项模型用例、
  零自动重试，不继承旧轮行为通过。新人工后缀已绑定本轮提交/HAP，四条现场提示
  离线自测通过；尚未启动人工操作或 C4。
  新 R4 在默认 ECDSA Identity 导入处停止，此前 QH、密钥口令/注释/重启、ECDSA 加密
  导入及 Host Identity/OpenSSH 已通过。新轮尚未调用模型。失败日志给出真实解析错误
  `parse private key failed: length invalid`；后续清理等待不存在的删除成功信号而报错，
  独立审计记录 id_ecdsa/导入源不存在，原 id_ed25519 已恢复且摘要匹配，Downloads
  权限恢复。原报告保持 invalid/interrupted，先补独立资源审计，不直接恢复矩阵。
  **本批有界诊断（20–30 分钟、零模型）：** 预期标准 ssh-keygen ECDSA 私钥可导入；
  最后正确边界是文件发送和命令提交，首个错误边界是锁定 ssh-key 解析器拒绝长度。
  解析器拥有编码有效性、KeyCommandService 拥有导入提交。先核查本轮残留，再比较
  标准生成器与锁定解析器及上游编码规则，区分随机标量编码、文件传输与工具参数。
  仅用隔离临时测试密钥，不读取或输出用户私钥；找到确定原因后再决定最小修复与
  R1–R4 范围，不换随机密钥反复撞通过，不增加兼容分支或扩展依赖。
  固定向量诊断已闭合：OpenSSH 接受 31/32/33 字节三种合法 P-256 整数编码，产品
  锁定的 ssh-key 0.7.0-rc.11 只拒绝 31 字节。相同短整数在原候选真机上复现同一错误，
  传输前后 SHA-256 一致，诊断清理通过；原随机失败密钥已清理，未事后读取其编码。
  上游当前源码仍限制最小 32 字节，没有已验证的现成修复可直接采用。此为内置库处理
  已支持输入的产品责任，不能当第三方豁免或不断换密钥撞通过。独立残留检查已确认
  临时密钥/导入源/夹具/账号/监听/映射均清理；原报告仍保持 invalid/interrupted。
  结论、标准依据和维护成本见 [ECDSA 编码诊断](design/ecdsa-private-scalar-boundary-20260922.md)。
  维护者已明确批准本地依赖补丁，不向上游反馈；要求尽量不干扰依赖升级、上游修复后
  可低成本删除。采用 Cargo 原生单库 `[patch.crates-io]`，保留该发布包、许可证、来源
  摘要及独立 diff；应用不增加密钥转换路径。行为回归放在产品测试中，撤除覆盖后继续
  有效；源码校验防止补丁副本漂移或升级后静默失效。先完成编码/导入/认证定向回归。
  同链工具清理修复限 20 分钟：先审计自有密钥是否存在，未创建时无需等待删除；任何
  部分创建仍走产品删除并再次审计。原导入失败和冻结报告保持失败，禁止伪装通过。
  当前候选正式门禁尚未通过，本批先交付修复和定向证据，不自动启动完整矩阵。
  本地补丁已实现：119 个发布包文件中仅 ECDSA decoder 改动，独立 diff/来源摘要和
  Cargo 覆盖集中在 `third-party/ssh-key/`。源码漂移、补丁变动、锁文件切回 registry
  会失败；原生构建摘要包含经校验的补丁来源。永久行为测试留在产品 core 测试中，
  升级与删除步骤见该目录 README。软件红绿、42 项 core/known-host/边界测试、56 项
  native 测试及 2 项原有定向触发测试通过；12 项真实清理代码反例、源码门禁和构建
  工具回归通过。独立 cryptography/OpenSSH 对照的 P-256/P-384 31 字节、P-521 64
  字节共 6 个明文/加密样本均完成产品加载及签名验签，临时文件清理通过。P-521 最初
  的 31 字节测试值被 OpenSSH 自己拒绝为曲线值不合法，故该曲线改用合法短编码样本，
  不将整数解码测试等同于 OpenSSH 私钥强度策略。证据在 `ecdsa-boundary/`。
  测试 PC 预检未发现设备；维护者说明正在充电并要求先推进非真机流程。ARM64 签名
  构建已通过；补齐许可证修改标记后按最终源重新打包。PR 保持待真机验证状态，随后
  用固定 31 字节 P-256 做导入/认证/重启/清理，再跑现有加密 ECDSA 命名场景；零模型。
  09-22 设备接回后，固定 31 字节 P-256 的 11 项导入/OpenSSH 认证/默认选择/重启/
  恢复检查通过；现有加密 ECDSA 场景的错误口令拒绝、重启后认证与 Preferences 不变
  通过。两轮成功场景清理通过，原 Ed25519 密钥/指纹/Host 配置和 Downloads 权限恢复。
  首次固定夹具因 Windows 挂载权限被 OpenSSH 拒绝，未进入目标导入，清理通过；改用
  Linux 0600 临时文件离线确认后再验通过，原失败证据保留。详见上述诊断文档。
  PR #227 的定向真机缺口已关闭；本批零模型，不启动新正式矩阵，旧报告保持原状。

MatePad 仍待合格物理测试机，其他扩展须独立准入。
